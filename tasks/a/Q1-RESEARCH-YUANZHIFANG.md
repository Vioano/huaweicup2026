# P1 结构构造与成绩反馈

Task: `a-q1-research-yuanzhifang`。本人新授权，session `yuanzhifang30-sudo/s-bdf7e1f4203b4ffda133950db055bd66`。

1. **目标**：结合 Pro 的 Task 门控/边界勘误和成绩台记录，降低 P1 官方 Makespan 与完整求解墙钟；独立于队长现有 `src/q1/`，交换正负证据。
2. **输入**：固定主线 `45f647b395b84e9569f418fd33d62c2b8eb4d190` 的冻结图/config/E0、Pro1 R3 及 Pro4 Q1 审计/最新综合、2026-09-24T13:54Z 成员本机成绩台快照。该快照部分原件未到，不能当中央完整榜。旧 fixed64 求解器固定 `4dff90ef699fd51845cf482951e8477066f5f566`。
3. **输出**：`src/q1_yuanzhifang/`、对应 tests/docs/results 及论文段；完整计划、未修改官方 result/trace、运行收据和 board-submission-v1。来源、合法性、周期、字节和墙钟分别记录，失败保留。
4. **限制**：不写官方、他人算法、评估设施或中央成绩台。本轮 Stage A 新预算：公开开发图 001/002/008/010/026/044/051/094，均4核；`component-pack`、`chain-wave` 各一个确定性方案，以及旧 fixed64 同机对照，共最多24次 solver + 24次外部E0。单次solver 20秒、E0 40秒，串行1worker，全批最长10分钟；超时不重试，非预期监督失败停止全批。0 E1/E2/训练/GPU。不复用封存预算，不因负结果追加随机搜索。线上构造不用任何已测成绩选择参数。
5. **验收**：结构守卫检查 COPY 路径收缩、全部核链和覆盖；单元测试包含跨层依赖与反例。由子Agent在固定代码后独立执行该小批、核验原件、导出预检；作者分析质量—耗时与机制失败。八图是公开开发集，不是盲测或100图最终成绩。外部E0独立计时；对照必须保留同条件负例，不用跨平台时间宣称提速。
6. **节点**：本轮交付一轮可复跑研究与实测，再依据证据确定下一算法增量；新批次另冻结范围，不能无目标空跑。中央接收由 `s-7c98…`，科学验收由队长路由。

## 初始假说

P1 每个Task重新构造边界COPY且等待前驱整体完成。细碎链包可能引入很多100/1000周期门控；随机fixed64可能将同一链散在大量Task中。两个无评分构造用于检验该机制：独立弱分量按计算负载打包；或无分叉链收缩后，将相同依赖深度的包合并为至多k个Task。相同深度的包不可互达，所有Task边向更高层，投影的核序联合无环。该证明不保证容量、spill、全局DDR或性能，完整E0仍必需。过粗的包可能产生内存压力和延后出口，必须保留退化结果。

## Stage B：首轮反馈后的固定图条件选择

Stage A 已结束：24/24 solver与24/24 E0成功，82.9444秒，无重试。component-pack在002退化至261945；chain-wave在044退化至135220。组件数不足k时整组件法无法启用所有核；组件数充足时优先保留完整独立分支，可避免同层打包把独立分支的中间张量散到多Task。由此冻结 `structural-switch`：若弱分量数≥k用component-pack，否则chain-wave。是本轮开发数据启发的规则，未证明普遍支配；不能用事后每格最佳代替该规则。

新验证面板在运行前固定为019/048/071/080，均k4；选择这些Pro既有公开图检验结构迁移及潜在退化，不称封存或盲测。每图只生成structural-switch与原fixed64各一次，最多8 solver+8外部E0；两者的固定SHA、进程计时、单worker、20秒/40秒和不重试语义同Stage A，新全批上限5分钟。Stage A额度已封存，两批分开保留。先与本机P2沟通错峰再开跑；不因个别结果不佳扩图或扫参数。其余性能解释沿用本卡及官方双指标口径。

## 本轮交付状态

Stage A固定原件9b07b791、Stage B固定原件0441891f；24+8次solver及外部E0全部成功、无重试，总批次墙钟111.5419秒。B两胜两负，不推广组件数阈值为可靠通用规则。11项单测通过；更正COPY桥内部关键路径后，32份静态诊断通过，未新增评分。代码、原件、逐例表、下界机制反馈和论文段齐备，任务Issue98持续保留队长沟通。成员本机入库、中央接收与科学验收以各自回执为准。

研究结论与下一假说见 `docs/a/q1-yuanzhifang/RESEARCH.md`：048/071的当前Task图已被下界证实无法赢fixed64，后续优先改变释放边界，避免仅调DDR；051保留新的324823方案作为基点。不启动第三批，不将此12图公开开发研究称为100×1–5核的最终验收。
