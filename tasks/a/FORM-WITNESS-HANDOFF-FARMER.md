# FORM 既有反例的可复用交接

负责人：@farmeruncle123；session `farmeruncle123/s-e7e5e4b76974459f9f6757cfdc31a84a`。沟通 [Issue #14](https://github.com/huaweibei123/huaweicup2026/issues/14)。按本人 WorkBuddy/CodeBuddy 能力与当前 Git TLS 阻塞，安排有限范围的证据交接。

1. **任务目标**：把已完成 FORM 中最有用的四个反例准确交给求解器/E1/E2 作者，回答“哪些能直接用作公开输入回归，哪些只是内部机制探针，分别能证伪什么”。不重复整套规则汇报，不重做实验。
2. **输入文件**：本人固定 `65d6c0e6facee2ec8ce9694c30dd805de99abf7e` 的 `formal/SPEC.md`、`formal/rules.jsonl` 及对应脚本/结果。四项为 F-PLAN-005 商图成环、`tests/adversarial/ranking-inversion-pair.json`、F-RESOURCE-001 DDR 竞争回改结束时刻、F-RESOURCE-008 Q3 在飞 cache hit 退休重插。最小阅读仅这四项及其已有原件；无需重新读 39 条、Pro 长文或新源码。
3. **输出要求**：不超过四个主体条目的 Markdown 交接表，每条列适用问题/核数/容量等前提、实际输入与预期观测、原命令、固定 SHA 文件链接、能否直接交官方公开 CLI、不能外推的结论。如只有内部探针，明确缺少的公开 graph/plan/config，不为凑齐而新造样本。优先复用已有表格，只补缺失的可复用边界。最终可在 Issue #14 正文交付，队长核对后归档；本地文档如需保存仅用本人 `docs/a/form/witness-handoff-20260924/`。
4. **限制条件**：30 分钟资料整理预算，0 新样本、0 重跑/评分/worker/构建。只依据已存在结果，注明作者报告尚非全量独立验收。Git TLS 撤销检查错误未解决前继续暂停 Git fetch/pull/push，不降级证书校验、不密集重试；允许使用当前正常的 gh API 读取固定文件与发 Issue。无需修系统证书、搬工作区或启动常驻服务，不修改 E0/公共真值/其他人的源码。
5. **验收标准**：四项来源和版本能定位，内部探针与公开输入不混同，结果/适用域/缺口明确；不存在的文件或未做过的实验如实写缺失。队长把可用项路由给实际接收 session 后另记接收/复核状态，本次资料交付不等于 FORM 最终验收。
6. **截止时间**：原 Issue 回实际已读/理解与开始时间，从本人实际开始计约 30 分钟交付或具体阻塞；完成即等待具体触发，不为等信空跑。若既有材料不足，报告精确缺口即停，不自行开启实验。
