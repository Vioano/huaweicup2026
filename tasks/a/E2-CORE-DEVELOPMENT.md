# a-e2-core-dev：E2 核心开发与交接测试

2026-09-24 用户最新分工：队长评估器专项负责 E2 开发，原队友在交付后独立测试。通信仍用 [Issue15](https://github.com/huaweibei123/huaweicup2026/issues/15)，沿用 session-v1 和已有本人授权，不新建账号、权限或后台服务。

开发：`nikolastarx/s-55b66a31d7bd49019122a179563dc1d2`；测试：`lyx0217/s-89ad75751f054b6b9929e42d899246be`；状态及写权交接由队长调度汇总。

1. **目标**：研发有实际候选筛选收益的高速 E2，交付核心方案、实现与开发验证，再由 LYX 在独立工作区测试。E1 工程化继续由 LYX 单写；本任务不抢占 `src/eval_exact/` 或 Q1/Q2 生成器。
2. **输入**：当前冻结材料、官方 I/O 与 [补充指标](../../docs/a/EVALUATOR_AMENDMENT_20260923.md)；旧 E2 固定 `d83d5f32a1c23f6450aa9c15fa85891c4ebddd7f` / PR20 的 rank、event、原生小核和公开候选池；[完整 Pro 索引](../../AI%20chats/README.md) 及相关已复核勘误。已有作者数据、专项复现与新开发验证分开，不把 Pro 建议当源码事实。
3. **输出**：独立研究目录和不覆盖的结果目录；方案/近似清单、固定实现提交、输入及代码/环境哈希、种子、完整命令、成本与误差/排序结果、失败/回退边界、给独立测试者的清单和 Draft PR。生产目录为 `src/eval_proxy/`、`src/eval_proxy_native/` 及对应测试，但仅在旧方停写核实后的明确交接点开始写入；交接前只在本专项独立研究范围工作。
4. **限制**：E0/config/raw、公共真值和封存集不变；不能把 rank_only 伪装 cycles、unchecked 伪装合法、超时/崩溃伪装 invalid。保留精确回退与最终 E0 确认；计划相关构图、特征、数据移动、准备/传输/同步计入相应成本，冷/热/批量/CLI口径分开。旧代码/未提交工作/进程先保全并交接，不能因离线默认释放生产写权。研究方法和语言可自主选择；不要求为了E2标签强行引入近似，精确评分路线也可验证。若与E1共用内核，必须明确共享层与单写范围；准备接口对准备接口、CLI对CLI作可比测量，不能通过让E1多输出完整JSON而把不同工作量的比值冒充等价加速。队长专属资源仍按既有授权和明确预算使用。
5. **验收**：同机、同版本、同配置 E2 ≥10× E1，Makespan 相对误差中位数≤1%、P95≤3%；≥30×为探索目标。保留 C++/Rust小原型与至少两个算法方向的真实对照要求，未完成如实列出。候选池必须完整标注 E0真值，报告短名单损失、失败域和端到端收益，不能只评价被选中的候选。先做开发验证，再由 LYX 对固定交付独立运行；缺少生产写权交接、测试或封存证据不置 done，不把三例/单图结果外推全域。
6. **检查点**：专项自行报告实际 T0、当前研究分支/HEAD、第一份有限实验预算与预计交付；无新增硬截止。首份可证伪结果后再决定扩展，不要求等 LYX 空确认才能做独立研究。原执行者实际停写回执与生产写权切换单独记在 Issue15；若收到冲突或未交接产物，先保全后路由。

本任务卡只调整分工与交接，不代表 E2 已经达标，也不撤销 LYX 现有 E1 工程化安排。
