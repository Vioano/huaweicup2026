# a-r1-oracle-test：队长公共接口与独立验收

负责人：@NikolaStarx
分支建议：`codex/a-r1-oracle-test`
沟通：两位成员任务 Issue 关联本任务 ID；成果经独立 PR。

1. **任务目标**：使 FORM/E1/E2 能在同一输入版本、接口和判据上交接，保留独立于优化实现的真值与验收。
2. **输入文件**：本轮冻结官方源码/config/题面与 `docs/a/source-manifest.json`；`docs/a/contract-v1.md`；两位成员公开开发产物。
3. **输出要求**：请求/响应 JSON Schema 与公共 `src/team_eval/` 适配；独立输出路径的 E0 调用；两组导入烟测的可复现重建及黄金输出；拓扑分块、负载调度、通信聚合等简单独立候选；分层差分/反例包/计时框架；按图或图家族拆分封存池；实际独立验收报告。
4. **限制条件**：不把导入审计自述当作已复现；不修改 E0/config；封存集不用来日常调参；变更公共契约须版本化并通知影响。队长侧不代替两位成员开发各自主产物。
5. **验收标准**：来源字节可复算、公共模式语义明确、E0 调用可复跑、失败/超时分类有据、比较器经过已知差异检查；最终 E1/E2 分别报告一致性/筛选质量与总成本。任务分配成功不等于这些实现已完成。
6. **截止时间**：优先公共 Schema/E0 接口与首批 smoke，使成员首轮交接可验收；具体时间随实际成本回报。尚无硬截止。

## 初始进度

来源冻结与可共享输入已在任务包准备；可执行 Schema、E0 适配和 smoke 尚未完成。此卡不是完成声明。
