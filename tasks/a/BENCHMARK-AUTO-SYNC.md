# 方案成绩台双向自动同步

负责人：NikolaStarx，session nikolastarx/s-c04feaf0736b49fa838f7fed36c9f5a2。网站接入协作：s-7c98；Fang本机接入：s-c909。

1. **任务目标**：用户明确要求队长与Fang网站代码及成绩可靠近实时双向热更新，正常运行不依赖Agent手动传包或模型轮询。
2. **输入文件**：既有board-submission-v1、固定官方输入/配置、中央只读账本、双方本人GitHub身份、已发布站点代码与现有结果目录。
3. **输出要求**：独立src/benchmark_sync常驻发布/接收/outbox/代码更新与故障状态、紧凑全历史快照、跨平台接入文档和实际双端回执。src/benchmark_board与前端仍由s7c98单写。
4. **限制条件**：不触发新solver/E0/E1/E2，不减少原件/准入标准，不清库覆盖历史，不合并成员SQLite二进制。GitHub只用免费功能，禁Actions/收费服务。各自身份与私钥不共享。更新先校验/健康检查，失败保留上一可用版本；队长离线时排队并显示延迟，不冒称硬实时。
5. **验收标准**：已有完整数据与同版UI真实可见；双向真实已有benchmark传输/中央回执/双方可见；自动重复与断线恢复不丢不重，损坏/冲突/回退拒绝且不阻塞无关有效包；无变化不重复大下载；正常同步0模型调用。脚本测试与Windows两人运行验收分开。
6. **截止时间**：本轮持续实施到可交付并完成可访问端验证；Fang本机部署由其本人已授权执行会话实测，不以消息发出代签完成。


2026-09-24 持续验收修复：Fang e777 提供63个完成项阻塞最后待办的反例。接收器改为未完成优先、持久轮转及不可变已验签回执缓存，加入慢验证/跨进程续传/信任变更复核测试。中央实际交接暴露端口检查误判TIME_WAIT；改为检测真实TCP监听，实际网站bind仍最终拒绝竞争，不杀未知进程。macOS Python3.12同步26项测试通过；Fang真实Windows换版和双向回执继续在Issue33实测，未把单测写成双机验收。


2026-09-25 原件吞吐补修：真实Fang P1 StageC交付含127个原件，串行逐文件GET在多个待办间积压。仅将固定提交的原件读取限为4路并发，签名、Git写入、游标和request发布仍单线程；逐文件SHA校验、断点复用与最长限流退避保留。新增并发上限/未完成不公开request和部分成功后断点恢复检查，不追加solver/E0。
Fang正式Windows同时回报真实TLS EOF后自动恢复；安全GET追加最多3次瞬时断连重试，证书验证与写入不放宽。
e777 Windows实测关闭回环端口约2.047秒才返回拒绝连接：端口探测改5秒等待明确结果，真正超时仍拒绝启动。c909指出发布时清空固定SHA目录树缓存，补有限16项LRU及发布前后GET计数回归，保留固定内容核验。

2026-09-25 监督器与Windows原子更新补修：e777/c909复现status.json读句柄引起WinError5；共享原子写入增加仅针对WinError5/32/33的10次有界重试，总等待2.62秒，持续拒绝保留旧文件并报错。另修active已升级但监督器仍运行旧模块：稳定独占launcher保持原生等待链，监督器健康切换后停止owned children并以75退出，由父进程启动active的新监督器；新监督器启动失败退回previous，不回退账本。新增真实子进程换版/候选导入失败/启动失败回退/重复父进程拒绝测试；Windows真实占用测试在macOS明确skip，待Fang执行，不冒称Windows已通过。旧launcher首次须唯一部署者一次受控迁移，后续签名版本自动切换另验。
