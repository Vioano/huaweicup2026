# A-Q2：情况 B 构造算法与有预算搜索

负责人：@yuanzhifang30-sudo。队长于 2026-09-24 授权，覆盖此前暂不分配的安排。

Task ID：`a-q2-core-search`。分支：`codex/q2-core-yuanzhifang`，本人独立 worktree。

沟通 Issue：[#33](https://github.com/huaweibei123/huaweicup2026/issues/33)。交接与起步命令：[Q2_HANDOFF](../../docs/a/Q2_HANDOFF.md)。成员自己的 session 地址由本人在 Issue #26 登记，队长不代造。

1. **任务目标**：独立研发问题二/情况 B 的合法构造与有预算搜索。先跑通官方 Q2 基线和语义对照，再研究核心归属、子图优先级/粒度与内存/通信之间的取舍。在相同总预算下比较官方确认的方案质量，不以候选数量或代理分数代替效果。Q1/A 并行继续，不等 A 最优，不等 P2 加速器；本任务不是共享 evaluator 重写。
2. **输入文件**：冻结题面、官方 Q2/Step1–3/校验源码、config、100 个官方 case 和 source-manifest；固定来源及最短阅读顺序见交接包。FORM 与四路 Pro 只是带版本的规则/研究线索，先核适用条件与反例。首批使用公开开发例，不读队长封存集调参；cycles、字节、墙钟秒分别记录。
3. **输出要求**：`src/q2/` 的构造与搜索入口、`tests/q2/` 的必要语义/回归用例、`results/a/q2-yuanzhifang/<run-id>/` 的计划/完整 E0 输出/日志/命令/输入配置代码哈希/环境种子、`docs/a/q2/` 的解释与失败、`paper/sections/a-q2.md` 的方法与局限。汇总至少记录 case、核数、候选来源、状态、makespan、搬运/spill、提议与评价时间、总时间、评价次数、实际资源预算。保留最佳合法 incumbent 和无改进/非法/超时记录；最终方案由未修改官方 Q2 CLI 确认。用 Draft PR 交付固定 SHA。
4. **限制条件**：保持官方输入输出格式与参数；不改冻结 E0、原件、公共真值、`src/q1/`、共享 `src/eval_exact/`/`src/eval_proxy/` 或他人结果。公共依赖/接口/契约改动提小 PR 或建议由队长整合。P1Evaluator 不用于替代 Q2。运行前写明核数、seed、候选/时间/内存预算及计时范围；超时非非法，未知结果非成功。队長 Colab/Pro/镜像权限不自动共享，算力与专项研究需要时由队长代跑。方法选择自主，允许有证据地放弃无收益方向。
5. **验收标准**：第一检查点完成材料核验、官方格式冒烟、A/B 差异表，以及固定核心归属下粗/细子图优先级的最小 Q2 对照；至少包含核内拓扑/全局等待环与内存或 COPY/FIFO 压力边界的明确检查。首轮原型在至少三个预先说明选择理由的公开开发例上，对照随机格式示例及一个明确的确定性合法构造，报告不同方案的有效性、质量和全流程成本；随机 stub 不是强基线。随后扩大结构覆盖、比较至少两个有实质差别的提议方向（可用证据替换），保留退化例，不预设必须实现某 Pro 方法或承诺固定改善百分比。理论主张写假设、证明/反例与官方适用范围。实现者自测、跨会话复核、队长最终验收分开；E1/E2 门槛不用于冒充构造算法已通过。
6. **截止时间**：无新增硬截止。建议实际接手后 60–90 分钟给首个有证据检查点，约 3 小时给可复跑原型或具体阻塞；首次实质回复给实际 T0、预计下一检查点（Asia/Taipei），可按设备和工作量调整。没有新证据时不空 ACK、不重复催跑；遇同一方向反复无进展，保全记录并交队长调整。

## 协作与交付记录

- A 执行会话 `nikolastarx/s-6607cb2735304751b36662035723372b`：继续 Q1/A，提供固定版本与可复用图层发现；不写 `src/q2/`。
- 精确评估器会话 `nikolastarx/s-55b66a31d7bd49019122a179563dc1d2`：现有共享模块单写者，不因本任务启动 P2 重写。
- 队长协调 `nikolastarx/s-a5bdb19389ee43d686b7976d3bcdf766`：Issue #33 路由、公共资料与接口整合、Pro/算力代跑和独立复核安排。
- FORM 的现有规则与反例按固定 SHA 读取；新增复核由队长明确交接，不把离线成员自动认定为已接受新工作。

成员接手后在 Issue #33 填实际分支/HEAD、已读/未读、关键风险、初次命令和结果。Atlas 初始 `todo` 表示已指派待回执；本人开始后才 `doing`，交付后 `review`，独立验收后才 `done`。公钥未核对前不虚构授权。
