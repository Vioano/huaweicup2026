# System Atlas：核心哲学与独立 Agent 产品构想

> 给 ChatGPT Pro、潜在贡献者和产品讨论参与者的背景材料。
>
> 整理日期：2026-09-20。当前实现：System Atlas 0.3.0。
>
> 仓库：https://github.com/NikolaStarx/system-atlas
>
> 这份文档根据创始人与 Codex 的设计讨论、明确提出的需求和当前源码整理。它不是逐字访谈。下文区分三种内容：**已明确的理念**、**已实现的能力**、**待讨论的产品推演**。尤其不能把未来构想当作当前软件能力，也不能把整理者的技术建议当成创始人已经决定的路线。

## 1. 我究竟想做什么

System Atlas 目前是一个用户级系统设计 Skill：用交互图理解系统，用结构化文件保存系统设计，让 Agent 直接查询这些结构，并把模块细节、代码来源、验证证据和尚未完成的部分联系起来。

后续希望把它发展成独立的软件 Agent 产品，**以 Graph Engineering 为核心组织方式**。这里的 Graph Engineering 是本项目的工作概念：把工程对象及其关系显式建模，使图可以被人理解、被 Agent 查询、随着工作更新，并承载可追溯的变化和证据。本文不预设它已经是一个具有唯一标准定义的行业术语。

可以用一句话表达方向：

**让人与 Agent 围绕同一份可探索、可查询、可演化、可验证的系统模型共同工作，并始终能够回到具体来源、具体改变和真实结果。**

当前以“系统设计”为切入口，是因为它天然涉及层级、依赖、接口、数据流、反馈、不同观察角度以及设计与现实之间的差距。未来产品是否还应承载任务执行、运行事件、协作和其他知识，应由实际使用价值决定，不能因为有一张图，就把所有东西强行塞进图数据库或节点编辑器。

## 2. 为什么从架构图开始，又为什么不满足于架构图

复杂工程中的信息通常分散在对话、设计文件、源码、测试、运行日志和人的脑中。用户看一张图时，会自然地追问：

- 这个模块负责什么？输入输出是什么？哪些内部步骤值得展开？
- 它和旁边的模块是什么关系？某条链路经过哪些边界？
- 这个节点属于谁、出现在哪些视图里、内部是否还有子图？
- 这是目标设计、已有实现，还是已经验证的运行事实？
- 我上次看过以后，究竟新增了什么、删除了什么、改变了什么？

如果这些答案仍然需要每次重新读完整个仓库或重新解释整段聊天，图就只解决了展示问题。System Atlas 想把“看得见”继续推进到“能精确定位、按需读取、解释变化、关联证据”。

独立 Agent 产品的设想是在此基础上继续推进：Agent 的上下文、建议和工程操作能够围绕明确的对象、关系、版本与范围组织。**这一执行层目前尚未完整实现。**

## 3. 已明确的核心理念

### 3.1 人与 Agent 共享权威模型

Human Interface 和 Agent Interface 应当从同一个权威后端读取图，不能分别维护两套互相漂移的事实。

人适合通过空间布局、颜色、折叠、展开、详情面板和手势来理解系统；Agent 适合通过 JSON、稳定 ID、查询参数、分页、版本与差异来理解系统。输入输出形式可以不同，底层语义必须一致。

“拓扑和图结构等效”需要严格限定：

- 在相同版本、相同过滤范围、相同展开状态下，节点身份、边的端点、方向、类型及包含关系一致。
- 宏观折叠可以聚合节点和关系，但应保留回到原始对象的映射，不能凭视觉方便编造连接。
- 某个视图没有显示某条边，不等于这条边不存在。
- 同一模块可以出现在多个视图中，但仍然是同一个实体。

这不是要求 Agent 像人一样操作网页，也不是要求两种界面长得相同。Agent 应优先通过结构化接口完成它能够可靠完成的工作。

还要区分两种“一致”：**图事实的一致**与**操作能力的对等**。当前版本已围绕前者建立统一后端；网页并不是完整的图编辑器，因此不能宣称所有网页操作和 Agent 操作已经完全对等。

### 3.2 权威意味着统一提交口径，不意味着图自动等于现实

后端负责决定当前接受的是哪一版图、查询返回哪一组事实、证据绑定到哪些版本。它不能凭“权威”这个名字让错误设计变成真实实现。

设计模型、源码、测试与运行结果各有自己的证据作用。模型与源码不一致时，应暴露差距并进行核对。不能悄悄把设计目标改写成当前代码的局限，也不能因为图中有一个模块就宣称代码已经实现。

当前方案让源 JSON 作为编写输入，经过校验后形成后端接受的不可变版本。未经验证的文件修改是候选，不能直接污染所有读取者。

### 3.3 从产品首尾出发，逐层向内部精化

创始人通常以产品目标、体验和日常语言描述需求。Agent 的责任是把这些描述转化为清楚的对象、关系、输入、输出、约束和验收方式，而不是要求用户先设计所有字段、算法和专业术语。

先明确用户输入了什么、系统应产生什么真实结果，再按需要展开内部模块。输入输出不仅是数据类型，也包括作用范围、发生时机、等待与失败表现，以及不能发生的副作用。

这里有一个重要约束：**图上的节点不等于一个必须独立部署的服务，也不等于必须另建协议的接口。** 内部算法步骤可以只是职责、关键不变量和源码引用。只有真实的数据交接边界需要适当明确的契约。公共定义应引用已有权威类型或 Schema，避免每个节点复制一套。

这条原则用于抵抗一种风险：图越画越细，系统反而被迫拆成大量没有实际收益的服务、队列、状态和文档。

### 3.4 图承载多种关系，不能把所有关系都当成流程箭头

包含关系、数据流、调用、依赖和反馈表达的是不同事实。一个模块属于另一个模块，并不意味着数据会沿包含关系流动；一条依赖边的方向也必须由模型语义解释。

用户最初用“链路”“闭环”描述想读取的结构，随后明确指出：这些是方便交流的说法，不是要把整个能力限制成两种结构，更不是要一次性收集图论里的所有结构。

因此，系统需要适合自身模型的少量标准策略，并保留扩展空间。图论提供严谨的工具，但算法目录本身不是产品目标。

### 3.5 Agent 的读取方式必须标准化，而且应当节省上下文

有能力的 Agent 通常知道如何拆分一个大 JSON。问题在于，如果每个任务都重新临时编写遍历逻辑，读取语义就容易漂移，预算、边界和版本也难以保持一致。

希望标准化的是“如何提出阅读问题、如何取得有边界的答案”：

1. 先取得概要与能力清单，知道当前模型规模、版本和可用入口。
2. 根据问题读取宏观结构、单个模块、附近关系、上下游、路径或循环区域。
3. 需要详细审查时再读取字段、来源和证据。
4. 再次进入同一任务时，优先读取差异，而非重复灌入整个大文件。
5. 确有需要时仍然允许全量读取；节省上下文不能成为隐瞒事实的理由。

宏观和微观应由同一份模型查询产生。无需人工维护一套“宏观 JSON”、一套“细节 JSON”和另一套“链路 JSON”。缓存或派生文件可以存在，但它们不应成为另一份需要人工同步的权威来源。

每次读取还应说明版本、范围、过滤条件、是否完整、是否分页、是否有跨边界关系。**少读一些内容，与误以为自己读到了全部，是完全不同的事情。**

### 3.6 交互丰富性来自意图区分和上下文连续

用户不应先知道所有 UI 专业词汇，Agent 才能设计合适的交互。设计者应主动把自然表达转换成适合的交互模式，并通过实际可操作界面验证。

在 System Atlas 中，这具体表现为：

- 点击模块主体只选中；查看详情有独立入口。
- 模块带有子图时，应让用户一眼看出，而不是进入详情后才发现。
- 单击子图入口，在当前图中展开简洁的下一层，保留整体上下文。
- 双击子图入口，进入完整子图视图；同时提供明确按钮和键盘替代。
- 简洁与详细显示满足不同阅读密度，不能通过简洁模式隐藏关键操作入口。
- 返回、刷新和热更新时，尽量保留选择、视角、展开状态与草稿。
- 手势、快捷键和按钮需要协同处理冲突；不能把所有大面积点击都解释为同一动作。

视觉品质是这个原则的一部分。用户已经表达对 Archify 原有主题和 Frutiger Aero 风格的偏好。增加能力不应退化为粗糙、通用、缺少层次的管理后台；也不应为了“丰富”无限堆按钮或动效。

### 3.7 设计、实现、验证、运行和用户认可必须分开

“图上规划了”“代码存在”“测试通过”“已经接入运行”“用户真正用起来满意”不是同一个状态。

证据需要带有适当范围：哪个文件、哪一版、什么检查、验证了什么、没有验证什么。文件哈希可以证明字节一致，不能证明逻辑正确。自动化通过不能自动升级成真实交互、真实宿主操作、听觉效果或用户认可。

当前模型分别记录设计、实现、验证和接入成熟度，并用来源绑定帮助识别失效证据。更广泛的用户认可和真实效果仍需具体项目提供证据；不能由图谱自行推断。

### 3.8 不确定性和恢复能力属于正常工作方式

未完成、未知、过期、断线和部分失败都应当能够被表达。一次错误编辑不应让整个图消失；一次网络或进程中断不应丢掉用户正在理解的上下文。

需要的能力包括：候选验证、保留最近有效版本、明确错误、重新连接、历史读取、冲突检查和有来源记录的回退。回退应保留前后历史，而不是把事故痕迹擦掉。

同时要尊重恢复的边界：图文件回退不等于撤销已经执行的外部动作，存储损坏也不应通过随意删除日志“修好”。可靠性首先意味着不误报、不丢失和可定位，再谈自动恢复程度。

## 4. 这个方向从哪里来

### Archify 带来的基础

Archify 提供成熟的图形渲染、主题、图类型、导航与输出校验基础。System Atlas 沿用其 MIT 许可的渲染器，并保留署名。当前仓库中的渲染能力不是从零独立发明的。

### Lecture State Supervision 带来的启发

它提供了分层查看、交互阅读，以及人机围绕权威状态工作的参考。创始人希望吸收这些能力，但明确不希望把复杂的逐步监督、生产控制流程和每一步审批整体移植过来。

“减掉工艺流程控制功能的 State Supervision”可以帮助理解早期方向，但不应成为完整产品定义。System Atlas 的中心是系统模型、系统设计和探索；生产监督只是可借鉴的来源。

### 其他工程实践提供的经验

在创始人的其他项目中，也反复出现共享后端、结构化 Agent Interface、非线性组织、先明确用户输入与真实结果、保留来源和分层验证等需求。

这些经验在这里抽象为通用设计原则。音乐软件、讲座制作或游戏开发中的具体流程，不应因此变成 System Atlas 的默认功能。特别是某一项目的业务时限、执行规则或状态枚举，不属于这份通用产品哲学的既定约束。

## 5. 当前 0.3.0 实现了什么

下面依据仓库源码描述现状，不能将其外推为完整独立 Agent 产品。

| 能力 | 当前实现 | 重要边界 |
|---|---|---|
| 结构化模型 | JSON 中的实体、带类型关系、视图、证据及成熟度 | 并非任意知识或任意时间图的通用 Schema |
| 稳定身份 | 实体跨视图复用 ID；包含关系和流关系分开 | 不自动从源码发现模块或边 |
| 常用读取 | `overview`、`local`、`reach`、`path`、`cycles`、`view`、`full` | 路径返回一个确定的最短跳数见证；循环返回循环强连通区域 |
| 读取预算 | 简要／完整字段、记录和字节预算、固定版本分页 | 边的端点可能位于另一页；超大记录明确报错 |
| 增量和变更 | 稳定 ID 的新增、删除、前后修改；版本事件通知 | 通知需要消费者订阅，不会自动唤醒空闲 Agent |
| 共享后端 | CLI/HTTP 与 Human viewer 消费同一已接受版本 | 等效限定为相同版本与范围，不是所有界面功能完全对等 |
| 热更新 | 默认每秒检查源与证据；通过校验后发布；浏览器按版本切换模型和子图 | 不是硬实时系统；外部源码改变不会自动重写架构 |
| 恢复 | 有校验的持久化历史、最近有效版本、离线读取、重连、冲突检查和回退备份 | 历史损坏时保留有效前缀并阻止发布，需要可靠修复 |
| 人类阅读 | 内联子图、完整子图、独立详情、密度与主题、缩放平移 | 操作习惯仍需真实设备和持续使用验证 |
| Agent 工作交接 | 网页记录要求，Agent 通过 CLI 接收并提交回执 | 没有自动任务调度、后台执行 Agent 或完整图编辑命令集 |

当前数据流可以这样理解：

```text
系统设计 JSON / 绑定证据
        │ 校验、构建、持久化
        ▼
已接受的权威图版本 ──────────► 版本历史 / 差异 / 恢复
        │
        ├──► Human Interface：视图、子图、详情、图数据
        └──► Agent Interface：manifest / query / diff / watch

网页中的变更要求 ──► 本地要求记录 ──► 外部 Agent 手动接续
```

当前的多个 JSON 响应是查询结果，不是互相独立维护的模型。磁盘中的历史版本是恢复与审计所需的快照，也不是不同界面各自维护的一份业务事实。

## 6. 图与时间：已经提出的问题和未完成的设计

创始人明确指出，未来图可能与时间有关；“链路和闭环”不足以严谨表达所有需要理解的结构。因此，不能只按静态流程图理解这个方向。

当前实现的时间是**模型与证据的版本顺序**：某版被接受、下一版改变了哪些事实、某个证据何时失效。它不包含完整的运行事件时钟，也不能回答“某次执行实际上经过了哪些节点”。

以下是有价值但尚未决定的产品问题：

- 系统结构、期望行为与实际执行轨迹，应该是同一模型的不同层，还是通过稳定身份关联的独立模型？
- 事件实际发生的时间、系统记录它的时间、设计版本的时间，是否需要区分？
- 一条结构上存在的路径，如何与一次实际发生的执行区分？
- 反馈边怎样关联观察、决策、动作和结果，才能证明一次特定反馈过程完成？
- 等待、并行、重试、中断、撤销和重新规划，如何表达而不把所有图变成状态机？

这些问题需要语义设计。引入“时间字段”或换成图数据库，本身不能解决它们。本文提出它们是为了指导研究，并未承诺选择事件溯源、双时态数据库或某个特定技术栈。

## 7. 从 Skill 到独立 Agent 产品：待讨论的推演

未来方向是独立 Agent 产品，这一点已明确；下面的产品轮廓是根据现有理念整理出的讨论方案，并非已批准的实现计划。

一种有价值的使用过程可能是：用户提出一个系统目标或问题；Agent 在图中找到相关范围，解释依据和缺口，提出有对象、有版本、有边界的修改；用户在合适层级理解变化；授权后完成操作并收集证据，更新共同模型。

这会要求在当前“阅读与设计”能力之上补齐一些内容：

1. **工程上下文连接。** 将图与已有源码、类型、文档和测试可靠关联，维护身份与来源；解决漂移，而不声称自动理解一定正确。
2. **有约束的变更能力。** 为增删改、冲突、前提、可撤销范围提供明确语义，避免 Agent 直接重写大文件时丢失不相关内容。
3. **行动与证据。** 执行器能够引用明确目标并返回可验证结果；设计上的成功不能代替实际动作与回读。
4. **持续上下文。** 用范围、版本、差异和来源让长任务继续工作，不依赖无限膨胀的聊天记录。
5. **人在其中的控制。** 用户能看清正在改哪里、为什么改、哪些是推测，并且能够停止、调整方向和恢复。

多 Agent 协作、后台运行和生产流程管理，都可能是未来应用层。但现在不应为了“以后可能需要”预先建立庞大的调度平台、权限状态机和策略目录。

独立产品的关键需要被证明：图结构是否让 Agent 的定位更准确、上下文更小、修改更可靠、交接更清楚、用户控制更容易。若只增加维护成本，图就没有兑现价值。

## 8. 需要避免的偏离

- **只发展展示。** 美观且可缩放的图很重要，但缺少身份、来源、版本和可查询语义，就不足以承载这个产品方向。
- **把所有内容节点化。** 一段解释、一个内部步骤或一个普通字段，不一定值得成为独立实体。
- **把界面状态当系统事实。** 选中、折叠、镜头位置通常属于阅读状态，不应该改写系统拓扑。
- **让 Agent 每次吞下全量文件。** 偶尔全量审计合理，重复无差别读取不应是默认策略。
- **反过来过度切碎。** 缺少边界关系、上下游和来源的碎片，会让节省上下文变成信息损失。
- **把推断悄悄写成事实。** 自动发现、模型判断和人工确认应保留适当区别与证据。
- **把闭环当作画了一个圈。** 图上有环，不等于动作、回读、效果和用户认可形成了业务闭环。
- **把当前 Skill 的限制永久化。** 不引入生产监督工作流是当前定位；未来 Agent 产品怎样执行，仍需单独设计，而不是永远禁止行动。
- **把愿景写成完成状态。** 未实现的协作、时间语义、自动发现和执行能力应明确保留为问题。

## 9. 如何评价后续产品是否值得继续

以下是建议用于验证的指标，不是已经测得的结果或创始人指定的量化门槛：

| 目标 | 可观察的比较方式 |
|---|---|
| Agent 更快理解系统 | 同类问题的上下文用量、查询轮数、定位耗时与回答依据 |
| 理解准确且不过度自信 | 漏掉跨边界关系的频率、把未知写成事实的频率、来源能否回查 |
| 修改更可靠 | 不相关内容被误改的数量、冲突能否被发现、回退能否恢复目标版本 |
| 人机没有事实漂移 | 相同版本和范围的图结构比对，热更新与断线恢复测试 |
| 用户持续保持上下文 | 返回后能否继续定位，更新是否丢失草稿或视角，重要入口是否可发现 |
| 图的维护成本可接受 | 代码变动后的人工维护量、失效证据发现时间、重复定义数量 |

应使用实际工程任务比较“普通文件检索与编辑”和“通过 System Atlas 工作”的效果，允许结果推翻某些图化设计。测试不能只证明自己定义的 API 能够互相调用。

## 10. 给阅读源码的模型：优先看这些文件

无需先把所有渲染器与历史测试全文读入上下文。建议按这个顺序核对本文：

1. [SKILL.md](SKILL.md)：当前定位、约束、标准阅读策略和交接方式。
2. [模型契约](references/system-design-contract.md) 与 [Schema](schemas/system.schema.json)：实际支持哪些对象与关系。
3. [Agent 接口契约](references/agent-interface.md)：分页、版本、同步与恢复的具体语义。
4. [查询引擎](design/query.mjs)：每种读取究竟做了什么，哪些关系没有被隐式遍历。
5. [权威后端](design/authority.mjs) 和 [HTTP 服务](design/server.mjs)：接受版本、持久化、通知、错误与回退。
6. [Human 投影构建](design/deliver.mjs)、[界面](design/viewer.html) 和 [交互说明](references/reader-interactions.md)：人机结构如何对应。
7. [Agent 接口测试](test/agent-interface.test.mjs) 与 [系统测试](test/system-design.test.mjs)：已经验证哪些行为，哪些没有验证。

渲染器继承了大量 Archify 代码。不要仅凭仓库中存在某个上游文件或测试，就认定 System Atlas 已经支持、暴露并验证了对应产品能力。

## 11. 可以直接发给 ChatGPT Pro 的研究请求

下面这一段可以与本文和仓库链接一起发送：

---

我正在做 System Atlas，当前是一个开源系统设计 Skill，未来希望发展成以 Graph Engineering 为核心的独立 Agent 产品。

请先阅读随附的《System Atlas：核心哲学与独立 Agent 产品构想》，再查看仓库 https://github.com/NikolaStarx/system-atlas 。请以当前源码和本文为依据，不要把产品愿景当成实现，也不要把 Graph Engineering 预设成一个已有唯一标准定义的术语。如果你无法访问某个文件，请明确说明，不要假装读过。

我希望你进行深入、可以质疑前提的产品与技术研究，重点回答：

1. 你怎样准确概括我真正想做的产品？哪些理念是核心，哪些只是当前实现方式？
2. 从现有 Skill 到独立 Agent 产品，最有价值且可验证的切入口是什么？首批用户是谁，他们会为什么持续使用？
3. 图在这里相对文件搜索、普通 Agent IDE、节点工作流和知识图谱工具，具体增加什么能力？什么时候反而不应该使用图？
4. 应怎样分离系统结构、设计意图、证据、工程变更和实际运行轨迹，既保持联系，又避免一个巨型通用 Schema？
5. 人和 Agent 共用一个权威后端，怎样进一步实现读写语义一致、版本一致和可靠恢复？请指出当前源码中的具体缺口。
6. 当前常用读取策略是否足够？如果增加，只建议最有收益的一到两个，并说明真实问题、边界和代价，不要列算法大全。
7. 如果确实需要时间与因果，最小且正确的语义模型是什么？如何避免把有向路径、循环和一次真实完成的反馈混为一谈？
8. 从“帮我理解系统”到“帮我可靠地改变系统”，最小需要增加哪些执行能力？如何保留人的可理解性、停止和转向能力？
9. 怎样处理代码与图的漂移、自动发现的不确定性、公共类型的复用和模型维护成本？
10. 请给出一条分阶段路线，每阶段都有可运行的用户场景、验证标准和不应提前建设的内容；特别指出最有可能失败的假设。

涉及外部产品、研究或行业现状时，请检索并引用来源，区分外部事实、源码证据和你的推断。不要直接替我选定图数据库、Agent 框架或大型编排平台；先证明需求与代价。请优先给出清晰的产品判断和几项高价值实验，再展开必要的架构细节。

---

这份研究的目的，是检验并发展一个方向：图结构能否成为人与 Agent 共同开展复杂工程工作的可靠基础，而不让图本身成为新的维护负担。

## 0.4 实现补记：多人协作

新增“队长电脑权威、Git 传递签名请求与发布副本”的协作协议。队员按节点与字段
获授权，提交带版本前提的小操作；队长自动验证、原子生效或拒绝，两个界面共享
已确认状态。远端整图改动不会进入队长本机权威。该能力不引入生产流程监督、
自动 Agent 调度或会话独占锁。详见 [协议与边界](references/collaboration.md)。

上文的 0.3 能力盘点保留为写作时的记录；本补记仅更新实现进度，不改变核心理念。
