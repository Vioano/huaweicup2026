# 本轮来源与文档验证

本检查用于确认工作副本和来源链接的身份，不提升原稿中任何科学主张的证据等级。

检查日期：2026-09-26；环境：本机 macOS、Python 3.14、Git；工作分支 `codex/paper-planning-20260926`，基础版本见 manifest。检查命令在仓库根执行：

```sh
python3 paper/sources/verify_sources.py
git diff --cached --check
git diff --cached --name-only
```

初轮整理分支的实际结果（后续增补见下文）：

- 3份继承原稿的Git字节/大小/SHA-256一致；导入正文与“仅固定相对链接”的记录变换完全一致，页首编辑说明另列。
- 12份固定核心文件、16个稳定聊天README的字节/哈希一致；12个新增/修改Markdown文件中的69个Git链接目标均存在。无新链接错误、无继承链接警告。这里只检查文件/对象，不验证URL锚点、网页当前可访问性或远端成员已读。
- 本次15个文件内42个完整提交ID均能解析为本地Git commit；未发现个人绝对路径。最大单文件约73KB，无复制PDF、Trace、压缩结果或聊天全文。
- 新增/修改文件限于任务卡列明范围，LaTeX模板、原作者稿、原始题面与数据均未改；暂存diff空白检查通过。
- 对本次新写目录执行普通 `dot_clean` 并扫描 `._*`、`.DS_Store`，无残留；没有清理原始资料目录或整个磁盘。

6份本地PDF单独记录原文件哈希和大小，未复制到工作树；题面在Git中，其余为用户本地材料。脚本不自动读取另一个人的绝对路径，也不把别的机器缺少这些PDF判为内容已丢失。

未检查事项：算法重跑、全部原始成绩字节复审、全部理论证明复核、所有图件再生成、Web Clip网页全量核验、外部文献新颖性与获奖身份、LaTeX编译和赛事格式验收。读到原作者“已审计”记录，不等于本任务独立复做同一审计。

## P3最终归档增补

基于已合入主库 `2789fe5efd68b32240c3bf0d5cef1c7598e8139a`，在 `codex/paper-p3-final-evidence-20260926` 只补论文索引与章节页首。来源为已通过组织Git API核实存在的 `8416300c7245925795aaa3acc64d4d5b31fa13d5`。本次直接读取 FINAL_REPORT、方法交接、summary、comparison.csv、决策表、绘图metadata与脚本，并查看真实PNG。

[检查收据](p3-final-update-validation.json)记录500唯一坐标、两配置新旧周期一致、逐核均值、Type7 p95及输入/代码/三种导出哈希的只读复核；CSV中的计划未变标志全部为False（即 `plan_changed_vs_forest=False`）。全部原方案逐字节一致仍引用作者原件审计，本任务未再逐份复核500原计划。没有新评分或绘图重生成。

更新后来源脚本检查通过：3份导入正文仍保持原字节变换关系，21个固定材料、16个聊天README与71个Git链接目标无错误/警告。来源检查涵盖图文件字节，但不表示正式图任务或论文宽度验收通过；新增总结图与6-3/6-4/6-7的差异已列明。

## P3图6-3同计划主曲线增补

基于主库 `65aaf58bf66506256953909ecb5ac2dfc0e62ab7`，读取固定图件 `d80134dca183abfcc2e2125d43d116036bd3877d`。独立确认CSV与所称2c24输入字节相同、500坐标唯一齐全、每例B跨核数一致，每K100对的mean(B/M2)和mean(B/M3)与plot-data五点一致；两个脚本、输入与6种主图/附录导出哈希吻合，已查看两份PNG。

[检查收据](p3-same-plan-figure-validation.json)记录统计和初步视觉检查通过；同计划原件身份仍沿用作者固定审计。本次不重新评分或运行绘图脚本。原图6.3英寸宽、10pt主字换算到146.6mm约9.16pt；真实论文插入效果、中文图注和正式主责/整稿验收仍待完成。

更新后来源检查通过：3份继承正文、33个固定材料、16个聊天README、72个Git链接目标，0错误/警告。图6-3具备匹配题意的研究素材，不能因此把其余图件或全文标为已验收。
