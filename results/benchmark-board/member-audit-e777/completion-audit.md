# 完整同步验收：证据与未完成项

**当前结论：本轮功能验收完成，见 [FINAL_ACCEPTANCE.md](FINAL_ACCEPTANCE.md)。** 以下是修复过程保留的分阶段检查点，曾经的“待修/待发布”不代表当前仍未解决；最终固定3794条、双端同正式版本、真实管理进程自动换版及明确测试边界均以最终报告为准。

本文件不以当前已通过的子集缩小用户目标。用户要求是所有数据、后端和前端一致、严格验证、双向自动实时热更新。2026-09-25本次续接仍处于验收中；上轮有实际源码/证据/共享交付进展，不是无进展等待。

| 要求 | 当前权威证据 | 结论/下一项 |
| --- | --- | --- |
| 全部已接收历史及原本机872不丢失 | snapshot-3154、snapshot-3209、formal-runtime-acceptance、formal-runtime-pr121；全部canonical记录/事件/旧历史扩展及SQLite逐行比对 | 已在各固定检查点证明；最终包与最新检查点仍须再核 |
| 正式成绩、分母、来源、状态及筛选一致 | 独立选择器；3154/3209全部138/156种算法/批次两模式；3626/3633默认全1500格两模式 | 先前实现通过；最终代码/固定检查点须与范围相符，不能只看计数 |
| 浏览器实际加载正确UI并显示相同数值 | 1500 DOM元组/15均值，ec5afa→9c7fd6真实自动换版；c909独立非默认筛选和Cache状态恢复 | 已有真实证据，最终DOM与HTTP/签名版本还要核对 |
| 双向数据、签名接收及去重 | receipts-first-two、receipts-duplicate、receipts-stagec-accepted；成员8笔真实签名accepted，含StageC added24/eligible23、b856 added0且2 attempt各一条 | 初始积压StageC已闭合；16:55:23 UTC固定检查点仍有3笔新研发在途，不能当已全部签收 |
| 断线恢复、不可篡改与回滚 | 真实TLS EOF后保留旧页面并恢复；独立对抗10项；正式PR121 Windows32项含候选和实际端口失败回滚 | 证据范围明确：未故意向生产投坏包，不能把夹具延迟当实网延迟 |
| 软件自动更新 | 首次接入后5e→ac2→fbc→c368真实自动部署，HTTP UI版本和签名固定源码一致 | 网站/worker已证明；supervisor自身不换版，见下文，尚不满足全部后台 |
| Windows常驻可靠性 | c909修WScript隐藏等待包装；本人实读Task Running和WSH→launcher→supervisor→children进程链；PR127 Windows39/39含真实读句柄 | 实际任务启动已证明，未登出验证登录触发；PR127待正式发布迁移与真实自动换版 |
| 同步状态准确、积压可持续推进 | PR122固定da4a新增4项Windows通过，修开轮假0、精确本地blob复用及有界快速积压处理 | PR122已合入仍待正式上线；StageC此前自然接收，不归因122，不把15秒轮询当端到端上限 |
| 与队长共同确认最终检查点 | Issue33分别报告数据ID、完整记录哈希、代码release/UI实际版本 | 待队长提供最终实读，随后本机匹配；签名下载不代替另一端当前运行证明 |

## 常驻管理进程的代码热更新缺口

固定PR122 `da4a4d5262e9d2f043699707f56b7c6d7a5dc496`：`Supervisor.activate` 重启网站/worker并更新active，但长寿命 `Supervisor.run` 仍使用启动时已导入模块；`bootstrap.py`生成的`start.py`只执行一次subprocess.call。没有发现管理进程自身新代码交接。

实机过程与之相符：原5e监督器跨多次子服务更新未更换；因本机包装异常进行一次明确维护冷启后，才进入502监督器。此维护不能算成自动软件升级。后续仅更新snapshot.atomic_write会修新子服务，但旧监督器的record/write_json仍绑定旧函数，WinError5风险仍可能存在。

已在[Issue33固定反馈](https://github.com/huaweibei123/huaweicup2026/issues/33#issuecomment-5818137154)要求正式单写作者补受控自动交接、真实管理进程版本及失败回退，或提供足以排除此缺口的既有机制证据。本会话不修改生产launcher、签名包、任务或进程。现有静态通过只按其HTTP/快照/发布文件范围解释，不能扩大成全部后台进程都已加载新代码。

## 新增UI批次比较的发布前验证

PR126固定 `c66caececbb201f309585f3f67e1d95127670acf` 的46个小型源码/测试文件按Git blob和大小逐一核对。Windows新增批次比较7项测试通过（0.642秒），前端Cache/异步筛选8项通过（356.3075毫秒），app.js语法检查通过；见 `pr126-windows-delta.json`。只把同一算例集合、同一来源、真实批次的完整成绩排序，不用跨批次补齐。此处没有重新运行整个既有测试集，也不等于该UI已正式上线；正式发布后仍需核对新批次端点与打开页面的自动更新。

## Windows修复包专项复核

新版PR126固定 `741fc585c5b5e3514c32be7ac51618c399c5916b`（独立题目标签、全部批次、固定覆盖式详情）另行完成Windows验证：44项Python初次42通过，两项错误分别为稀疏目录漏掉旧feed夹具和本机无创建symlink权限；从同固定Git blob补齐夹具后对应1项通过，合计43个不同测试通过，原symlink断言仍未执行，不能报44/44。新增批次/专注页9项均通过；Node11/11及语法通过。见 `pr126-focus-windows-tests.json`。不提升系统权限、不改断言，真实IAB上线交互仍另验。

队长正式补丁PR127固定 `5d9b2708fbe88b215fc46e6092ebaaf7b2d98f5b` 已在隔离Windows环境跑完同步39/39（80.738秒，0跳过）；48文件387990字节均符合固定Git blob。新增7项包括Windows真实暂占释放与持续占用、错误分类/重试上限、真实子进程自动换版、双父进程拒绝、坏候选保留旧服务及启动失败回退。见 `pr127-windows-tests.json`。这解决了发布前的可复现检查，不等于生产已经修复；仍需正式合入签名、s-c909一次受控旧启动器迁移，随后至少一次真实签名版本自动交接，单独核父进程、监督器、网站与worker实际代码。

PR127再合并已批准PR122后的固定 `0eab42de8c5652f330bd0fa8bb36ffc8077aa3dd` 也实际执行Windows全套43/43（99.678秒，0跳过），不是用两份旧测试相加；49文件393576字节核对一致，见 `pr127-merged-windows-tests.json`。到此无需在相同代码上重复这些测试，下一步是正式签名发布后的实机迁移及自动交接。

PR122已由队长合入 `0ec86ff8f8e2da4ca3863d5e39b201b819fe6dcb`，s-c909实报本机release `ad50f4ee73656091f83efe835df1d6af07940b3cfb9e12766f6097f20ff7e274` 自动安装16:54:50.871418Z；队长实报同release安装16:55:10Z。本会话既有IAB无手动刷新观察到3756记录、同步中10accepted/1awaiting，假0现象已消失。完整静态资源/实时进程最终复核仍待最终包。PR126原固定版已通过上节测试，但网站owner有新交互要求待修订，不能把旧候选直接视为最终版。

## 双方固定数据检查点

`captain-checkpoint-3695.json` 对应队长Issue33评论5818485150的固定3695条快照：本机先前已独立认证的保留包全部canonical记录哈希为 `072118072beb61c2ac11b11ad1954a56815e360edd783c276fc61b83cdc4b231`，与队长实际SQLite对照值完全相同；前3633记录全部未改、新增62。StageC实际24条来源记录的状态也是23ok/1timeout，23eligible。此为同一历史检查点双方实读相符，不冒称最终新代码或两台当前时刻都停在该版本。

## 实验与权限范围

独立任务只做镜像、通信与发布验收，以及按用户原请求导出固定P1全500统计；0新增solver/E0。不触发GitHub Actions，不变更信任根，不复制私钥，不故意断用户网络或登出。所有最新修复由正式作者经PR与已签名发布通道交付；必要一次引导修复由既定部署写者记录。
