# P1 R4 conditional gap pairing, not a new benchmark

Pure arithmetic pairing of the fixed v4 500-row feed with the archived static-bound scan and the existing 100-case baseline audit. All 500 graph and config identities match. No solver, Task compiler, response simulator, E0/E1/E2 or cloud job was run.

The bound proof and its transfer to official binary64/EPS behavior are submitted for audit, not newly certified here. Columns named conditional are only valid if the underlying lower bound applies to the same optimization domain as the measured U. A loose maximum gap is not an attainable improvement promise. The official K1 reporting point stays 1; its candidate repartitioning is reported separately.

Reproduction: `python scripts/p1_pro_r4_packet.py --repo . --output-dir <new-directory>`. It reads exact Git object identities and recomputes paired ratios. Newly generated UTC metadata and ZIP timestamps may differ; compare the frozen source hashes and numeric rows. The uploaded packet is identified in the matching AI chats manifest; it is not duplicated as an AI-generated attachment.
