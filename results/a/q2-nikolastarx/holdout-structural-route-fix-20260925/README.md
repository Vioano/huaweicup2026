# 097 zero-E2 structural route fix (no scoring)

The runtime-fixed holdout stopped after eight accepted cells. `097-k5` then produced a legal complete plan, solver exit 0 and an `ok` ledger with **zero E2 calls**, but the producer rejected its `reverse_construction_unavailable` skip before independent E0. The frozen algorithm remains at `15d86e13b4a8abb4bce445ac241aecac13f553bf`; the stopped run and its manifest are untouched.

The runner now accepts this zero-E2 route only when all fixed evidence agrees: incumbent selected, one unique unscored plan, zero oracle requests/scores, no reverse detail, and both the reverse error and incumbent construction error exactly `UnsupportedStructure('requires both fork and join')` with the known `gap_candidate`/`unsupported_structure` classification. Other exceptions or unexpected construction kinds still fail. Acceptance here means **eligible for independent E0**, not an official score.

The public `results/.../offline-tests.py` was run without any solver or evaluator. It replayed the actual saved `097` ledger/plan through `inspect_solver` and `cell`, where a fake monitor deliberately blocked the independent E0; it also rejected unknown-error and unexpected-kind variants. Three earlier native-path fixtures still passed. The new manifest in this directory changes only the runner SHA and copies the prior selection, single-core baseline and c665 paired references byte-for-byte. Fixed 15d preflight returned `preflight_ok`, 12 cells, calls 0. Any real E0 or remaining-case dispatch needs a separate coordinator gate and budget; eight prior accepted cells must not be rerun.
