# Frozen P2 bidirectional holdout12: offline handoff

No solver, E0, E1, E2, evaluator, or cloud workload was launched in preparation. The runner is an independent copy of the fixed 15d local producer; the full500 runner and supervisor were not changed. The selection is pre-registered in `selection-manifest.json` (SHA-256 `6060d2a281e963614eb0634dbf90454b3b7b52be73d0d6cc67287436e33d16b1`). Runner preflight independently rereads all 92 eligible **raw JSON `ops` lists**, checks their original graph bytes, sorts by `(len(ops), case_id)`, splits four consecutive groups of 23, and selects three per group by SHA-256 of UTF-8 `salt + three-digit case_id`. It checks every member, selected list, dispatch order and 12 K5 coordinates against the selection bytes and run manifest. No historical performance is consulted.

The root coordinator supplies the new run manifest with schema `q2-bidirectional-holdout12-v1`, exact `coordinates`, `selection_manifest: {"path":"selection-manifest.json","sha256":"6060d2a281e963614eb0634dbf90454b3b7b52be73d0d6cc67287436e33d16b1"}`, the current runner SHA, and the unchanged source/input/E2/interpreter/baseline hashes. Limits: 12 K5 cells, one worker, ≤48 E2 API requests, ≤12 independent E0 starts, 0 retries, 180 seconds per solver and E0 child, 1800 seconds total, 2 GiB per-cell and outer aggregate RSS, 2 GiB observed output growth, 10 GiB free-disk reserve. One possible internal E0 fallback is reserved; success requires zero. The first failed/fallback/unknown cell stops dispatch. If the solver ledger is absent, the row retains unknown call counts, while the batch conservatively charges one possible fallback and stops. Independent E0 plus possible internal fallback must not exceed 12.

The coordinator created `local-holdout-manifest.json` (SHA-256 `a6a8978b808cda853f6e902e9f82d47c68783d241f44f005fe0237857f988455`) and separately reported a zero-scoring preflight against the clean fixed 15d worktree; see its `preflight-receipt.json`. The exact preflight command is:

```sh
/Users/nikolastar/.codex/worktrees/p2-gap500-s59ee-20260925/huaweicup2026/.venv/bin/python -B scripts/q2_bidirectional_holdout.py preflight \
  --repo /Users/nikolastar/.codex/worktrees/p2-bidirectional-full500-s7d28/huaweicup2026 \
  --raw-root /Users/nikolastar/.codex/worktrees/q2-feedback-s8ee/huaweicup2026/data/raw/a/official \
  --e2-root /Users/nikolastar/.codex/worktrees/q2-feedback-s8ee/huaweicup2026/output/q2-e2-paircheck-603b-s8ee \
  --python /Users/nikolastar/.codex/worktrees/p2-gap500-s59ee-20260925/huaweicup2026/.venv/bin/python \
  --manifest output/bidirectional-holdout12-preparation-20260925/local-holdout-manifest.json
```

The supervisor gate is a separate coordinator admission JSON with `status:"admitted"`, `scope:"p2-bidirectional-holdout12"`, future UTC `expires_at_utc`, and exact `source_commit`, `runner_sha256`, `manifest_sha256`, `selection_sha256`, `supervisor_sha256` pins. It checks status, scope, expiry and hashes before dispatch and every sample. Once explicitly admitted, pass the same repo/raw/E2/Python/manifest paths through `scripts/q2_bidirectional_holdout_supervise.py --repo ... --raw-root ... --e2-root ... --python ... --runner /absolute/path/scripts/q2_bidirectional_holdout.py --manifest ... --gate /absolute/path/admitted-gate.json --output /fresh/private/parent`. The supervisor gives the runner a fresh `results/` child, keeps logs and receipts beside it, and starts one 1800-second clock before gate/preflight. An observed RSS/output/disk/deadline fault stops the known process tree and preserves partial evidence. Output growth is sampled, not a filesystem hard quota; a very brief orphan between process snapshots cannot be proved absent. Completion requires exactly 12 accepted rows, both statuses completed, exit zero and no live known child.

Offline checks: run `/Users/nikolastar/.codex/worktrees/p2-gap500-s59ee-20260925/huaweicup2026/.venv/bin/python -B output/bidirectional-holdout12-preparation-20260925/offline-tests.py`. This recomputes selection, rejects a wrong hash/order, replays three existing qualification cells through the real `cell()` using saved receipts and a fake monitor, proves one-worker stop after the first failed cell and unknown-fallback reserve, rejects wrong-scope/hash/expired gates without dispatch, and times out/cleans one fake sleeping process. These checks are not a new holdout score or a full live-run validation. The admitted gate must be fixed and reviewed before any real dispatch.
