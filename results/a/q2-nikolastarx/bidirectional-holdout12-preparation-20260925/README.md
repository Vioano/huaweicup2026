# P2 15d independent K5 holdout: preparation only

No new scoring. Full500 remains held. This is the coordinator-requested 12-cell diagnostic, not a formal full-score batch or automatically authorized follow-on stage.

Selection was fixed before reading saved c665 paired results: exclude old probes 005/010/064/068/069/071/086/088, rank the remaining 92 by (raw `len(ops)`, three-digit case ID), four consecutive groups of 23. Within each group select three by SHA256(UTF8(`p2-15d-k5-holdout-v1` + ID)), no separator, ties by ID. Dispatch: **006,019,052; 061,070,074; 089,099,097; 076,003,084**, all K5. The selection manifest includes all eligible original hashes and counts. Runner independently reproduces all four groups.

Solver stays `15d86e13b4a8abb4bce445ac241aecac13f553bf` in a detached clean checkout. Separate holdout producer and outer supervisor are under `scripts/`; no solver edits, no per-ID rule and no old-probe reruns. One worker; at most12 cold solver launches,48 E2 calls,12 independent E0 starts; 0 retries. Per solver/E0≤180s, batch≤1800s from before preflight. Sampled aggregate RSS≤2GiB, output growth≤2GiB, free disk≥10GiB; these are observations, not hard OS quotas. Failure, unknown route, source drift, process-observation/cleanup problem stop dispatch. Success requires zero fallback; fixed E2 API may already perform at most one internal fallback before returning, which consumes one failure allowance and prevents that cell's independent E0. Independent E0 plus possible fallback≤12; unknown is not zero and is never refunded.

`local-holdout-manifest.json` freezes source/input/interpreter/E2 identity, budget and selection. `command.json` is the actual requested local command, deliberately not executed. A new coordinator-supplied gate with future expiry and five pins is required. No admitted gate is included. `preflight-receipt.json` passed with0 scoring. `offline-test-receipt.json` records pure-input selection recomputation, rejected wrong pins/order/expired gate, three prior-cell fixture replays, first-failure single-dispatch behavior and one fake-process timeout/cleanup. This does not claim live benchmark acceptance. Root reviewed the holdout deltas against3bead producer/supervisor.

`c665-paired-baseline.json` checks saved12 plan/result bytes, archived sanitized receipt hashes, graph/config/source identities, old complete solver and external E0 timing plus native counts. The source module hashes are pinned to c665; the historical runtime checkout/runner was ff47. These are old-run timings on a shared host, not current-version measurements or a controlled performance experiment. After execution compare all12 M/DDR/native agreement/solver wall, retain losses and failures; decide the next window separately.

Preparation model: Sol medium for bounded runner adaptation, soft6000-token budget (actual token use unavailable), no recursive agents or real scoring. Root handles selection, paired provenance, review and resource request. Archived helper scripts and local command preserve the working output layout; use the original worktree paths or reconstruct the same relative layout, not an assumed executable archive cwd.

C03 mathematical consultation is in flight in the existing huaweicup6 Pro chat, independently of this holdout. Formal full500 remains c665, K5 mean4.549756996698352; user stop condition is not achieved.
