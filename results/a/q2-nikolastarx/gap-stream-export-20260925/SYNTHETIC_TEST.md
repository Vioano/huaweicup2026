# Synthetic checks, no evaluation calls

On 2026-09-25, a Python snippet constructed a 50-row accepted prefix plus one running row from the frozen coordinate manifest. `accepted_prefix` returned 50. Adding a later accepted row after the running row raised `ValueError`. In a temporary directory, `archive_bytes` accepted identical bytes twice and rejected a third write with different bytes. The temporary directory was removed automatically. `python3 -m py_compile scripts/q2_gap_stream_export.py` passed.

These checks cover prefix selection and immutable writes only. They do not establish full board admission or score correctness. No solver or evaluator was called.

After the review fixes, an ephemeral four-cell fixture copied and decompressed the existing pilot plan/result bytes, copied its process/online receipts, and reordered a *temporary* 500-row manifest so its first four rows matched those pilot cells. The temporary summary was explicitly marked `completed` and given that temporary manifest's hash. The exporter emitted four fixture records and a second identical call was idempotent. `python3 src/benchmark_board/protocol.py <temporary-feed> --submission` returned `valid=true`, `submission=true`, `records=4`, `eligible=4`, with no reported failures. Both temporary directories were deleted automatically. This checks board structure and archived pilot-byte compatibility; the synthetic summary/manifest mean the four `eligible` fixture rows are **not** a fresh500 algorithm score, nor a board delivery. There were zero solver/evaluator calls, zero enqueue/push actions, and no permanent fixture feed.

Root repeated the real-byte fixture through the documented script CLI with PYTHONPATH removed, using `python3 -B results/a/q2-nikolastarx/gap-stream-export-20260925/check_fixture.py`. This exposed and fixed direct-script import-path initialization. The retained fixture runner then returned valid=true, records=4, eligible=4, repeated export bytes were identical, and all temporary source/feed files were deleted. The result is schema/available-byte compatibility only, not an independent rescore or central acceptance.
