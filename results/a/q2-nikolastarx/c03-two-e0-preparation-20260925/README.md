# C03 two-plan E0 preparation

The two C03 plans are frozen under `results/a/q2-nikolastarx/c03-static-once-20260925/`. `mechanism-manifest.json` pins the original case 069 graph hash, official config hash, and both exact plan file hashes. The runner checks these, the official source manifest, and a fresh gate before creating output.

Gate template is `pending` and cannot dispatch. The root must supply a new `admitted` gate with matching scope, runner/manifest hashes and an unexpired timezone-aware `expires_at` after a current resource check. The runner permits at most two E0 calls, one worker, 30 seconds per process, 90 seconds total including preflight, 512 MiB RSS; no retries, E2 or solver. Use a new output directory. It takes `--raw` pointing at the directory containing the original `case_069.json` and `config.txt`.

Prepared runner: `evaluate_two.py`. It uses `Path(__file__).absolute()` and does not call `resolve()`. No real evaluator was started during preparation.

This is an archived host-bound preparation package, not an admitted run. The exact executable copy lives at project output/c03-two-e0-preparation-20260925/; copy the package there before reproduction, and obtain a new gate. Archived files do not grant a scoring budget. No official score was generated by this preparation.
