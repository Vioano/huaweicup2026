# R6 branch-aid：085/K5 官方机制验证

唯一固定源 `db64a9375ffda535736ac54031a1184ee9f8f3d9`，一次条件 E0；没有搜索或重试。当前图先执行 heavy/sink 构造，其实际返回的分支为 `sink-peel`，新生成计划与 v4 原始计划逐字一致，再调用一次 branch-aid。不能从 `heavy-or-sink` 的选择标签推断本图使用了 dominant-component 分支。

| 指标 | 旧 v4 同图计划 | 新 R6 候选 |
| --- | ---: | ---: |
| Makespan（周期） | 783703 | 681874 |
| 相对官方单核加速比（官方单核3187899周期） | 4.067738671410 | 4.675202456759 |
| 额外 DDR（字节） | 8128530 | 10742148 |
| 调度总 COPY（字节） | 9818264 | 12431882 |
| Spill（字节） | 0 | 0 |
| Task 数 | 97 | 157 |

Makespan 下降 **12.993315%**；该格加速比提高 **14.933697%**。新增60个Task，静态预测新增COPY2613618字节、930个COPY实例，与官方调度COPY增量逐字节一致；本次没有新增spill。额外DDR增加 **32.153637%**，因此不能称为两个质量指标同时变好。

当前输入的基线重建、新候选构造、字节核对和输出落盘，冷子进程墙钟 **1.153906秒**；外部 E0 **3.127436秒**；含五个小图测试与监督的整个窗口 **4.689555秒**。构造/E0进程组采样峰值分别128416/219648 KiB。该生成入口尚未接入统一选择器，时间不能写成完整统一求解器的耗时。五个合成结构测试通过；真实图heavy/sink构造1次、branch-aid构造1次、E0=1、E1/E2/重试=0。

官方旧时间线中，三条重核的重复长Task每轮76641周期。新计划把其中一个独立上游分支交给轻核，原核保留Y和汇合尾J；例如原Task12从76641变为Y的50141，加J的14376及同核间隔100。与之对应的后续共同前缀Task启动周期，从重复86740降到约75350–75351。各核Task执行区间总长由 `[764703,764703,759364,539277,423881]` 变为 `[655921,653737,648699,651610,649235]`；这是Task区间统计，包含区间内的等待，不能当作真实Pipe利用率。结果支持“平衡重复波内的重任务”这个机制，但不是跨图收益保证。

构造也拆了一个仅让原始最大Pipe工作量从3068降到3040的小波；新Task7启动反而晚440周期。整体仍净改善101829周期，这说明单看最大原始工作量不足以预测局部收益。后续优先验证结构覆盖和收益迁移；不为这440周期立即新增参数试跑。

本实验只确认一个候选在一格上有效。若仅做这一格的算术替换，五核100图均值增量也只有0.006074638（约当前均值的0.151%）；这不是新统一算法全量成绩。正式全量仍为 **4.025907473836023**；相对再提高5%的目标为 **4.227202847527824**。尚未获得足以停止优化的全量突破或最优性证明。

旧原件：固定提交 `9c5f87548cc7588465a638e032993969b5cac891` 的 `results/a/q1-unified-v4-full500-20260925-s59/20260924T1952Z-s59ee/cells/085/k5/`。新原件在同目录 `official-085-k5/`；监督器和准备时的零评分控制回归另外保留。T0采集第一次发生正则解析及后台服务误分类；原始数据未显示真实资源门槛失败。root纠正解释后重新读到新鲜free3241148416字节、pressure86%、近期swapout0、无评分进程，再进入唯一已批准窗口；原误解析记录本地保留，不改写成成功记录。
