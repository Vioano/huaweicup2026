# P1 intact-frontier: case 051 constructor transfer

This public package derives from the untouched local raw records in `results/a/p1-intact-frontier-transfer-20260925/`. The three constructors used frozen `src/q1/intact_frontier.py` at commit `9293aad78dde0dc170d05bc2c4142db634d46b4d`, SHA-256 `bca85389d883e952366db5f2a6307a24f6a616951338b25f7d546335a4d21cbc`, on official `case_051.json`, SHA-256 `884e8b12ac1f7a9b569958909680e8c2f6055966a59c9f929ffd5cee48aae43b`. The original source and input bytes are included here. See `manifest.json` for all included-file hashes and fixed archive, configuration, and official-code identities.

One worker made three constructor calls, no retries, no Task compilations, and no E0/E1/E2 calls. The observed batch wall was 0.258981 seconds, under the 120-second aggregate cap; each command had a 30-second timeout. Per-cell UTC times, walls, limits, process status and portable command form are in the individual receipts. The original supervisor reported Python 3.14.5. It requested the main-repository `.venv/bin/python`; that interpreter is Python **3.12.13**, resolving to the recorded 3.12.13 uv installation. Therefore 3.14.5 describes the supervisor, not the constructor subprocess. The original raw `batch.json` and `manifest.json` are preserved outside this derived package; their absolute local paths are deliberately omitted here.

| Cell | Constructor wall | New plan SHA-256 | Old plan comparison | Prior Fang E0 reference |
| --- | ---: | --- | --- | --- |
| `051-k3-paced` | 0.039078 s | `9806867d136803fab19f0d03fad66ad7c920a5d3f7a0fa8d44095b89bbebf5fe` | JSON equal; old file ends CRLF and new LF, so bytes differ | Hash recorded in receipt; **not marked byte-reusable** |
| `051-k4-paced` | 0.038469 s | `fa829d669f649cd083a651452fb484b030cb2081d863160c50b47dce91d5e049` | JSON equal; old file ends CRLF and new LF, so bytes differ | Hash recorded in receipt; **not marked byte-reusable** |
| `051-k5-root-heavy-fused` | 0.072371 s | `d7de42f7eea7ca83787244b2307448d6d34b5b61045fd902fb2a97540b8ed5e2` | Exact plan bytes equal | Prior Fang E0 result may be cited by exact-plan identity; no local E0 occurred |

The prior plan bytes are included to make these distinctions inspectable; the prior result hashes and their source commits are in each receipt. The `diagnostics.json` files describe structural candidates, not official quality. This is a three-cell local constructor transfer, not a full-algorithm score or a feed.

For a fixed historical comparison, the unified v4 full-500 feed (solver source `a0537aeb72dc702af86d67d3194587d581ac207c`, data commit `0e0d7cd327c51cc6ac365e01f4b6a7d2b28f9297`) records case 051 Makespan **291,222 / 254,308 / 253,856** at K3/K4/K5. The prior Fang E0 result files identified by the receipts report **278,618 / 244,533 / 231,551**, respectively. These are historical E0 figures for different fixed plans; this transfer made **zero new E0 calls**. K3/K4 constructor outputs match those Fang plans as parsed JSON but differ by their terminal CRLF versus LF, so this package withholds byte-identical result reuse for those cells. K5 is an exact-byte plan match and can cite its prior Fang E0 result by identity. Neither comparison is a full-algorithm score.
