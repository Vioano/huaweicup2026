# P1 Colab full500 attempt: partial public export

Fixed run `20260925T0110Z-s6607` declared 500 cells (100 graphs × 1–5 cores). The run stopped after 68 successful cells, one timeout (`014/k4`), and 431 unrun cells. The feed contains only 68 successful cells. This is **not** a complete 500-cell result; no full-batch mean is reported.

`batch.json` records the complete cell ledger. `failures/014/k4/run.json` and `plan.json` preserve the failed E0 attempt. `014-k4-plan-comparison.json` records a byte-identical prior-plan comparison; it does not replace the failed E0. `collection-receipt.json` records archive verification and VM shutdown. `environment-evidence.json` distinguishes solver Python 3.12.13 from collection Python 3.13.15. CPU model, GPU, installed dependency versions, and true peak RSS were not measured and remain null with reasons. Offline bootstrap/dependency costs were not independently timed. Each cell used a new interpreter, but OS file caches were not flushed; `cold_start=false`.

Successful-cell artifacts appear in `cells/`, with fixed single-core denominators in `baseline/`. Exported E0 results are from this new run. The main repository board protocol reported `valid=true, submission=true, records=68, eligible=68`; its scope is format and available bytes only, without rerunning a solver or evaluator. Earlier `public/` and `public-v2/` exports remain local only as superseded development artifacts and were not submitted. The saved final check is `protocol-preflight.json`. No solver, Task compilation, E0/E1/E2, or retry was run for this export.
