# 统一入口三格验收与全量检查点决策

固定求解器提交：`834d8c957538ee069c66aadac9509552a4cc69d7`；入口 `src.q1.branch_refine`。相同入口按输入图结构构造父方案和至多一个 branch-aid 候选，按在线精确目标 `(Makespan, scheduled COPY bytes)` 比较，不读历史赢家、参考成绩或 case ID。

2026-09-25 15:07:55–15:08:30 UTC，既定顺序 085→075→016、K=5，统一入口与独立未修改官方 E0 实际运行完成。批次墙钟 35.581657 秒；3 solver、12 在线 E1、3 外部 E0，E2/重试均 0。外层启动会话返回码 0；各子阶段返回码 0，协调者另行回读进程组清理并关闭资源窗口。

| 图 | 旧统一 v4 Makespan | 当前 Makespan | Makespan 下降 | 当前选择 | 冷求解墙钟 s | 外部 E0 s |
|---|---:|---:|---:|---|---:|---:|
|085|783703|681874|12.9933%|branch-aid|10.479166|3.175251|
|075|378832|378832|0%|parent，拒绝略差候选|3.163088|1.188883|
|016|3223183|2941234|8.7475%|parent，保留结构细化收益|9.403653|8.035998|

三格的诊断目标与官方 `(Makespan, scheduled COPY bytes)` 分别精确相等：085 `(681874,12431882)`，075 `(378832,7292086)`，016 `(2941234,119940642)`。root 独立逐字节核验收据声明的 27 个原件、两键输出与三个目标；批次收据 SHA-256 为 `5872f7d610f8958f4fab2995a2da018ddbd42aad241911dc1206aa435ddc6ea6`。

次要指标须保留：085 额外 DDR 为 10742148 B，较旧方案 8128530 B 增加约 32.15%；075 保持 5454896 B，016 为 119547424 B。三格 spill 均 0。不能把 Makespan 改善表述成所有指标同时改善。

## 结论与下一步

统一入口通过了“选中有效候选、拒绝退化候选、保留先前结构收益”三个有意选择的检查；这足以支持冻结算法并准备完整 benchmark，不证明其他格子的表现。已验证完整主成绩仍是 v4 固定算法在 100 图 × 1–5 核上的 K5 均值 **4.025907473836023**。不得把这里三格或此前各独立机制的局部成绩拼接为新全量成绩。

下一轮使用相同固定算法、规则和预算生成全部 500 格，每格重新计量冷求解墙钟；仅在原方案字节、图、配置、官方语义及旧评价原件逐项核同后复用旧 E0。新 full500 的 runner、预算与共享资源窗口另行冻结和协调。用户已明确有实质改善即可更新，不必等待 +5%；当前仍未启动新 full500。

## 当前瓶颈

- 整块任务可保留局部复用，却会使每波负载无法均分；合法拆分能改善尾部空闲，但引入新的 Task 边界、DDR 搬运及等待。085 的实测收益与 DDR 代价同时存在。
- 局部工作量代理不能替代最终事件模拟。075 的独立候选有更好的局部负载数字，但官方 Makespan 略差；统一入口已实际拒绝它。下一步需要针对真正限制完成时间的区段构造，而非重复微调无关小波。
- 结构覆盖限制整体均值收益。R7 的共享输入带构造目前只通过 6/100 图的严格结构识别，尚无构造/评价成绩；不能据此宣布全局上限或直接推广至其余图。
- 新增求解耗时尚需全量计量；本次三格求解约 3.16–10.48 秒，不能推断全部 500 格的尾部时延。增加运行时间本身不会消除上述结构和搬运权衡。
