# P3 最后一轮全量验收（2026-09-26）

结论：同一固定求解器完整 100 图 × 1–5 核，500/500 官方有效；最终输出计划的字节全部与 Forest500 相同，质量没有提升。沿用 Forest（solver `311322b996c0948e8a6a9c7ec6ddfe6ae41fbee1`）作为论文主算法，本轮选择器保留为已验证的负面消融。K5 >5 的目标没有达到；按用户要求结束继续研发，不再向 Pro 发起新问题。

## 固定身份与执行证据

- 求解器：`f6fd8153375a7fb64f9af2c8f36c35356fb7d878`，入口 `src.q3.fifth_core_final_solve`。
- 监督/校验 runner：`65c07e062ed9be78dda32ef4bb58a9891f373de9`。
- 500 个唯一坐标，0 重叠、0 缺格、0 失败/超时/重试；500 次新进程求解，982 次在线 E0，E1/E2=0。
- 首段 400 格/788 E0：19:27:41.032041Z–20:27:43.746843Z；到原派发时限后停止，已有格全部成功。
- 另经具体授权的第二段仅运行原未尝试100格/194 E0：20:36:09.794745Z–20:47:10.124326Z。两段不改算法、输入、每格候选/调用上限，不拼历史赢家；不能说原一小时跑完500格。
- `final-export-20260926/summary.json`、`comparison.csv` 来自已终态原件；严格审计每格计划、官方结果、原始子进程收据、实际调用账与精确选择门槛。后处理新增评分为0。

## 全量指标

| 核数 | 图数 | 相对官方单核均值 B/M3 | 同计划 G=M2/M3 均值 | 字节命中率逐图均值 |
|---|---:|---:|---:|---:|
| 1 | 100 | 1.199759568 | 1.002339646 | 6.465513% |
| 2 | 100 | 2.312002295 | 1.007923645 | 16.252185% |
| 3 | 100 | 3.258687134 | 1.031379926 | 25.958996% |
| 4 | 100 | 4.091503002 | 1.051201636 | 24.528984% |
| 5 | 100 | 4.757616679 | 1.082917173 | 31.363332% |

以上均为逐图算术平均；不是总周期相除，字节命中率均值也不是把不同图字节合并后的命中率。全部计划字节与旧 Forest 一致，Makespan、G、额外搬运指标均未改善。500个P2配对逐格核图/config/official/plan/核数；有在线同计划P2时用实际在线结果，其余只复用身份完全相同的已存P2原件，不能换独立最优P2。

## 第五核构造的真实作用

K5：96例结构不适用，1例由该候选的合法下界剪掉，3例通过官方P3得到更短耗时，但被冻结的联合门槛拒绝。接受数0。门槛要求 M3 严格下降、M2 不增、G 不降；这是本轮选择政策，不是官方新增合法性要求，也不证明仅最小化 Makespan 时该候选无价值。

| 图/K5 | Forest M3→候选 M3 | Forest M2→候选 M2 | Forest G→候选 G | 判定 |
|---|---:|---:|---:|---|
| 035 | 32994 → 未评分 | 未新增 | 未新增 | 候选下界剪枝 |
| 064 | 6981 → 6877 | 7029 → 6899 | 1.006876 → 1.003199 | G下降，拒绝 |
| 068 | 116345 → 97971 | 131631 → 98913 | 1.131385 → 1.009615 | G下降，拒绝 |
| 088 | 85789 → 66224 | 97305 → 66729 | 1.134236 → 1.007626 | G下降，拒绝 |

不能把这3个未被算法选中的候选离线填入主表；应把它们作为目标之间取舍的机制证据。本轮比旧 Forest 全量973次在线E0多9次E0，输出却无改善，故不把额外构造留作推荐主求解器。

## 求解效率与运行环境

本轮500格端到端子进程墙钟：平均 3.202649s，中位 1.157935s，P95 15.235049s，最大 36.896687s，总和 1601.324500s。P95为线性插值分位数（Type 7）。包含导入、读图、构造、在线E0、选优、落盘和子进程清理；父runner的逐格额外审计和Git原件读取不计入该子进程时间，另体现在批次时长中。

这是共享主机、1worker的新进程测量；OS页缓存不受控。首段与P2实跑重叠20:23:54.673461Z–20:24:03.588371Z；第二段与首段离线复制核验有约1.97s重叠。不能与旧4worker批次直接算程序提速倍数，也不宣称真机加速。

## 原件与交付状态

- 两段全部逐格原件在各自 run 目录，未修改官方结果或计划。
- 批次原始大JSON保留在执行机；Git用确定性无损 `batch.json.gz` 加 `batch-archive.json` 保存。第二段summary记录的是原始raw JSON SHA，按同目录archive清单解压即可恢复精确字节；不可把压缩SHA与raw SHA混为一谈。首段315MB大索引不作为成绩台JSON artifact解析。
- `SEGMENT1_ARCHIVE_COPY.json`、`SEGMENT2_ARCHIVE_COPY.json` 为逐文件与聚合核验收据。
- 五份 `final-export-20260926/board-feed-*-of-05.json` 保留两个真实run_id/每格attempt_id；本地协议预检各100/100 eligible，见 `PROTOCOL_PREFLIGHT.json`。这不是中央接收或网页验收回执。
- `figures/` 内 PNG/PDF/SVG 和metadata：实际渲染已目视核对，标签/轴/图例完整，旧新重合如实显示。图只呈现已审CSV。
- 论文方法交接：`paper/notes/p3-final-round-method-handoff-20260926.md`。
- 本归档固定后交成绩台owner做中央接收/只读composite视图，交总调度串行镜像同步；实际接收/镜像回执在后续 DELIVERY_STATUS.md 中记录。

冻结前的原计划和预算说明可从 runner 固定提交的同目录README读取；原始提案、失败/剪枝与单例机制证据保留，不因负结果删除。
