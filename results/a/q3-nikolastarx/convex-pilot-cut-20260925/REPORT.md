# Convex pilot cut, case 005 only

The single-set contraction criterion in `FROZEN.md` is correct for a DAG. The proposed closed-form cutoff also requires the fixed core word to be a topological extension of the original DAG; then a contiguous same-core segment cannot have a path leave the segment through a same-core word outside it and reenter. The script checks same-core arc order, five fixed toy DAGs and every toy suffix by explicit contraction. Complexity is O(V+E) time/space for one core cutoff after topological ordering; explicit full-op quotient verification is also O(V+E). This says nothing about simultaneously merging multiple segments.

One unchanged frozen script was executed for the specified 005/key `1000000048`/core 0/consumer `1289` pilot only. Consumer rank is 202. The maximum alien-core reentry exit among ranks ≤202 is 1, giving earliest safe start **rank 2**. The segment spans 201 consecutive original compute operations (first op 17, last op 1289), assigned to its first subgraph. The output [candidate plan](CANDIDATE_UNVALIDATED.json) preserves all 4113 original compute operations exactly once, all five core schedules, and the original owner of every op; it removes only the 200 redundant singleton subgraph IDs from core 0's word. The complete original op graph has 4209 operations and 6889 deduplicated op arcs derived from every typed raw edge. After retaining COPY operations as their own vertices, the candidate quotient has 4009 vertices and 6599 arcs and is acyclic by an independent Kahn check. [Machine result](RESULT.json) records input hashes, toy checks and counts.

The earlier warmup audit attempted two simultaneous merges and found a cyclic quotient; this run makes only **one** contraction and does not retry that rule. Neither graph convexity nor the all-edge quotient check proves official Task/Step behavior. The archived prepared sequence is already post-Step1 rebucketing; without the raw Step1 sequence, we cannot say when the key's COPY_IN would issue inside this new bucket. The prior singleton local-memory certificate cannot be applied to a 201-op subgraph. No official constructor, Task, Step, E0/E1/E2, or pipe-bound call occurred, and this candidate has no verified legality, capacity, makespan, cache gain, or score. The other target `1471` was not constructed.

The first shell launcher could not find `timeout` and exited 127 before Python started; its raw stdout/stderr are retained. One subsequent standard-library-supervised Python process ran the frozen script with a 60-second limit, returned 0 in 0.118 seconds internal wall time, and wrote exactly one candidate. There was no script retry or parameter search. Script SHA-256 `2eec4af98e399719db0fcc70b7d6ee52fa7bdecffac736c37e75d5aa2435c83d`; result SHA-256 `1c0a1f52f584f5bc9e76e5ae9dd0c77021a5d2c947d62f1431a06fe1a7e00edb`; candidate SHA-256 `f4c58ccf77f5a8add20687eb20247ede169ff34b78a0f24e1c8bda805f1c75a2`.
