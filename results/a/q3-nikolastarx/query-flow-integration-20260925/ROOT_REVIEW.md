# 统一入口一格集成：根会话复核

冻结 solver 是 c960cd38724fc8d5cc8c4e8942a58cdcf74fdc43，算法源码本轮没有修改。交付提交可以包含后续文档和外部运行器，但必须保持 c960 为祖先、src/q3 相对 c960 无差异，并通过 manifest 全文件集合及字节核对。根会话已调用 verify_manifest 做只读验证：92 个输入/源码文件一致；execution_authorized=false；run 与 integration-control 均不存在；新 E0 为 0。

新范围只验证 071/K5 从原图到最终计划的统一入口，最多 4 P3 + 2 P2，单 worker、每次 90 秒、整体 600 秒、首异常停止、零重试。外部最终评价没有另给次数。此前预制计划的 R8 两次调用已用完，不能复跑或转移预算。069 和全量 500 格没有包含在本申请。

已检查 query_flow_solve 的接受门槛：M3 严格下降、对应新计划 M2 不高于旧计划 M2、整数交叉乘积验证 G 不下降。新 runner 在官方函数入口前记账，保持官方函数体与文件原件不变；首次异常通过 BaseException 中止整个测量，不允许内部合法性回退继续耗用其他评分槽位。此严格异常中止是本次实验运行配置，不能把其失败样本掩盖为一般求解器成功。

根实读了新 supervisor 的退出先验检查和独立终态分支，以及 terminal-zombie-mock-results.json。定点 mock 覆盖 zombie 父进程正常结束、已核残留组、PID 复用/未知身份拒绝发信号与既有失败状态保留。早期 integration mock 调用的是旧 helper 的终态函数，不能充当新终态实现验证；已有原件和这一区别均保留。最后短命入口修订经过静态审查，完整 main 的真实进程行为、OS 竞态和瞬时峰值尚未验。

资源规则是待调度审核的提案：pressure=1、无其他 scorer、磁盘至少 10 GiB；运行中本组 RSS 超过 2 GiB 或 swap used 比 T0 增长超过 256 MiB 即按已核身份停止本人组。完全空闲物理内存和 swap free 仅记录，不沿用 6 GiB 门槛。每秒采样不能保证捕获瞬时峰值，RSS 含共享页也不是独占物理占用精确值。

计时同时保留 solver 分项、runner 从读取与核验至输出落盘的墙钟，以及外监督墙钟；后者还包含监测与收尾。fresh Python 进程不等于 OS 文件缓存已冷却，不能写成已测冷缓存极限。算法成功仍须核对计划原件、两层调用账、官方结果与成对比值，监督器退出 0 本身不证明方案质量。

本复核不授予运行许可；必须取得总调度对本 manifest/代码和新 T0 的单次明确放行。
