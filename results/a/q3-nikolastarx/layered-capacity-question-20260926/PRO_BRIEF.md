# P3 五核四轨图 068 的容量与负载瓶颈（给 Pro 的固定材料）

本文件只记录待解数学问题和可复核静态证据，**不是官方性能结果**。原始图为 `data/raw/a/official-cases.zip` 中的 `case_068.json`（解压后的 SHA-256 `dfd9a58ef9d26a8a4567026b50af8b4499d87eebb3d98f8208b909b11e963c6d`）；原版配置 `data/raw/a/official/data/config.txt` SHA-256 `dcd10de54b23f8366428fb24e828812b1da9549e6eae4a3c3f38604fe5ae77b9`。官方配置为每核 L1 524,288 B、UB 131,072 B，跨核依赖至少 500 模拟周期。提交只能包含 `node_to_subgraph` 和 `core_schedules`，每个原计算 op 恰好一次；官方 Task/Step 会插入 COPY、处理 incarnation 与容量/FIFO。

当前固定完整 Forest500 算法的五核均值是 4.7576166788448955，目标超过 5；这份新机制尚未进入完整求解器。图 068 的既有 Forest 五核 P3 Makespan 为 116,345 周期、同计划 G=P2/P3 为 1.131 左右。通用 compute-only 松界较低，但不保证可达。

现有多层 R9 构造 `src/q3/layered_query_flow.py` 在原图上识别 4 条持续私有轨道、4,158 私有计算 op、1,193 共享计算 op（49 个无向弱连通分量）。五核时简单把四条轨道放四核、第五核空置：共享分量按现有直接规则全落在前四核，第五核没有任何 op。该候选在静态 no-spill 桶前沿已经失败：core0 的 UB 在本核桶 145、原 op 4551 达到 221,208 B，比 131,072 B 上限多 90,136 B。四核比较得到相同非空核顺序、共享归属和峰值，因此不是“多加第五核”造成的退化。此充分容量证书失败**不能证明原图或该提交一定非法**；尚未调用官方 Task/Step。

两个预先固定的机制探针（均纯静态，未调用官方程序）进一步定位问题：

1. 将原先归 core0 的 15 个共享分量（706 op）**全部**迁到空闲 core4：221,208 B 的同一个 UB 峰值原样移到 core4；静态 compute/FIFO 长路径 `Ldelta` 由 119,073 降至 93,450，但它只是图/FIFO 代理，不是官方 Makespan。远端 COPY 连接数 468→474。
2. 按初始超限桶上各共享分量的活跃字节贡献降序，搬最短的足额前缀：三个分量（最小 op ID 4510、4444、4473）对应原峰值贡献 40,960+32,768+24,576=98,304 B。重算后 core0 UB 降至 114,716 B，但 core4 在另一桶升至 212,992 B，仍超 81,920 B。没有试其它子集或参数网格。

原件：`layered-fourtrack-frontier-diagnostic-20260926/`、`layered-shared-core-repair-20260926/`、`layered-shared-split-20260926/` 下各有 `FROZEN.md`、`RESULT.md`、`result.json` 与脚本。临时 idle-core 源码改动已存 `rejected-idlecore-extension.patch.gz` 后撤回；主构造代码未因这些负例改变。另有 `nonattention-headroom-20260926/REPORT.md`，说明追一个孤例不足以实现全量五核>5；既有共享流水线和一般 gap-DAG 构造不能因 R9 识别失败而被忽略。

请基于原始 op/tensor DAG、上述完整证据和官方冻结 Task/Step 源码，回答三个有可证伪落点的问题：

1. **证明或反例：**在不拆共享弱分量、并将全部共享计算置于所有私有计算之前的 R9 子类内，是否能从这些活跃区间推出 068/K5 的最小 UB 峰值必然大于 131,072 B？不能仅凭我们试过两种分配就作全称结论。若不能证明，给一份可检查的剩余候选或准确的未知状态。
2. **更广的直接构造：**若允许将共享分量/持续轨道按原始 DAG 的合法切口进一步拆分，或把无私有祖先的共享 op 延迟到首次真实消费者附近，如何确定性地产生第五核实际工作、控制各核 Task 桶前沿、避免原依赖/COPY FIFO/Step3 内存边引入环？请给清晰状态、转移/贪心不变量、适用守卫、复杂度和至少一个小反例。不能复制/融合原 op，不能假设理想化内存分配或从 cache hit 推断周期。
3. **最小验证：**只推荐最多一份 068/K5 静态计划的生成规则。先列出必要的纯静态检查以及官方 Task/Step 后必须读出的证据；只有候选合法且结构守卫通过，才考虑一次 P3 与同计划 P2，并比较 M3 下降、M2 不恶化、G 是否上升。若原图 088 才更适合此构造，请明确给出原图依据；不要以 case ID 写在线选择规则。

请区分已读原件、静态事实、数学证明、代理估计、官方未测项；若实际无法读取图/源码，请不要假装读过，并把结论限制在本摘要提供的数据。我们需要改变能推广到一族图的构造，而不是继续为单个编号试参。
