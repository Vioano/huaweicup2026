# 首次静态检查：识别器遗漏共享后处理参数

冻结源码bc003d94375448fbc14ba535fb8fa78ef7646d88；071/k5一次construct于0.015916秒内在 `_decompose` 的 `unclassified branch joins private flows` 处拒绝。0 pipe_bound、0 Task/Step/E0/E1/E2/VM、0重试；069未运行。完整stderr/summary保留，不能将失败目录重用或改写成成功。

随后仅按原JSON的op→tensor→op邻接只读查出：原计算源15（MATMUL，15cycle）直接供685/704/723（SIGMOID）；原计算源31同样供742/761。它们是无计算前驱的共享参数源，分别进入3条/2条流的后处理。当前实现的S仅由“QKV投影的共同祖先”生成，因此漏掉只供后处理O的共享参数。这个实现定义窄于R8四阶段模型；该拒绝尚不能当作query-flow方向负性能证据。其他原根11/13供前处理3流，27/29供前处理2流。

后续修正仍须按图结构识别共享祖先，不能对节点15/31或样本071特判：识别私有A/O归属后，去掉仅识别用的真实K/V广播，按所有私有阶段种子的共同上游祖先求S；S不得含A/O或依赖私有流，跨流join和阶段回退继续拒绝。先加共享源仅供多个O的toy回归及非法join检查，再冻结修正源码和独立第二静态阶段。此阶段已终止，不能重置其调用账，也没有批准任何官方评价。
