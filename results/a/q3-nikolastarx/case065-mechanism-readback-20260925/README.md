# 065/K5 mechanism readback: bounded result

Source and budget were frozen in `INPUT_FREEZE.md`. The actual read-only forest root was `/Users/nikolastar/.codex/worktrees/p3-forest500-s59ee-20260925/huaweicup2026`; the exact failed command and captured error are preserved in `FAILURE_RECORD.md`. The snapshot's five forest artifacts and raw 065 graph matched their recorded SHA-256 in read-only checks. Existing E0 result reports **Makespan 11270 cycles**, versus baseline 53068. The generic 5904 lower bound is loose; the 5366-cycle difference is neither measured idle nor a demonstrated improvement opportunity.

Direct inspection of the saved E0 `per_core_timeline` showed one task per core, each starting at 0. End cycles were **8710, 11270, 9443, 10964, 11064** for cores 0–4. Core 1 finishes last with a `COPY_OUT` at 11269–11270. Its `step3_by_core` reports 126 `PIPE_V`, 13 `PIPE_M`, 36 `PIPE_MTE2`, and 14 `PIPE_MTE3` ops, with 52 `memory_dependency_count`; core 3 has 160/12/30/12 and 121, core 4 has 147/14/33/12 and 90, respectively. Core 0 has only 54 V ops and finishes 2560 cycles before core 1. These are operation counts and observed task ends, not pipe busy-cycle totals or causal wait attribution. The result reports zero inter-task dependencies/transfers, but that does not mean all original graph dependencies vanish within tasks.

Saved movement is 371930 scheduled bytes, 55728 added partition bytes, zero spill; cache records 9 hits among 168 `COPY_IN` accesses and 28080 hit bytes. These show traffic and low hit rate, **not** that DDR or cache caused the late core-1 finish. The timeline contains op start/end events, but a validated dependency/pipe reconstruction was not completed. Thus the exact critical predecessor chain, COPY release wait, and pipe busy/idle cycles remain unproven.

Falsifiable construction direction for the author: identify dependency-closed V work currently assigned to late cores 1/3/4 that can move to the earlier finishing core 0 without adding high-fanout remote tensors; predict the affected original predecessor releases and per-pipe work before building one fixed candidate. Reject if no such closed region exists or the added cross-core release burden dominates. A later candidate must be independently checked for legality, E0 Makespan, added DDR bytes, and end-to-end solver wall time; this readback does not authorize that run.

**Stop record:** The one permitted static summary process exited immediately with `FileNotFoundError` because its script derived the repository root one level too low (`.../results/results/...`). Per the frozen first-exception rule, it was not retried. Its original body was restored as `failed_analyze.py` for evidence preservation. All conclusions above come from the preceding read-only SHA and result-field inspection, not from the failed process. No constructor, Task, Step, E0/E1/E2, solver, or VM call was made in this audit.
