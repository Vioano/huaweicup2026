# Prepared Step1-only replay; not executed on case 005

Script `step1_only.py` SHA-256 `ab029d2afc54d7ca415d2c2c835d987936a0fe9e049acc83d258fc4f314b675a`; pure synthetic mock `pure_mock.py` SHA-256 `80ec2bf67d36b77e7819ced8e8d3e64350c3b22b75c27197ceb24ac449d65f0a` (two checks passed). HEAD at preparation: `2c1e12ac4c55a920f62078d707d95fdf5f5eb100`.

Frozen inputs: archived `prepared.json.gz` SHA-256 `91c9e4ff2995d091922f7ae1b0975de98174a9487a1d0458a05c60535beb5c44`; old plan `2885b28a512598e4bae525c7bf4b214ac9b2452de19425603fcdea2ae8e7038a`; convex single-merge candidate `f4c58ccf77f5a8add20687eb20247ede169ff34b78a0f24e1c8bda805f1c75a2`; unchanged official `schedule_step1.py` `d8fe721ff3dbe036e34a20c00cce6430960860000a49eb467e63465f76b84034`; `multicore_cut_evaluate_problem_3.py` `eab1504dead881f4b67c0f0498cbc2dbbd9039dc3c9d198c6af58773c127eeb0`. Sample is exactly case 005, core 0, key 1000000048/consumer 1289. No other graph, candidate, or tuning.

Source reconstruction argument: `multicore_cut_evaluate_problem_3._build_scene_b_tasks` calls Step1 on its `graph`, then Step2, then `_build_extended_graph` and `prepare_step3_execution`. For core 0 the saved Step2 record has `new_ops`, `new_tensors`, `new_edges`, `removed_edges`, and `spill_records` all empty. Step2 code's `ext_edges` is then the original graph edges (its rewiring condition is never met), and `_build_extended_graph` appends no op/tensor. Step3's `execution_graph = deepcopy(ext_graph)` appends direct `MEMORY_REUSE` op edges, so `task_return[0]['0']['graph']['edges']` is **not** the Step1 graph. This script uses saved `step2[0]['ext_edges']` and saved task graph ops/tensors, and requires the graph-edge multiset difference to equal the recorded Step3 memory dependency source/target pairs exactly. Static inspection of the archive found 730 such added edges and no core-0 spill/new/removed fields; the one-shot script will recheck these facts before importing/calling official Step1. It never uses `task.op_preds` or the post-Step3 edges for Step1.

The unchanged official `step1_schedule` is called once. Official `_prioritize_task_seq` is then applied first to the archived old `op_subgraph` mapping; its output must match archived `task.seq` exactly or the script stops without making a candidate claim. For new priority labels, original compute op labels come from the new plan. The script derives each COPY_IN label as the earliest local consumer SG and each COPY_OUT label as the latest local producer SG, using the recovered Task graph and the same min/max rule as `_build_scene_b_tasks`; every archived old COPY label must first match this reconstruction. With fixed owner and one monotone contiguous SG contraction, this determines the new COPY labels without Task construction. If any COPY lacks a compute endpoint or old label differs, execution stops. It then calls `_prioritize_task_seq` on the same single raw Step1 sequence and the new SG order, recording complete old/new MTE2 lists and the target COPY positions. These are sequence positions, **not issue times**. The script does not call Task, Step2, Step3, E0/E1/E2 or pipe bound.

Dispatch is reserved for root/Luna after review. Proposed exact command in this worktree:

```sh
python3 -B results/a/q3-nikolastarx/convex-pilot-step1-20260925/step1_only.py
```

Run it under an external supervisor with one worker, a 60-second hard wall timeout, stdout/stderr retention, **one** real 005 script process and zero retries. On first exception retain logs and stop. The script writes only `RESULT.json` in this directory on complete success. This preparation does **not** authorize that dispatch. No result or official Step1 call has happened here.

Correction to the prior convex pilot proof report: it checked only direct same-core arcs, which does not establish that the entire core word is a topological extension across paths leaving and reentering that core. The explicit full original-op quotient DAG test for the *specific* emitted candidate remains an independent acyclicity certificate; the asserted general closed-form cutoff requires the stronger topological-word premise or an explicit quotient check. This Step1 script does not rerun or extend the convex candidate search.
