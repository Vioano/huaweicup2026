# 共享输入容量分段：044/046 五核官方机制验证

构造/runner 固定 `78d82a99ba00f07e2890c87a7132955369efa73b`，manifest SHA `7591f1f41ca3eb49401105117b27e9ff9a278727246f7f9d3d3af5b76d44d88b`。直接求一个受 L1 共享输入并集约束的连续分段 DP，不搜索官方分数、实例编号或历史切点。两个旧控制来自同一固定 forest500 算法，已核对保存的原件哈希，未重跑。

| 原图/五核 | 旧 M | 新 M | M 改善 | 旧→新 extra / B | 旧→新 spill / B | 旧→新字节命中率 |
| --- | ---: | ---: | ---: | --- | --- | --- |
| 044 | 41205 | **38390** | **6.8317%** | 1751296→140800 | 1585152→0 | 60.4754%→1.1007% |
| 046 | 83050 | **82505** | **0.6562%** | 1538048→466944 | 1087488→0 | 46.3055%→0% |

减少重载可同时降低 Makespan 和字节命中率；高命中率不是独立优化终点。这里没有新计划 P2 配对，不能报告其 CacheGain。切点还改变计算分配与跨核搬运：044 的 partition_added 从166144降至140800B，046从450560增至466944B；不能把全部时间收益单独归因于消除 spill。

新方案各段共享输入均不超过 L1；其中最大段仍有516096B，仅余8192B，所以静态约束原本不保证激活可共存。本次两个正式结果的 spill=0 是观察事实，不能推广成所有输入上的保证。新固定计划计算/跨核下界分别26842、74990cycles，距离实际M仍有11548、7515cycles；这些是固定计划松弛下界，不是全局最优。

全100图五核的独立静态筛查仅6图满足此构造守卫；001/036/083/092的候选下界已不能改善已有forest方案，未消耗E0。剩余正是044/046。本实验仍是已见结构样本验证，不是未见泛化实验或新完整500成绩。

## 调用、时间与资源

正式调用恰为 **2 P3 E0、0 P2、0 solver、0重试**，没有另外重建 Task/Step3。官方内部正常准备包含在调用内。T0 `2026-09-25T00:40:04.493252Z`，结束 `2026-09-25T00:40:04.863680Z`。两个官方子进程外层墙钟0.183752s、0.178339s，采样进程组RSS峰36,831,232B、33,832,960B；它们不是求解端到端时间或真实OS峰值。

协调窗口限定1worker/每格60s/整批120s/采样RSS512MiB。启动双样本 unused1976/2009MiB，运行样本2098MiB，结束2171MiB；pressure均2、swapouts均845228。外部监督未触发停止，自己的进程已正常退出；采样不是连续观测。预算已耗尽，不补跑。

`reproduce_summary.py --forest-root <保存forest500原件的工作树>` 只读原结果、校验哈希并重算表格，不新增官方调用。`run.json`、`resource-*.json`、`evaluation/*/result.json.gz` 保留实际证据。下一步接入统一算法，以完整新500批次验证；不得把这两格拼入旧批次。
