# 三图单岛修复：静态阴性结果

构造源码固定 `212dca8a86c80d37332d7829e72c95b262b5d967`。直接从只读官方 ZIP 解析 066/082/071，重新生成 Attention gap placement anchor，再调用单岛修复；没有调用完整 solver、E0/E1/E2，也没有历史分数选优。

| 图/5核 | 初始计算下界 | 修复后计算下界 | 局部接受岛数 | 是否返回新方案 |
|---|---:|---:|---:|---|
|066|58542|58542|1|否|
|082|133160|133160|6|否|
|071|5218|5218|0|否|

066/082 局部出口提前未缩短整体增强 DAG 的最长路；函数按约定返回原方案，因此本轮没有依据花官方评分预算。不能把局部接受次数写成性能改善。也不能据此证明所有联合迁移或其他切分无用。

修复入口单独耗时约1.65/1.11/0.14秒，仅用于核对实现开销，排除了 anchor 构造、输入输出和官方评价，**不是完整算法墙钟**。完整求解时间请用 calendar500-feedback 的外层计时。当前三图都是已有研发反馈图，不构成留出集泛化证据。

`probe.py` 是本轮确切脚本；`summary.json` 记录其内容哈希与输入/结果哈希。为避免覆盖证据，脚本拒绝已有结果文件。复现时在源码212dca8的全新隔离 checkout 中复制这个 `probe.py` 到同一相对路径（不复制本目录生成的JSON及三个子目录），锁依赖后运行 `uv run --locked python -m results.a.q3-nikolastarx.island-static-20260925.probe`。重复墙钟有波动，不能要求计时字节相同。
