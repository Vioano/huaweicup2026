# 069/071 critical path frontier audit

Run `python3 results/a/q3-nikolastarx/attention-frontier-audit-20260925/reproduce.py --forest-root <path-to-forest-worktree>` from this worktree. Standard library only. It reads the official raw graphs, the [prior headroom summary](../attention-headroom-audit-20260925/summary.json), and the exact saved plan/receipt referenced there. Missing forest artifacts are read from the supplied worktree. Graph, plan, and receipt bytes are SHA-256 checked against the official manifest and prior summary. The script independently reconstructs each compute op's core from the plan's subgraph mapping and schedules, then checks every receipt dispatch and each critical boundary against it. `summary.json` records repo-relative paths, artifact source labels, and identities. No plan construction or evaluation is run.

The saved longest delay path of 071 has segment lengths **7→4→2→93→6** ops across cores 3→2→1→4→1. Its first three cross-core edges are original **78→80**, **91→105**, **391→460**. These are a concrete compact multi-capsule interval, followed by the long attention row. In 069 the lengths are **8→3→164→10→6**, with a short **90→91→101→92→543** interval before its long middle segment. The 071 interval is the stronger first structural candidate for joint assignment analysis: inspect original nodes **72–78, 80, 81, 90, 91, 105, 391, 460** and the exact tensor readers in `summary.json`. This identifies a candidate set, not a proposed new placement.

For each of the eight path boundaries, the exact pair of receipt `capsule_dispatch` modules has one original producer–tensor–reader triple, one source op, one tensor, and one target op. The critical tensor sizes are 96–1536 bytes. Shared reader fanout is present at 071's 91→105 and 391→460, and 069's 92→543; such fanout can constrain joint movement. This narrow *pairwise* boundary does not make the whole short interval a one-tensor separator: the union of all capsules touched by 071's first three segments has **13 external tensors / 32 dependency triples** (9 capsules, 13 ops); 069's first two segments have **11 external tensors / 17 triples** (7 capsules, 11 ops). Tensor byte totals in the JSON count each cut tensor once, not DDR traffic. This local quotient audit is neither a treewidth estimate nor a physical timing bound. The saved path and prior static lower bound do not prove a causal loss or physical optimum. Official Makespan remains the quality measure; any new plan needs separate legality and E0 assessment.
