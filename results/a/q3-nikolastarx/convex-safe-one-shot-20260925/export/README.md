# Convex-safe 005/K5 P3 diagnostic export

This feed has one successful official P3 E0 result for case 005/K=5 (Makespan 26,427). It is a negative comparison against the prior threshold 24,522, not a legality rejection. The frozen conditional P2 threshold was not met, so there is no same-plan P2 score or CacheGain.

`solver_wall_seconds` and `evaluation_wall_seconds` are null. Worker diagnostic wall was 8.616339791973587 seconds and includes surrounding instrumentation; evaluator-only time was not isolated. Receipt T0/T1 are preserved. The phase made one E0 call, with no retry or other evaluator calls.

`board-feed-convex-safe-005-p3.json` is a one-record `board-submission-v1` feed. `evidence-manifest.json` indexes retained run originals, source/plan/admission identity, input hashes, and baseline. This is one diagnostic result, not a full solver, full matrix, independent replay, or board acceptance.
