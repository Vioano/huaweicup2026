# 201-op convex pilot: static necessary-condition failure

The single frozen static process exited 0. `RESULT.json` SHA-256 is `4fdaa6011428d2493f35b4db155eaeb13d88c089137fa61e9946e488d866c684`; `stdout.txt` SHA-256 `86ef0aef6a51442cc3c342570dfc99b5608e73fc0a0be671fb5812bafe982856`; `stderr.txt` is empty (SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`). No retry, official Task/Step/E0/E1/E2 call, new scoring, or new Step1 call occurred. The prior Step1 result contained the sole official Step1 invocation.

All four input hashes matched `FROZEN.md`. Old and new plans have identical compute-op ownership; cores 1–4 have identical subgraph words. Core 0 uses the archived Task's 795 ops/911 tensors and **1,992** `step2[0].ext_edges`, excluding exactly **730** old Step3 memory edges. The new full Step1 priority sequence has 795 unique ops. Core 0 has 236 L1 and 474 UB on-chip tensors with uses. Counting each tensor's full interval from first to last producer/consumer position gives L1 peak **18,624 / 524,288 bytes** at sequence position 241 and UB peak **21,844 / 131,072 bytes** at position 487. This supports a no-spill capacity placement for Step2 under the interval model; it is not observed Step2 behavior or a Step3 runtime memory peak.

The forced five-core graph has **5,135** namespaced op vertices, **17,083** distinct directed arcs and all **381** saved cross links. Cores 1–4 retain their saved graph edges and FIFO; core 0 uses recovered original edges and new per-pipe FIFO only. Tensor contraction connects **every** local producer to **every** consumer; `(core,id)` names prevent cross-core ID collisions. DFS found an actual **71-edge directed cycle** in `RESULT.json` (`cycle_witness` lists each source, target and edge type). It traverses cores 0 and 4, including two cross links, 46 MTE3 FIFO arcs, 19 tensor-contracted arcs, 10 V FIFO arcs, two M FIFO arcs and two MTE2 FIFO arcs. **No old Step3 memory edge is needed for this cycle.**

With the input/coverage guards and capacity result above, this necessary union graph already has a cycle, so the 201-op candidate cannot pass the final joined execution graph under these saved dependencies and the new Step1 sequence. This is a structural rejection, not an issue-time prediction, Makespan result, or official score. The newer 194-op candidate was not analyzed here. The one-pass window is closed; any later candidate needs a separate frozen analysis.
