# R9 first official diagnostic gate (not executed)

Root read the port, prepared guard, probe and independent proof. A fixed single 005/K5 candidate is selected because the two layered plans already reproduce exactly and 005 is the smaller case. Static constructor source is 2f74818b81155c9439392622296716d4bc6c4005. The 005 and 086 one-shot static reproductions both matched archived Pro plan bytes, with 0 Task/Step/E0/E1/E2 calls; no static rerun is authorized.

The whole prepared graph guard must verify actual Task coverage, local tensor identity, COPY counts/buckets/traffic, Step2 spill absence, four pipe FIFO orders, strictly forward memory edges, and the union DAG crossing count <=4. It cannot replace row recognition or a final official evaluation. Step3 local preparation peaks are not observed multicore runtime peaks; official result.memory_peak_by_core copies the same local values. Root checked this at frozen official P3 lines701-704.

## Exact old control

Root retrieved source feed and the three plan/P3/P2 artifacts with git show from fixed19bebf35205d23fdd832781540f8879da52eeb62, checking each declared SHA before saving bytes in control/. Source paths need not exist in this worktree: they were read from the immutable Git object. control/source_record.json retains that feed row. The full source feed SHA and paths are retained in control/control.json. Old M3=30642, M2=37327, G=37327/30642, all five-core. No fresh scoring produced these values.

## One candidate, two evaluations maximum

One P3; only after all prepared guards pass and M3<30642 may one same-plan P2 run. Accept only M3 lower, M2<=37327, and exact fraction M2/M3>=37327/30642. This does not inflate G by making no-cache execution worse. One worker, 90s/phase, 600s/batch, zero retries; any exception or failed guard stops. 086 has no E0 budget. This diagnostic does not establish a full solver or a new full500 score.

## Resource and source gate

The proof/runner/guard tests used only existing originals or synthetic objects. Actual dispatch needs a separately fixed source HEAD and manifest plus coordinator admission after P1 releases its window. Each worker remains in the outer supervisor process group. No official code changes are allowed. The runner records every reserved/started call and saves a Task capture before checking it. Existing R8 and unified071 budgets remain spent.

A correct static construction still may lose officially due to COPY service, DDR contention, cache timing, allocation order or memory dependency chains. This uncertainty is why the first check is one small pair instead of a full benchmark.

## Archive check

Git whitespace check passes for code/docs/JSON. RESOURCE_PREFLIGHT.txt preserves raw macOS tool stdout containing trailing spaces; those original bytes intentionally remain unchanged. Static runner pins the constructor, parser files, input and candidate hashes, but records prohibited-call counts by inspected code path rather than a dynamic profiler. Its two 60-second child timeouts bound compute; the stated180-second whole-stage budget was observed (under1second), not enforced by an outer timer. No reuse of this already-spent runner is authorized.
