# R9 005/K5 diagnostic board export

`board-feed-r9-005.json` is the two-record `board-submission-v1` export of the retained R9 one-shot run. Its SHA-256 is `27f811e306d4f5ffe52468cb807f6ea5748ecb287259624cb96207ab18ab8b25`; `generate_feed.py` SHA-256 is `ce3808ce0c803fe4f837d3586cac8bdd5ccea1f3c9ab40616380677d8e01fe7b`. Source algorithm and runner are fixed to `e9ff04c3019dedadd5b2ff71ecfdd57ac1aabe4a`; constructor entrypoint is `src.q3.layered_query_flow.construct_layered`. No source was modified and no scoring was executed for export.

The feed records official E0 P3 M3=24522 and same-plan E0 P2 M2=29026, both `status=ok` because the evaluations and guard completed. The local admission criterion was separately `accepted=false`: although both makespans improve on old M3=30642/M2=37327, the exact cache gain P2/P3 falls from 1.2181646106651003 to 1.183671804909877. The eligible count is a board byte/format admission result, not acceptance of this R9 mechanism as the selected solver. This is one 005/K5 diagnostic, not the fixed algorithm's 500-cell performance.

`solver_wall_seconds=null`: the plan was made in earlier static work and this probe did not time complete construction. Each `evaluation_wall_seconds` is its worker's actual `diagnostic_wall_seconds` (P3 8.403244792018086 s; P2 7.181347334000748 s), including wrapper/profile work and, for P3, prepared capture and guard. These are not isolated evaluator timings. The guard's `step3_local_memory_peaks` are saved prepared local peaks, not independently measured final multicore runtime peaks.

`evidence-manifest.json` (SHA-256 `661de486a864e2e731e2233c470201ec145284f4d32826693c5fd24b57765116`) lists full run/result/worker/prepared/resource/reservation/claim/raw stdout/stderr and old-control references; the control JSON itself names and hashes its three bundled originals. Feed `artifacts.manifest` points to it because board schema only permits named artifact slots. The reused official single-core 005 baseline original is `results/benchmark-board/official-singlecore-20260924/005/result.json.gz`, SHA-256 `3efb00f0d7750733b54df72ce2acde81b6bbc510d92771f6414ff985864340a4`, M=95618. This was read, not rescored.

Zero-scoring preflight used the **main-project** `src/benchmark_board/protocol.py` with `--repo` pointing at this worktree and `--submission`; saved stdout is `preflight.stdout.json`. Exit code 0, `valid=true`, `submission=true`, `records=2`, `eligible=2`, no reported failures. The first schema check rejected an extra artifact key; its exact draft is preserved as `rejected-draft-schema.json`, then the evidence-manifest layout passed. This export was not imported to the board, committed, pushed, or notified to the board owner.

The prior unsubmitted feed with the wrong `producer_session` is preserved as `draft-producer-misattributed.json` (SHA-256 `33b48dbde2db3355f3f6ce4e9eeed96f4ee5bd09b0a02ea79a5b38c46bb271c1`), with its original preflight and manifest renamed `draft-*`. The corrected feed uses actual producer session `nikolastarx/s-3172f7b01b604cfb90aefd6396bd87bc`, worker finish timestamps for `observed_at`, the run receipt’s CPU count, and explicit actual admission manifest/decision refs. The R9 Pro source directory and local verification boundary are noted in each record. After this identity-only correction, the main-project protocol preflight was repeated once and again returned `valid=true`, `eligible=2`, `records=2`. No score was rerun.
