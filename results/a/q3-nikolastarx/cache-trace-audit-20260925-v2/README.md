# P3 官方 Cache 事件诊断

输入为 `c2d628ba0fe8dce4630e5f9c0a5c8fb810fcd41a` 同一算法完整100图×1–5核的原版P3结果。对应完整交付已合入主库 `130dfe4c6a10d394a6d04257f660d5a66c51c624`；本次使用其中无基线增强的原始 `board-feed-500.json`，SHA见summary。没有重新求解或执行E0；不是独立E0复现。

逐原件核对SHA、Makespan、核数，按原事件数组（保留同刻处理顺序）重放FIFO，核对每次命中/未命中、插入/淘汰、最终FIFO和命中字节/次数。结合真实COPY_IN结束时刻，将miss按优先顺序分为：单条tensor超过容量、首次访问、已有同key未完成DDR读取、否则为淘汰后的再次读取。它们是观察到的事件类别，不是互相独立的根因，也不是可节省字节/周期的证明；第三类可能同时发生在先前淘汰之后。

| 核数 | 命中字节 | 首次访问miss字节 | 同key未完成读取时重复miss字节 | 淘汰后重读miss字节 |
|---:|---:|---:|---:|---:|
|1|139302786|504319590|0|763293886|
|2|408425818|528571504|29996020|189281502|
|3|503141642|513503078|48273434|182003602|
|4|536535632|514388044|80403276|173753540|
|5|624584862|528838050|84289826|192906450|

表为各核数100图字节求和，仅作诊断，不是官方加速比平均值。所有500结果中单条tensor超L2容量的miss均为0；这不代表所有工作集能装入1MiB。

五核058为255棵归约树、每棵65个计算节点，当前通用affine顺序，hit=0；淘汰后重读17838080B、未完成读取时重复miss18382848B、spill59330560B。079为288棵同规模树，淘汰后重读39266304B、spill184246272B。结构与事件共同支持优先验证减少树中间结果前沿的顺序；不证明其必然改善官方M或CacheGain。

边界：官方每个COPY_IN完成都会尝试insert，包括此前命中的操作；若执行中被淘汰，完成时能再次插入。零字节COPY_IN可以插入但不记录hit/miss，官方timeline不提供对应key，诊断显式报告无法二次绑定的数量，本批500份全为0。重复timeline操作键拒绝。测试包含错FIFO/错统计/重复key、零字节绑定范围、hit完成后重新插入。

复现（`ARTIFACT_ROOT`是已有完整交付的本地checkout路径，输出须为新目录）：

```sh
uv run python -m unittest tests.q3.test_cache_trace_audit -v
uv run python -m src.q3.cache_trace_audit "$ARTIFACT_ROOT/results/a/q3-nikolastarx/witness-full500-20260925-s59/20260924T2032Z-s59ee/board-feed-500.json" "$ARTIFACT_ROOT" results/a/q3-nikolastarx/NEW_CACHE_AUDIT
```

配套同plan P2全量评估由s59独立运行；此诊断不能代替其CacheGain证据。首次分析v1后补充重复timeline/零字节校验，重放500份生成v2；分类数值未改变，所有实际工作均为离线读原件。
