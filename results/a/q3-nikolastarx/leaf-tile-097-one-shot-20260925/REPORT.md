# 097 单核逐叶分块：一次官方机制验证

固定构造及探针源码 `638ef288bfd287a436095965441bfbd0a2f9d488`。原始图、固定配置、官方代码和实际依赖哈希见 `manifest.json`；预算在评价前冻结。按真实图结构与容量代数选择 4×4，保持 forest 控制方案全部原操作、singleton 映射和核心归属。只改每核优先词，不改归约树或物理地址。

| 方案 | 官方 Makespan / cycles | 额外搬运＝spill / B | 累计 M 空隙 / cycles | 正空隙数 | 最大空隙 / cycles |
| --- | ---: | ---: | ---: | ---: | ---: |
| 已有 forest | 10,746,146 | 11,665,408 | 441,873 | 397 | 1,224 |
| 已有 band + 一叶前瞻 | 11,215,134 | 9,347,072 | 910,861 | 1,904 | 546 |
| 新逐叶 4×4 tile | **10,662,163** | **4,026,368** | **357,890** | **101** | 3,672 |

三者均为 3,328 个 M、M busy 10,303,488 cycles、首 M 起点 172、尾部 613。因此本次减少的 83,983 cycles 全部等于累计 M 空隙减少量；相对 forest 改善 **0.781517%**，spill 少 **7,639,040 B（65.48%）**。最大空隙却增大，不能只用最大单次等待选候选。这是时间分解恒等式，不是每个空隙根因的完整证明；本次没有额外 Task/Step3 依赖重建。

新方案 Cache 字节命中率为 0，forest 为约 0.07716%。绝对 Makespan 和搬运量改善，并不意味着每一个次要指标都占优。新方案没有 P2 配对，不能声称其 CacheGain；更不能把这一个机制样本拼入 forest500 后宣称新算法全量达标。

## 执行与资源

只有 **1 次新 P3 E0、0 P2、0 solver、0 重试**；官方 E0 内部准备包含在这次调用中。T0 为 2026-09-25T00:10:29.474596Z，官方子进程外层墙钟 **1.291287 s**，采样进程组 RSS 峰 **139,427,840 B**，子进程退出码 0。外围主机监督共 3.071186 s；它包含采样收尾，不能当求解器速度。构造是单独静态准备，也没有完整在线求解计时。

00:07Z 预检因 unused 980/896 MiB 未启动，保留原记录。00:10Z 准入前两次 unused 1802/1708 MiB；运行采样 1931/1790 MiB，结束 1274 MiB；pressure 均 2，swapouts 均 845228。单次预算 60 秒，RSS 采样警戒 512 MiB；没有触发终止。主机观测是采样证据，不是连续监测或独占性能测试。

本地路由标识发布时换成公开协调 session，原始字节在工作树外私存；`provenance-derivation.json` 记录唯一替换和原/发布哈希。`resource-preflight.json` 中监督脚本哈希保留真实 as-run 值，公开的脱敏脚本不能冒称与它逐字节一致。官方结果原字节未变。

## 复核与下一步

`reproduce_summary.py --forest-root <已有 forest500 工作树>` 只读三个固定结果并核对哈希和时钟分解，零新评价。已有两控制的固定来源由 `gap_frequency_probe.evidence` 绑定。`evaluation/result.json.gz` 是本次未修改 E0 的完整输出，负面指标同样保留。

该结果否定了“逐叶分块必然因更多等待而退化”，但只支持本图一核的一次机制收益；`Q≤C` 仍不是官方零 spill 证书。下一步先审阅 Pro R5 的条件证明，再决定通用选择规则。097 五核全局松弛余量较小，研发同时转向 044 共享输入容量与 attention 的跨核路径，避免为这一个样本扩参。
