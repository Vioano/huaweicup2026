# 离线分桶编译独立核查

只读 `compile.py`、同目录已有产物和 `pipeline-prefix-static-20260925/` 的三份保存输入；只新增本审计及 `test_compile.py`。命令：`python3 -B results/a/q3-nikolastarx/partial-bucket-compile-20260925/test_compile.py`。单进程 0.050 秒，6/6 测试通过；0 Task、Step1/2/3、E0、solver、VM、外部发信及提交。实际 token 用量不可用。

测试分别验证：h=10 与保存示例计划/预测词一致且原 plan、快照、证书不变；h=0 和 h=17 均拒绝；改写保存的 pre-Step2 字拒绝；改写 pilot 的原映射拒绝；h=15 的六笔前缀冷读、两笔 pilot 激活位置 `[6,7]`、五核 1,678 节点联合图和原峰值上限一致；最后一项刻画下面的输入完整性缺口。测试没有重复跑完整七断点表，也没有调用官方代码。

**发现：语义重复的映射键被静默归一。** `compile.py:172` 用 `int(u)` 构造字典，却未检查转换前后键数量或键的规范形式。给保存计划添加 `"00" + 已有opID` 的第二个键、保持相同 SG 时，`compile_split` 仍返回正常候选，并在 `compile.py:232` 输出时丢掉新增键；第6项测试复现。因而它目前不是任意外部 plan 的完整合法性检查器。当前示例源文件的 SHA-256 已在结构表固定，且原输入未发生该问题；若复用此 API 接收新计划，入口应先拒绝非规范/语义重复 op ID，并核对来源身份。此项是离线转换器的输入边界缺口，未声称冻结官方解析器会如何处理该输入。

**未独立证明的边界。** `compile.py:218-220` 用传入证书的原峰值作更严的容量上限，函数本身未验证证书与 plan/快照的 SHA 绑定；若传入错误或被改写的证书，容量通过不再有原证书意义。`compile.py:85-128` 的联合图覆盖原 Task 数据、Pipe FIFO 和跨核 COPY，但不包含 Step3 内存复用边；它证明的是 pre-Step2 条件无环。`compile.py:291-303` 只选择 cold-input 首次消费者变化的受限断点族，中途激活或 COPY_OUT 归桶变化可使同一 q 的其他 h 产生不同 FIFO。新方案还需官方 Task/Step2/Step3 和 E0 复核，当前没有求解质量或实际 Makespan 结论。

复杂度复核：`compile.py:186-190` 现已使用 `inputs[v]`，每条 pilot 链边只扫目标的输入张量，消除了旧的逐边全图遍历。单候选主要为重建 ports、桶、生命期和全核拓扑图，时间 `O(V+E+Σ_t |producers(t)|·|consumers(t)|)`，内存 `O(V+E+Σ_t |producers(t)|·|consumers(t)|)`；如果一个张量同时有大量生产者和消费者，展开的依赖对数会支配成本。这个复杂度比 README 中的 `O(V+E)` 空间说法更严格，值得在下一次整理文档时修正；本轮按唯一写范围没有改 README。

## 父会话修复回执

非规范数字映射键现已拒绝，测试6改为验证拒绝；原6项测试再次通过（0.046秒），0官方调用。README修正了依赖对展开的空间复杂度，并明确证书由冻结入口绑定身份。旧审计发现作为历史保留，不声称任意外来证书已被认证。
