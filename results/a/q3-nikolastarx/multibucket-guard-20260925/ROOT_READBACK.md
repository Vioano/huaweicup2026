# Root verification readback

The first synthetic test failures and import-only recovery failure remain preserved. The separately frozen `import-path-recovery` sets PYTHONPATH to the repository root without changing guard/fixture/runner bytes. Its one process passed all five corrected synthetic tests, then default and multi guard on archived R9 singleton prepared data. Exit0, wall0.4297925s, zero official calls. Root reviewed the entire guard diff and source. This validates the explicit multi-op checks on toys and old compatibility; no new194-op official Task exists yet.
