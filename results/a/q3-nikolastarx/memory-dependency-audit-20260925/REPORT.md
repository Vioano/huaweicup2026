# 079计算间隙的实际内存依赖证据

固定控制源码`fbe5533506ba550d38dd8f3eb9a37a6846a6013c`。2次官方Task构造、10次核内Step3模拟、0新多核P2/P3评分、0solver、0retry；6.556795秒。保留已有plan/结果SHA、官方输入身份和源码清单。未改官方代码或删除任何依赖。

两个计划各5核的实际op集合、所有Pipe完整投影、内存边数量和extra COPY分解与原结果逐项一致；全部PIPE_M开始时刻均精确等于前一M结束及全部真实前驱结束的最大值。内部间隙按达到最大值的前驱边分类（有并列时归为组合类，避免重复计数）。

| 计划/关键核 | M忙时 | 内部间隙 | 内存复用边限制 | 数据边限制 |
|---|---:|---:|---:|---:|
| new pair/0 | 5925744 | 238401 | 188088 | 50313 |
| old forest/2 | 5925744 | 148248 | 117912 | 30336 |

新pair三处最大的1224周期间隙具有实际WAR边，如ADD1380→MATMUL1350，来自L1已释放张量1000002498的2048B额度，源ADD完成时刻恰为M发射时刻。原图的这两个操作位于**同一棵归约树**；1350是该树第33个叶MATMUL，不是下一树首叶。早先仅凭时间线提出的跨树等待假说必须收紧。

该证据说明冻结运行时图确实以这些内存边限制M，不能将边直接删除后声称得到合法优化。内存额度来源是官方执行模型的一部分；新顺序会重新生成Step2重载、Step3额度边和Cache时序。

下一候选：在全部M都是独立计算叶的树族中，保持core归属和每条Pipe操作投影不变，将原词M0,V0,M1,V1,...变为M0,M1,V0,M2,V1,...。只让一个叶的输出比旧顺序提前存活，不累积成全叶先算；这是拓扑合法的候选，静态计算前沿至多多一叶输出，不替代含外部输入和异步生命周期的官方容量验证。对原forest和新band owner分别做固定对照，不把两个效果混为一项。
