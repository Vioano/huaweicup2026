# R6 固定机制的四格迁移准备

085/K5已验证正收益，下一步以082/075/047/005四种负载与门控比例检查迁移。选样来自静态旧计划结构，而非算法中的图编号路由。统一仍使用当前输入heavy/sink基线 + 一次branch-aid，算法字节保持db64a937版本；只为运行器加入已通过结构测试的精确源哈希复用，不重复运行相同五项结构测试。

每格的候选生成最多一次，只有当前基线与固定旧计划原始字节相同、且产出不同合法候选才调用一次E0；其余格保留为未评分原因。不搜索参数、不补跑、不以历史赢家替代新方案。四格最多4次E0，E1/E2为0；一次worker、每格180秒、总720秒、RSS1536MiB、probe30秒、E0120秒。身份、清理或资源故障停止全批。

input-and-budget-request.json固定输入来源及哈希。源版本冻结、协调准入和T0通过之前不执行。当前只是准备，不能声称取得任何迁移分数、统一全量改善或5%目标达成。
