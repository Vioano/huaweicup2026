# P1 008/K5 coalesced phase saved-plan E0

One official E0 evaluation of the fixed research plan. This record is the **coalesced real-reserve phase explicit q7/s3 research construction**, not a unified algorithm or a full-500 result.

E0 returned Makespan 98,932 cycles, scheduled DDR 3,760,344 bytes, extra DDR 1,105,920 bytes and spill 0. E0 wall was 0.921667229 s; the process exited 0 and cleanup was confirmed. The exact plan SHA-256 is `e9327269bc95a81d17ca907a617aed896fd6fdde2a3174044d975f746569f9ce`. The separate official single-core baseline is 487,605 cycles.

The saved plan came from an earlier plan-production run (27 Tasks, 2 Fraction). Its 2.62559666 s figure is only the internal phase interval, not full end-to-end solver wall; `solver_wall_seconds` is therefore null. The current window had 0 solver, 1 E0, 0 E1/E2, 0 retries. Cloud and VM controller time is preserved in `run-public.json`, not reported as solver time. The exact-plan model match is not evidence of broader equivalence.

Feed: [board-feed-008-k5.json](board-feed-008-k5.json). Public run receipt: [run-public.json](run-public.json). Referenced raw plan, result, trace, log, and canonical baseline paths/hashes are in the feed.
