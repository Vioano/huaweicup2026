# R5 008/K5 saved-trace static audit

`audit.json` is read-only arithmetic on this run's locally archived Colab `diagnostics.json` (evidence archive SHA-256 `215c6f501a4f5ed835c20411b7f809605b0247cbbe11902c25ff88901b233d9b`). It reuses the existing pure saved-trace DAG checker on the archived `compiled_ports`, trace and Task intervals. All 1,464 operations match the observed-duration precedence DAG in each variant, with zero unexplained gap. This checks neither fair DDR service nor a counterfactual schedule. There were zero new Task compiles, Fraction responses or evaluator calls.

The phase proposal changes the saved Fraction makespan from 121,475 to 102,473, a 19,002-cycle (15.64%) reduction with the same 53 Task IDs and claimed same partition bytes. Its observed critical chain shifts from core 0 to core 2. Relative to the already saved A whole-seed model (101,836), phase is still 637 cycles slower and 3,209 above the 99,264 threshold. The path decomposition explains the comparison arithmetically: phase compute work 91,704 versus A 99,264 saves 7,560, while DDR duration on the respective observed paths is 9,769 versus A 2,472 (7,297 more) and gate time is 1,000 versus A 100 (900 more). These are path sums, not isolated causal effects.

Core 2 ends Task 28 at 97,552, pays a 100-cycle gate, then runs Task 32 from 97,652 to 102,473 (4,821 cycles). Task 32 is the remainder tail1 after the 7-chain reserve (Tasks 22–28) and 14-chain body; it is not part of that reserve. Core 0 and 1 finish analogous tails at 100,855 and 101,901. Thus the remainder tail1 is on the observed critical path; its *incremental* cost against another partition or ordering cannot be inferred from this trace. Per-core M/V busy time remains 50,952/48,312 on cores 0–2; phase increases M/V overlap to 6,623–6,872 versus control 5,060, while global DDR busy union remains 63,084. The phase trace redistributes concurrency rather than reducing DDR work.

This is a local read-only audit of locally saved Colab Fraction trace bytes, **not** a local model reproduction or official E0 result. Integer retirement and DDR fairness are not validated by the precedence checker. The `audit.json` contains per-core occupancy, gates, terminal intervals and path evidence.

Reproduce the static audit using `python3 results/a/p1-r5-phase-colab-20260925/static-audit/reproduce.py --output NEW.json` from this worktree; the default input is the frozen relative `run-0717Z/evidence.tar.gz` and existing output is refused. The A comparison reads `results/a/p1-r5-local-audit-20260925/traces/whole_seed-audit.json` at SHA-256 `6f1a7f8ebd027253a3d0480440c673ff0111df0c15c2eafbc15a014213fe87b6`. The Colab run produced the Fraction traces; this audit did not rerun it.
