# 合并真实整链 reserve：模型达到 98932，待独立 E0

固定 008/K5 控制对已于 2026-09-25T07:38Z 在一个 CPU Standard 窗口实际运行。控制方案的 Fraction Makespan 为 **108668**，错相方案为 **98932**；它比此前整链 A 的同模型保存结果 **101836** 少 **2904 周期**，也比预声明条件门槛 **99264** 少 **332 周期**。这支持进入一次独立 E0 验证，**尚不是官方成绩或统一全量改善**。

原始两键计划、编译签名、两次完整 trace、下载清单和停机回执见 [run-0738Z](run-0738Z/manifest.json)，摘要见 [result.json](run-0738Z/result.json)。算法源为 `77977e3000904a47013458e666fee1ecddefb5de`，控制器源为 `109c6bb0c5c949df8bc94bcde1135fd09751bdd2`。最终相位计划 SHA-256 为 `e9327269bc95a81d17ca907a617aed896fd6fdde2a3174044d975f746569f9ce`。

实际一次编译 **27 个 Task**、两次 Fraction 响应；E0/E1/E2/retry 均为零。两组搬运量均 **3760344 B**，保存响应的 DDR 忙碌并集均 **63084 周期**。研究 runner **2.62560 s**，完整控制窗口 **37.81011 s**；两者都不是统一生产求解器的端到端计时。stop 返回 0，fresh sessions 精确为空，本地 watchdog 已退出，远端剩余 runner PID 为空。窗口已释放，没有自动启动 E0。

以下说明构造与运行前验证。减少 Task 数不是一般的质量保证；本次结果只适用于冻结输入、配置、分区与响应模型。

前一次 R5 对照证明，同分区重排可降低该模型方案的 Makespan，但 53 个 Task 的错相方案仍比旧整链 A 慢 637 周期。观察路径上的计算节省被 DDR 持续时间与门控增加抵消，详见 [R5 实测报告](../p1-r5-phase-colab-20260925/README.md)。

本提案只改变 reserve 的组织方式：每核把相位前的 h 条完整链合为一个 Task，把相位后的 H-h 条完整链合为另一个 Task，空段不生成 Task。两次 body 和 drain 不变，余数 tail 保留。控制方案将两段都放在 body 前；错相方案将后一段放在 body 后。每个控制对保持相同分区、Task ID 与核归属，只改变合法 Task 顺序。它适用于已经证明独立、无 COPY 桥的私人真链，不能直接扩展到一般 DAG。

对已冻结的 008/K5 原图，固定 q=7、s=3、参考周期 35405、链计算量 4512 时，结构检查得到 **27 个 Task**（各核 5/6/6/5/5），原提案为 53。两份计划均通过官方结构及 Task 顺序校验，5 项纯计划测试通过；这份运行前证据见 [structural-preflight.json](structural-preflight.json)，后续真实运行证据另列于上文。

代码：[构造](../../../src/review/p1_coalesced_phase_plan.py)、[研究 runner](../../../src/review/p1_coalesced_phase_probe.py)。研究 runner 复用既有完整 MEM 编译及两次有时限 Fraction 响应守卫，不改生产统一算法。相位参数来自明确的当前输入参考值，代码没有 case-ID 分支或包宽/相位扫描。

合并改变局部 FIFO、MEM 前驱、缓存生命周期和实际相位，不能用旧 trace 推算新分区的成绩。本文不将所有数值差异归因于单一门控或 DDR 因素，也不证明 Fraction 与 E0 对该计划一致。下一步交付上述固定计划做一次独立 E0，再判断能否推广为按图结构选择的通用构造；不从一个机制图推断全量显著提升。
