# 结果

正式实验建议放在 `<task-id>/<run-id>/`：包含指标 CSV、预测/失败样本、运行记录、输入与代码哈希。保留实际运行命令、单位和固定随机种子。不同人或不同配置不要共写一个目录。

`demo/` 由 `uv run python src/demo.py` 生成并被 Git 忽略。确认要共享的正式结果应通过任务 PR 明确提交；不要把整份缓存或大型模型权重随意加入 Git。
