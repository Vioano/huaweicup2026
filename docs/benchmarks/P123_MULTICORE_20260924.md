# P1/P2/P3 多核对照：2026-09-24

状态：**运行中，尚无最终全量结果**。本文记录已经确定的范围、实现版本和统计口径；预检单元、部分 CSV 或已生成的局部图均不能替代 100 图完整报告。最终成功/失败计数、完整结果清单和统计结论由本轮主执行会话在收尾后回填。本文编写过程没有新增求解或评价调用。

## 实现与覆盖范围

用户确认 P2 使用现有通用基线完成全 100 图对照。P1 采用仓库默认 32 次候选评价预算。算法源码在各自固定工作区或只读源码快照中运行；控制器不改变求解器，不按旧成绩挑选历史赢家。

| 角色 | 固定提交 | 本轮实际入口与含义 |
| --- | --- | --- |
| P1 | `4dff90ef699fd51845cf482951e8477066f5f566` | `src/q1/search.py search`；有预算的候选搜索，seed=0，在线 E1，最后用原版 E0 确认选中计划及原 stub 基线 |
| P2 | `0b58c123cccf02fc993b741d79dcd8511e4dd38f` | `python -m src.q2.construct`；按拓扑次序与计算权重划连续块的确定性基线，构造时不评分 |
| P3 | `a4e7ee13310d693ec4fb5cc236669ceb3b172d1f` | `python -m src.q3.solve`；结构守卫选择 resource_word 或 affine_eighth，在线一次原版 P3 E0，通过后发布计划 |
| P1 所用 E1 | `5bfe53a29c1ba05167239f51ea937e602f7f85b4` | `src/eval_exact/` 的既有精确批量实现；本轮不接入后来新增的原生后端 |

P2 的名称固定为 `q2_contiguous_baseline`。本轮没有将 Fang 的四核 M1/M2 搜索或 PR59 的历史强种子重排包装成可泛化到任意核数的求解器；该基线的成绩不能代表那些优化方法的成绩。

输入为冻结官方 `case_001.json` 至 `case_100.json`，全部使用同一固定 `config.txt`。计划范围如下：

| 评价单元 | 核数 | 计划单元数 |
| --- | --- | ---: |
| 官方 singlecore 基线 | 1 | 100 |
| P1 搜索 | 2、3、4、5 | 400 |
| P2 通用基线构造 | 2、3、4、5 | 400 |
| P3 在线构造 | 1、2、3、4、5 | 500 |

合计 100 个独立官方单核基线、1300 个 solver 单元。P1/P2 表中的 1 核列复用官方单核基线，明确标为 `official_singlecore_baseline`，不是另外运行了一遍 P1/P2 求解器。每个成功 P3 单元另以完全相同计划进行一次 P2 无 Cache 对照；它不增加 solver 单元数，但消耗额外官方评价。

协议的完整调用预留上限为 E1 12800 次、E0 2300 次：后者由 100 单核、最多 800 次 P1 两类确认、400 次 P2、500 次在线 P3、500 次 P3 同计划 P2 对照组成。这是上限，不是实际调用量；未启动、失败、超时和结果未知须结合过程收据分别记账，不能以成功表行数反推完整实际开销。

## 数值指标

`makespan_cycles` 是官方事件模拟的完工周期数。求解器运行几秒和计划执行多少周期是两个不同量，本报告不把它们相除称为加速比。

P1/P2 的逐用例多核加速比定义为：

```text
multicore_speedup(case, k) = official_singlecore_cycles(case) / makespan_cycles(case, k)
```

官方 singlecore 将全部非 COPY 操作合并成一个子图，放到核心 0，通过官方 P1 路径执行；其等待参数仍读取官方配置。它不等于“任意算法在 cores=1 时产生的计划”。P1/P2 有效单核基线行的比值为 1。

P3 的主要 Cache 对照保持图、计划字节、核心数及配置身份相同：

```text
cache_speedup(case, k) = same_plan_P2_cycles(case, k) / P3_cycles(case, k)
```

这里的无 Cache（也可在图上标作 noL2）是原版 P2 执行模型；有 Cache 是原版 P3。没有将官方 Cache 容量改为零，没有用另一个 P2 优化计划作分子。P3 的 `official_singlecore_cycles / P3_cycles` 单独提供为补充多核指标，不能与上述 Cache 收益混为一项。配对 P2 失败时，P3 本身仍可有有效 Makespan，但 Cache 比值记为 NA。

官方配置保持 L1=524288 bytes、UB=131072 bytes、DDR 带宽=60 bytes/cycle、P1 同核/跨核等待=100/1000 cycles、P2/P3 跨核 COPY delay=500 cycles、P3 Cache 容量=1048576 bytes、Cache 带宽=250 bytes/cycle。原文件不修改。

`cache_stats.hit_rate` 直接取官方按字节命中率，不改用命中次数/访问次数。`data_movement_bytes` 保留官方完整字段映射，不猜测字段含义或把计划 COPY 字节总量当成实际 DDR 字节。周期数不经整数强制转换修饰。

每个问题、核数和指标分别计算有效逐用例比值的算术平均，并列有效数/预期 100、失败、缺失、指标不可用数量；中位数与极值另列。它不是总周期之比。未覆盖全 100 时，只能称“有效子集均值”，不能称完整 100 图均值。失败或缺失单元保持灰色 NA，不补零、不填理想核数，也不从旧研究结果补格。

## 墙钟、并发与失败

运行环境由主执行会话核对：Windows 11，AMD Ryzen 7 5800H，8 个物理核心、16 个逻辑处理器，约 15.86 GiB 物理内存，Python 3.12.13。协议保存具体 Python/platform 字符串及依赖锁哈希。未实测整批 RSS、父子进程总 RSS 或硬内存上限。

控制器最多同时运行两个 benchmark 单元；这不等于全机只有两个进程，P1 会使用自己已有的有界提案/评价子进程。子进程设置 `PYTHONHASHSEED=0`，OpenBLAS/OMP/MKL 线程数为 1，并禁写 Python 字节码。算法的模拟核心数 1–5 与主机上并发跑几个评价单元无关。

P1 参数固定为 `--candidates 32 --budget 60 --candidate-timeout 5 --confirm-timeout 30 --seed 0`。其中 60 秒是既有搜索预算，5 秒为候选阶段超时参数，30 秒为官方确认阶段参数，不应把 60 秒误读为包含所有确认、写盘和进程启动的完整求解时限。控制器另对 P1 求解进程设置 150 秒保护；P2 构造 120 秒；单核/P2 官方包装器、P3 在线求解及 P3 同计划 P2 对照各有 180 秒保护。超时清理可能产生额外时间，这些不是硬实时保证。任何后续修改以新协议和实际过程收据为准。

| 表中时间 | P1 | P2 | P3 |
| --- | --- | --- | --- |
| `solver_wall_seconds` | 子进程启动到退出，包含候选生成、在线 E1、两类最终 E0 确认及求解器写盘 | 仅通用基线构造进程，尚未包括外部 E0 | 包含结构选择、构造、一次在线 P3 E0、证据与计划发布 |
| `e0_wall_seconds` | 选中计划 E0 确认的内部组件，已在 solver wall 内；不是两类确认总和 | 单列外部 E0 包装器进程墙钟 | 在线官方 import/config/evaluate 组件，已在 solver wall 内 |

因此不能机械将两列相加：P1/P3 会重复计算；P2 如需“构造加最终验证”可另报两项之和。P3 同计划 P2 对照是额外实验成本，保存在 `no_cache_process/process.json`，不隐入在线 P3 算法的求解时间。完整单元墙钟保存在 `cell.json`，可包含额外核对、压缩和配对工作。并发共享机器且各单元非重复独占测量，墙钟不是稳定尾延迟或语言/内核性能结论。

官方包装器自身的 `eval_seconds` 只包围一次官方函数调用；`runner_seconds` 还包含进程内核验、读取、import 和压缩回读等工作，但不包括进入 main 前的启动/import。外层 `process.json` 才提供完整子进程墙钟。

失败、官方拒绝、超时和结果未知保留原始证据，并记 NA；控制器不自动重试，不暗换候选或算法。已完成收据可以被继续查阅；未完成目录须先核对进程和调用状态，不能直接删除重跑。恢复只处理同一协议下尚未创建的单元；算法源码/参数/配置/评价器身份变化必须另开运行目录，不能覆盖旧协议后混合统计。预检单元保留其真实身份和结果，不依据成绩选择性删除。

全量启动前仅修订控制器的错误处理：清理失败写入证据并阻止后续进程启动，成功标记移到最终比值及计划哈希检查之后。三个模拟故障测试通过，没有启动算法或官方评价。原 `protocol.json` 与首版控制器源码均保留；`harness/revisions.json` 记录原协议哈希、每版控制器哈希及修订理由，对应源码快照放在 `harness/`。新 `cell.json`、`process.json` 与 `invocations/` 记录实际控制器版本；旧预检对应原协议版本。实验身份和预算逐字段比较保持一致，只允许有明确修订说明的控制器哈希变化，已完成预检不重跑。

本批 09:39:59Z 启动时实际载入的控制器 SHA-256 为 `b622d7b281923b372a74086afa6b88dff1b51927d2b2f6988c5656f915e05c4b`，完整字节保存在 `harness/p123_run-b622d7b281923b372a74086afa6b88dff1b51927d2b2f6988c5656f915e05c4b.py`。之后工作区中的 `src/benchmarks/p123_run.py` 增补了跨进程输出锁、持久清理失败标记和未结束运行检查，源码 SHA-256 为 `6f2d1decf45e25a7ea49014980dea41cc51e124b11f9897db67448292ffc992b`；六项本机隔离故障测试通过，但这个新版本**没有替换本批内存中正在执行的控制器，也未重新启动实验**。本批是否使用某项保障必须按 invocation 中的实际版本判断，不能把后续代码测试回填为本批使用过的能力。

八个预检单元及两个基线的 215 个文件在 09:48:45Z 另行记录原件哈希，保存在 `harness/preflight-observed-manifest.json`，清单身份链接到版本沿革。该时间是实际补充记录时间，不倒填为实验开始时间，不声称具有作者签名。单核 002 先以包装器手动启动，没有外层过程收据，不能补造该条墙钟；单核 014 和其余预检均保留当时控制器的过程收据。

## 源码与结果证据

`docs/a/source-manifest.json` 固定官方文件身份，官方 code 聚合 SHA-256 为 `de11a83db8d7c47ed328b15a7df71d613a833b16cd23ee9fe877999578a1ace0`，配置原字节 SHA-256 为 `dcd10de54b23f8366428fb24e828812b1da9549e6eae4a3c3f38604fe5ae77b9`。独立官方包装器在 import 前检查全部官方 Python 源码集合、逐文件大小/哈希、聚合哈希和配置；已在清单中的同名官方图也逐字节核验。

控制器对求解器工作区检查固定 HEAD 与已跟踪源码状态；API 恢复的源码快照使用固定提交、Git blob 身份及 `snapshot.json` 中逐文件 SHA-256 核对，保持原算法字节。P1 使用的图还与共享官方图比对。复制到快照的官方 code/config 同样须与清单匹配；不能依靠平台换行转换后的文本看似相同来替代原字节一致。

运行证据布局为：

```text
results/a/<run>/
  protocol.json                固定源码、参数、配置、依赖和控制器身份
  harness/                     原控制器与明确记录的异常处理修订
  invocations/                 每次继续运行的范围、状态与控制器哈希
  baselines/<case>/            官方单核 summary/result 与进程收据
  cells/P1/<case>/k<k>/         搜索证据、计划、两类 E0 确认、cell.json
  cells/P2/<case>/k<k>/         构造、独立官方评价、计划、cell.json
  cells/P3/<case>/k<k>/         在线 P3、同计划 P2 对照、cell.json
  comparison.csv               逐用例表，保留失败和空指标
  progress.json                过程进度；不是最终验收
```

`p123_evaluate.py` 保存的 `result.json.gz` 是完整官方函数返回对象的紧凑 JSON，无 CLI 专属输入名补充和 Trace 格式化；不能声称它与官方 CLI 输出文本逐字节相同。其 `result_sha256` 指解压后的 JSON 字节，`result_gzip_sha256` 指 gzip 文件；包装器实际解压回读核对。P1 原有 CLI 结果/Trace 仅在本轮生成后无损压缩并核对原字节哈希；P3 在线原入口的 receipt 中 `result_sha256` 指压缩文件，读取时按各自字段定义验证，不能混用。

公开汇总只保留相对路径、文件名或角色标签。子进程 stdout/stderr 是本地故障证据，可能含私人绝对路径；发布前需检查，不能未经核对把完整本地日志当公开汇总。私钥、账号令牌或会话地址不属于实验产物。

## 复现命令

以下从 benchmark 工作区根目录执行；目录名为可替换的项目相对路径，不包含开发者个人路径。先准备四份固定源码目录，分别对应上表完整 SHA。目录必须是干净的固定 Git 工作区，或带可验证 `snapshot.json` 的原字节源码快照；P1 工作区须有同一套官方 100 图，各角色均须有核验通过的官方 code/config。使用 Python 3.12 环境和仓库 `uv.lock`；依赖准备与实验墙钟分开记录。

```text
uv sync --locked
uv run --locked python -B scripts/a_materials.py --extract
uv run --locked python -B src/benchmarks/p123_run.py --p1-root ../sources/p1-4dff90e --p2-root ../sources/p2-0b58c12 --p3-root ../sources/p3-a4e7ee1 --e1-root ../sources/e1-5bfe53a --output-dir results/a/p123-multicore-REPRO-RUN --cases all --cores 2,3,4,5 --problems P1,P2,P3 --workers 2 --include-p3-one
```

`REPRO-RUN` 必须替换为新的运行名称。该命令会真实执行上面的完整范围，不是只读报告命令。未完成运行不得因目录已存在就自动换名称重复投递。

单个官方基线包装器的可复现入口如下；这也会消耗一次新的官方评价，只用于明确安排的独立复核，不在全批之后默认再执行：

```text
uv run --locked python -B src/benchmarks/p123_evaluate.py --mode single --graph data/raw/a/official/data/case_002.json --config data/raw/a/official/data/config.txt --official-root data/raw/a/official --manifest docs/a/source-manifest.json --output-dir results/a/p123-single-REPRO-RUN
```

已生成 CSV 的制图入口只读结果，不运行求解器或官方评价；图目录同样须为新目录：

```text
uv run --locked python -B src/benchmarks/p123_plot.py --csv results/a/p123-multicore-REPRO-RUN/comparison.csv --output-dir figures/a/p123-multicore-REPRO-RUN
```

图中保持 001–100 全部用例，并分别给 P1/P2 多核比值、P3 同核同计划 Cache 收益以及注明补充性质的 P3 多核比值。结果均值表须同时展示有效数量；部分运行也能制图，但图不能因此标作“全量已完成”。

批次停止、CSV 聚合完成后，可运行以下只读审计。它不会重新评价计划；完整覆盖表示每项都有结果或失败记录，不表示每项计算都成功。`--output` 必须指向新文件。

```text
uv run --locked python -B src/benchmarks/p123_audit.py --run-dir results/a/p123-multicore-REPRO-RUN --require-full --output results/a/p123-multicore-REPRO-RUN/audit.json
```

## 收尾待回填

当前没有最终 100 图均值、全矩阵成功声明或算法优劣结论。主执行会话收尾时补充：最终协议/控制器身份、实际开始结束时间、100 个基线与 1300 个单元逐项覆盖、失败/超时/不完整 Cache 配对、实际评价调用账、均值及其有效数、最终表图和完整结果清单哈希。若某项未测或未完成，保留事实与 NA；不能以有限预检、程序退出或图已经生成替代完整证据。
