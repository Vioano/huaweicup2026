# 选题策略评估报告

负责人：本任务 Codex；供用户决策。分支：`codex/selection-report`。沟通 Issue：无；未发送消息。

1. **任务目标**：依据用户提供策略评估六题，给出首选、备选及翻转条件；不求解赛题。
2. **输入文件**：用户指定外部资料目录中的 `HuaweiCup_Selection_Strategy.md` 与 `2026题目/`。使用 `--strategy` 与 `--source-root` 传入，来源哈希见 `results/selection/run.json`。原件未修改。
3. **输出要求**：`output/pdf/HuaweiCup_Selection_Report.pdf`，两页A4；`results/selection/` 评分与情景CSV；`figures/selection/` PNG/SVG；`src/selection_report.py` 可复现。
4. **限制条件**：固定策略权重30/30/15/15/10；未知H以相同中心占位并保留0–4范围。用户明确缩减为材料评估，不开发求解器、不跑长实验。
5. **验收标准**：PDF页数≤2，实际渲染无缺字/遮挡；全部比较分由评分表一致计算；分析评分不冒充实测或获奖概率。
6. **截止时间**：本轮交付，2026-09-22，Asia/Taipei。

## 交付记录

运行：项目锁定环境 Python 执行 `src/selection_report.py --source-root <题目目录> --strategy <策略文件>`。已在主项目执行 `uv sync --locked`；未修改依赖。PDF经 `pdfinfo` 与 `pdftoppm` 校验和逐页视觉检查。

结论：A首选，D备选；材料分析评分68.75与61.25。H完全未知，不认为冷门或题号本身有利。中心权重扰动稳定，专业强队与本队能力假设改变则翻转。评分区间见 `score_ranges.csv`，是分析范围而非置信区间。

已完成：六份DOCX正文/附录文字阅读、全部附件文件结构扫描、A评估器README和接口检查、F数据说明阅读。B部分公式为嵌入对象，未逐式核验；未把提取不完整的公式用于计算。未执行评估器、数据清洗、模型拟合或团队能力测试。

F数据说明页眉/页脚包含与题面要求矛盾的建模建议和未实测数值（如无需检验集、直接给定R²）。这些不作为本次结论或方法依据；只采用正文的数据来源/字段说明与题面交叉核对。正式求解前需核对可信原包。

官方邀请函在本轮只读核查：通常每题两支入围，9月23日08:00开赛、9月27日12:00提交MD5截止。未核验用户本地材料与正式题包的校验和，也未核验报名/队员资源；报告只针对所供材料。
来源：https://cpipc.acge.org.cn/cw/contestNews/detail/4/2c9080189dcfa24e019dddacc24a1314?page=0

绘图风格：ChenLiu-1996/figures4papers，CC BY-NC 4.0，使用其配色与排版原则；新写中文双页布局和敏感性图，未复制求解代码。

团队邮箱完整只读查收：0话题、0评论、0指派事项；未发送消息。PR：未创建，本地报告交付。

附带维护：按用户新增要求，在主项目 `AGENTS.md` 写入外置盘元数据清理方法及LaunchAgent核查状态，保留其他任务最新修改；未触发整卷清理或改动后台配置。
