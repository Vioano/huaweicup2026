# Preserve historical payload bytes, including CSV CRLF, across checkouts.
* -text
# Whitespace in extracted source text and generated SVG is original evidence.
data/processed/selection/*.txt -whitespace
figures/selection/*.svg -whitespace
results/selection/*.csv -whitespace
