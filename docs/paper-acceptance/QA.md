# 2026-09-26本机验证

平台：macOS，本机Python 3.14.5，Node语法检查；Codex内置浏览器实际点击。没有启动GitHub Actions，没有新增依赖或算法/evaluator运行。

## 已通过

- 18项独立状态协议测试：草稿不改变验收、不同账号提交/复核/接受、不能跳过复核、同账号换任务不能代签、并发旧revision保留为冲突、新论文或标准不承接旧状态、缺检查或移动分支证据拒绝、真实评论作者核对、编辑/删除来源使记录和下游判断失效、重复同步不重复应用、未知发送不盲目重试、发送后回读去重、PDF变化拒绝、扫描子串误报回归、作者自报不改变验收、作者报告基线不匹配拒绝。
- 实际网页：深色首次显示、浅色可读、刷新后保留手动主题、恢复深色；逐项清单/详情；关键词筛选、句段搜索；作者262条基线报告可见；并排显示检查点01第16页和参考II第10页的真实PDF图像；四份参考的代表页返回已核PDF生成的PNG。
- 窄屏响应式基本检查：设置390×844视口，导航和单列布局可读取，长表在自身容器滚动；测试后已恢复原视口。未声称实体手机或全浏览器覆盖。
- 真实表单保存、共享发布及回读：修复浏览器验证发现的checkbox集合读取错误后，发布L01真实整改意见，回读 [5844258371号评论](https://github.com/huaweibei123/huaweicup2026/issues/217#issuecomment-5844258371)。独立的本机空状态目录再次同步得到同一作者、评论和L01 revision=1。没有写入模拟的“通过”。
- 标准升级为.3后，.2记录留历史并显示失效；新.3下53项均未接受。句段检查已实看SP-003与SP-022（后者11项具体问题），保留原文、物理页码、要求及未复核状态。
- 实际HTTP接口返回200；非本机Host、外站Origin、缺少专用头的写请求返回403。浏览器已查看页面阶段未捕获脚本错误；最后修改再次执行Node语法检查。

## 实测边界

本机两个状态目录接收相同GitHub记录，只证明共享来源读取一致，不证明两位队友、Windows或跨设备实际验收。没有自动常驻监督任务。页面仍由本次启动的Python服务提供，终止进程后需要按README重新启动。

来源报告的262条是作者自报，含正文及图稿说明；610个词语候选和22处句段实例不是“已经找全所有错误”。此前基于关键词的检查漏掉整句多个表达，已按用户反馈补入SP-022并改变检查要求。未标红处不判合格。栅格图中文字、权威术语源及新稿科学含义须继续逐项回读。

作者表示已重写Markdown，不等于固定PDF交付或语言通过。当前全文语言未通过；0项最终接受。

全文目录新增后，用固定提交下的11份章节文件、实际检查点TeX和图注清单生成747个检查单元，按类型统计376条句子；不把92条图稿说明或15幅图内待检查任务混称正文句子。逐项复核目前0，不能声明完成全文审阅。

## 双向协议与类级标注增补

- 22项本地协议测试通过：新旧源稿混用、伪造引文、覆盖遗漏、子Agent自称通过、批注重复/无法回指均拒绝。批注校验实际通过7事件/7类别，acceptance_changed=false。
- 59条模型提议保留：8条不采用，51条需作者处理或解释；一般初审331块与谓宾专项331块属于同一源文，不能相加成662个不同内容。没有新最终通过记录。
- 本机8879实际读取state/semantic-audit/annotation-workflow成功。浏览器实际从CLASS-04进入6条关联意见，原句、规则、固定源稿链接与状态可见；语法检查通过。
- 本次主体改动仅验收台/协议/AGENTS入口，作者稿件和供用户批注的CP02未改。新稿PDF、图内文字与51条改文尚未完成验收。

## .6批注接收与角色更新

- 实际核对前7条097240c3固定批注原件的SHA256、原句、用户原话及坐标；另外10条读取作者工作稿。累计17个事件、11类要求，USER15/16保留双事件而合并同一位置。
- 增补QTY-001后共60条审阅意见，8条不采用、52条待作者处理或解释；不会因作者或原写作任务角色变化而自动通过。
- 根AGENTS入口仍经PR218整合。原写作任务已回读协议.2固定版本，最新.3角色调整和.6标准另待实际签收。

本次更新后实际回读8879标准.6、17事件/11类和当前分工；真实浏览器切换深色/浅色并恢复深色，检查类别列表与团队状态，控制台无错误。22项本地测试通过，JS语法检查通过。
