# P2 全核补齐与并行授权

用户于本会话明确要求使用 GPT-6 Sol subagent，多实例并行调度 evaluator，补齐 P2 新算法覆盖，并指出单核全为 1.000。旧台有全500格的连续构造结果，因此缺失的不是数值格，而是新算法覆盖：本轮开始时 tensor_packet 四核只有70/100新实测，其他核数尚未全测。旧单核100条的实测Makespan等于固定官方A单核分母，不是界面强制为1；改动公式或分母不能补齐实验。

本次固定使用 e64723bdf99669c44f76d8e90ab0379a8578522e 的 tensor_packet，与现有四核70条保持一致。容量窗口原型另有证明和小例，但尚无官方实测，不能悄悄替换当前算法身份。新协议均为 `*-parallel-spec.json`，原串行spec保留，尚未运行的5C与6A/B/C由对应并行spec明确取代。当前用户授权优先于旧串行窗口安排；P1/P3已按准确session通知。

执行两条独立流水线，最多两个P2批次同时运行，每批原runner仍为1 worker、线程1：

- GPT-6 Sol A：round5c（四核余30），随后round7a/b/c（二核100），round9a/b/c（五核100）。
- GPT-6 Sol B：round6a/b/c（单核100），随后round8a/b/c（三核100）。

新增总上限430次solver冷进程、430次外部未修改E0，不对已成功70个四核用例重评。每个用例只有一个直接构造方案，非候选搜索。每批仍solver30秒、E0 90秒、批1800秒、收尾15秒、0重试、首个意外失败停止；各批输出互不覆盖。失败保留原件并回报，不自动重试，未运行部分须独立说明。运行时不修改source_paths、runner、导出器或官方文件。

这两条流水线获一次明确START后可在其分配范围内依次完成各批，不再为已授权批次重复请求窗口。前一批若失败则停下回报。主任务监视内存；不足时在下一批边界暂缓分发，不能修改已进行用例的预算或把超时当成功。P1/P3和OS可能共享CPU/内存，文件缓存未控制，批次及逐用例墙钟如实保留，但不作为独占机器效率比较。整轮吞吐另列；并行不能减少实际计算调用计数。

固定官方配置、E0与基线分母均不变。每个核数完整100图后报告逐图B/M算术平均；单算法全量均值与成绩台历史最佳混合均值分开。保留新方案退化、额外DDR与spill，不仅上传改进项。成绩从生成、核验、Git推送、队列接收、中央签名回执到本机镜像显示分别确认。

## Windows 账本中断与事前恢复修订

5C实际17:08:26.297280Z至17:08:34.117251Z运行，070成功；071 solver完成后，原runner替换账本时遇WinError5退出，071无E0，余28图未启动。旧文件全部保留，零E0重评。原因尚未唯一确认；运行中的只读Get-Content可能持有禁止删除共享标志，因此所有lane改为只从exec stdout获取活动进度，批次结束后才读ledger/run。

新 `round5c-recovery-parallel-spec.json` 事前固定剩余29图。071的旧solver端到端记录不完整，因此明确重新冷构造一次以得到完整可审计测量；新增预算是1次solver，整轮上限由430/430修订为431 solver / 430 E0。此决定是修复基础设施中断，不是算法参数搜索或隐去失败；不把原5C宣称零重试完成。070成功记录复用，官方E0无重复调用。A lane先执行已启动7A，再插入此29图恢复批，其后续7B/C与9A/B/C。B lane不受影响。各新批自身仍首失败停止、零自动重试。
