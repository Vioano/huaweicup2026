# P1：从 Task 门控到反链打包

本专项由 Fang 的独立 session `s-bdf7e1f4203b4ffda133950db055bd66` 执行。第一轮目标是建立很快的结构种子，再用官方结果决定是否值得接释放理想等局部改进。不是将 Pro 作者成绩当本机成绩，也不是已完成全100图求解器。

## 本轮实际采用的材料

资料锚点 `45f647b395b84e9569f418fd33d62c2b8eb4d190`：

| 原件 / 已读重点 | 对当前实现的影响 |
| --- | --- |
| `AI chats/20260924-Pro1-A题方法/附件/r06-RESEARCH_MEMO.md`，尤其 §4–9 | P2/P3 的资源词与固定归属细化不直接迁入P1；P1细Task会放大等待，受保护合并也可能退化。 |
| `AI chats/20260924-Pro4-算法方案设计/附件/r03-AUDIT.md`，§0–1、§8–12及可见勘误部分 | 按Task重建边界；同核不意味着共享驻留；入口/出口门控与backing可抵消省字节的收益。共同拓扑序用于结构守卫，不能代替E0。 |
| `AI chats/20260924-Pro4-算法方案设计/附件/m4ed03d4b-SYNTHESIS_REPORT.md`，§1–11及§12起始 | 当前数值相同不构成未来行为等价；释放理想不只限旧Step1前缀；机制构造可独立获益，无需先证明大行为商。 |
| 固定 `4dff90ef699fd51845cf482951e8477066f5f566` 的 `docs/a/Q1_RELEASE_IDEALS_REVIEW.md`、`prototype.py`、`structure.py`、`search.py` | 队长已有独立数学核，本专项先做不同的快速种子，避免争写和重复实现。对照fixed64包含官方局部编译，需计入其完整solver wall。 |
| 冻结官方 P1 builder、Task release、Step1及COPY收缩代码 | 核序联合图需无环；跨核真实依赖等待1000、同核前项等待100，均取max门控，配置不修改。 |

四路 README 已读以定位最新材料；没有宣称四个超长完整聊天及全部附件逐字读完。Pro中的运行/训练/Goal建议均未作为自动执行授权。

## 成绩台反馈起点

2026-09-24T13:54UTC 成员本机服务返回797条记录，P1历史239条；其中部分为缺原件的报告，且生产源尚未拉全。实际读到四核002、044的已核历史计划分别90582、116227 cycles；其余所选开发图大多只有未入榜报告。新fixed64全500格的交付入口6664a63已见，获取失败时没有补造数字或称已完成中央同步。

该快照用于选诊断对象，不用于在线选参。001有200个三操作弱分量；008有108个八操作弱分量；044有11个124操作弱分量；002/051各是单一弱连通图。010/026/094为不同尺寸的多分量图。它们涵盖独立包、汇聚依赖、混合Pipe和纯Vector等公开开发结构，不能称留出集。

这些观察支持两个待证伪假说：随机拓扑分块可能打断串行链、增加边界和门控；逐链单Task又可能过碎。介于两者之间的反链打包可以直接生成少量Task，但过粗也可能放大spill或延后出口。实际正负结果决定下一轮。

## 构造与结构证明

`component-pack`：在包含被排除COPY路径的计算依赖图上取弱连通分量，按计算工作降序分配到k个箱；每箱成为一个Task。分量之间没有计算路径，因此箱之间没有Task依赖。每核一个箱的计划在结构上无环。只有一个分量时自然退化为单Task，预计有明显并行性损失；本轮保留这种反例。

`chain-wave`：先收缩满足源点唯一后继、目标点唯一前驱的计算边，得到串行链包。按收缩DAG的最长路径边数定义深度，同一深度的包不可互达；每层按工作量均衡到k个箱，每个非空箱为一个Task，核序按层递增。收缩沿无分叉链进行，商图无环；不同箱间的真实依赖严格从低层到高层，全部新增核序边也严格增加层数，因此二者联合图无环。初始计算图的COPY中介依赖必须保留，不能仅删COPY端点。

每次分箱以累计Pipe工作量的最大值做排序，重复输入字节和异核前驱数只用于同分选择。它是启发式工作估计，不是官方Makespan、共享DDR模型或已证下界。没有在线评价、随机重启或枚举全计划。最终计划只含官方两个字段，并调用官方结构validator；完整执行成功仍以外部E0为准。

这一证明仅涉及结构：同层包的实际输入释放时刻仍可能不同，合并会把它们绑到最晚输入；同一Task中过多独立分支还可能增加容量压力。不能从无环推出无spill或质量改善。

## 复杂度与计时

设收缩后的计算DAG有n点/m边、p个包、h层、关联张量针数q，k≤5。并查集与包边扫描近线性；堆式拓扑与排序约为 O(m+n log n+p log p)。分箱对每个包扫描k核及其输入集合/前驱，总量约 O(k(q+m+p))，但必须另计原图到计算图的关系展开、被排除COPY路径遍历和官方validator本身。多生产者/消费者张量展开可能产生乘积边；不能把整个输入过程笼统称为线性。

本轮不调用官方局部编译器来构造新方案，fixed64对照会调用。二者计时都从冷Python子进程启动到读取原图、生成、校验、方案持久化和退出；单独E0在此后运行并另记。机器可能同时有其他用户任务，因此小样墙钟是当前负载下观测，不声称独占硬件、稳定P95或跨平台倍数。

## 验证记录

父会话于2026-09-24在Windows / Python3.12.14运行：

```text
python -X utf8 -m unittest discover -s tests/q1_yuanzhifang -v
```

5项测试通过，含64种四节点前向DAG × 两种构造 × 1–5核，共640个结构计划，由独立传递闭包再次检查依赖与全部核链无环；另检查COPY路径、输入edge顺序确定性、非法核数和空核不跳过联合环。是有限结构验证，0次E0/E1/E2，不外推执行或性能。

Stage A 的24格官方实测由子Agent以固定算法、独立runner/worktree执行；预算与停止条件见任务卡，结果另附。本轮Windows无 `dot_clean`，只对本人输出做AppleDouble/DS_Store只读扫描，不操作整盘或其他工作区。
