# 共享输入候选：完整结构族机制复评

1. **目标**：根据全部原图的结构识别结果，判断同一个共享输入构造是否有足够收益进入下一轮完整求解器实验；不事后只挑058等成功用例。
2. **输入**：冻结 `reuse-coverage-20260925/coordinates.json` 中全部33个有剩余调用且方案不同的坐标，覆盖10个已见图。固定官方配置和官方P3，旧对照为311算法forest500逐格记录。全部100图早已看过，不声称隐藏测试。
3. **输出**：`src.q3.reuse_family_probe` 保存33份确定性原方案、元数据、合法下界、官方结果/哈希/调用与外部墙钟，路径在 `results/a/q3-nikolastarx/reuse-family-probe-20260925/`。不写旧批次，不向成绩台伪报新统一全量成绩。
4. **预算**：最多33次进程内直接构造/静态界计算；058/5、079/5原件仅在原计划字节、图、配置与官方身份核同后复用，其余最多31次新P3 E0；下界不低于旧incumbent的候选跳过。0求解器、0P2、0retry、1worker、90秒/次、600秒全批。与同计划P2补证批次顺序执行，共享主机P2任务另占1worker。任何身份错误或评估失败停止。源码和本约定先提交后运行。
5. **验收**：生成计划与结构扫描保存的SHA逐格相同；官方P3身份、完整无损结果、调用账和前后源码哈希匹配。该过程比较旧固定算法与单候选，不等于重新运行新包装求解器。旧solver墙钟不挪作新计时。
6. **后续与停止条件**：以官方M为主，报告所有改善/持平/退化及extra/spill/字节hit差异。若只有个别小收益而多数无收益，优先修改理论构造，避免盲跑500格；若收益覆盖有意义，再冻结新统一入口全量验证，含在线构造/评分/回退的实际墙钟。仍不将代理最优当作P3最优证明。
