# 在线路径检查点：代码已准备，正式调用待单独安排

前一批17次正式调用已封存，不借用24上限余量。本阶段补齐协调者指出的缺口：
不能把事后选出的best方案配上单候选构造的28ms，当作在线求解器成绩。

`python -m src.q3.solve GRAPH --cores 4 -o PLAN --evidence NEW_DIR`
只从输入结构决定一种策略：守卫满足用resource_word，否则用固定affine_eighth。
不读case文件名、不读取旧成绩、不比较多个模拟分数；此规则受开发批次启发，尚无泛化证明。
随后**在线执行一次未修改P3 E0**，成功才发布两字段正式计划，同时保存完整结果和读取/策略回执。
失败会终止，不把失败当作合法性通过，不自动重试或暗中改变构造。
既有输出拒绝覆盖；发布使用同目录临时文件和原子hard-link，不暴露半写出的正式文件。

这一次E0是完整求解成本的一部分。之后测量必须由外层进程时钟覆盖解释器启动、imports、
读取输入/配置、分析和构造、E0、结果与计划写出、必要收尾、退出；内层计时仅作分段诊断。
完整评价会同时给出Makespan，但本路径不按这个数反复改候选。

## 有限检查提案

拟对008/044/080原开发图各执行一次在线路径，共3次新的正式P3调用，不重复外部评分：
把新输出plan与旧优选plan的SHA核对，再把新完整E0结果与旧同计划完整E0逐字节核对。
这验证的是完整调用路径/选择策略实现及计时，不是独立图泛化、独立验收或E2性能。
3次以外不执行，不动Q1/Q2预算；每进程30秒、1worker，外层批次120秒且至少留30秒收尾。
第一失败或计划身份改变即停止并分析，不重跑，先存调用预留与失败输出。
正式执行须另列固定源码及启动时间，当前仅为具体待评测代码，不将其标成已运行。

新增3组无E0单测：策略按结构选择、只有两字段计划、发布不覆盖已有文件且清理临时文件。
这些属于软件路径检查，既不消耗正式E0也不证明官方可执行性。现有8组结构测试可一并回归。

之后才讨论更广的图/核数覆盖、Cache输入亲和或改分核；这一小路径检查不自带那些实验授权。
