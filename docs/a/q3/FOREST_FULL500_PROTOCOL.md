# 统一森林前沿算法全量验证

新入口 `src.q3.forest_solve` 先执行完整fresh witness策略，再在总在线E0调用数尚未达到3时，尝试一个由原图决定的树前沿顺序。结构不支持/同计划/经安全下界证明不可能严格改善则不评；否则调用一次E0，仅真实Makespan严格更低时替换。单核也可适用；不依据文件名、历史分数或人工每格选择。计算代理不能直接录用候选。

本轮完整实验为100图×1–5核，一份固定solver提交和入口，固定配置。唯一数据执行者s59，自建独立工作区/输出目录。先冻结清单、代码/依赖/配置/输入哈希和控制器，再运行。500个fresh solver进程，至多1500个在线P3 E0，0retry；每格90秒，整批1800秒，至多4workers。4个内含pilot固定058/5、079/5、001/1、082/4，成功与预算账核验后展开；pilot不另外加调用。与其他任务评分窗口排队，不抢占在途运行。候选原件和失败/超时均保留，不越界补齐成绩。

报告单算法全量成绩、每格计划/官方原件、E0账本与端到端求解wall；不以旧c2获胜结果填格。185个测试184通过、1项需显式开启的E0复核跳过，非185次E0；随后提前结构守卫局部改动另过10个相关测试。前期两图机制实验是2直接构造+4外部E0，成本单独记，不混入本次500。

同plan无L2对照：本次计划产生后，用graph/config/官方源码/plan四项精确身份核对已有c2无L2原件；完全相同可引用原件并明确旧评估来源，不能称新测。不同计划才新增外部P2评估，上限500次、0retry，每次90秒，另列实际调用与墙钟；先完成主批、冻结实际变化清单与独立manifest，确认资源窗口，再评这些计划。这是外部控制对照，不参与本求解器在线择优，也不能用不同计划/核数替配。

成绩台投稿按同run/algorithm/source及每格原件绑定，经既有正式协议接收；可以分片保存与发布已验证结果，但部分覆盖不称500/500。源码、实验、同步、中央accept和两端网页显示分别记录，不互相代签。
