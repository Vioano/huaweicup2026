# A 题第一轮：规则、对抗样本与双层高速评估器

2026-09-23，队长 NikolaStarx 已授权派发。正式任务 ID：

| 主负责人 | Task ID | 范围 |
| --- | --- | --- |
| farmeruncle123 | a-r1-form-adversarial | 形式化、语义对抗生成器、反例分析和缩减 |
| lyx0217 | a-r1-fast-eval | E1 热点等效加速与 E2 极速近似两条路线 |
| NikolaStarx | a-r1-oracle-test | 公共接口、E0 适配、独立基线/差分、封存集及验收 |

yuanzhifang30-sudo 本轮不分配任务。三条工作线同步推进，不等待完整规范或最优切图算法。

## 阅读顺序与效力

1. 本文及本人 `tasks/a/` 六字段任务卡：本轮人员、首批范围和交接约定。
2. [契约 v1](contract-v1.md)：FORM、EXACT、PROXY、TEST 的完整目标及接口含义。
   另读 [2026-09-23 补充规范](EVALUATOR_AMENDMENT_20260923.md)：官方格式优先、E2 更低数值误差、C++/Rust 与算法并行探索；明确覆盖的条款以补充规范为准，导入原件不改。
3. [导入审计](READ_AUDIT.imported.md)：上一份材料报告的源码观察和六次烟测，供独立核对。
4. 题面及本轮冻结的官方源码。发生冲突时记录题面要求与代码行为，不私自调和。

本轮新增：FORM 同时负责语义对抗样本算法；规则卡、生成器和最小反例成组交付。队长独立维护 E0 判据、封存验收集和最终验收。实现者可提改进建议，不自行放宽门槛或修改公共真值。

契约中的 3 倍、10 倍、50 倍和质量百分比是研发/发布目标，不是已有实测结果，也不要求第一批探索就全部达到。首轮验收按任务卡的有限范围，如实报告不足。

## 共同输入已经发布

- `data/raw/a/problem.pdf`：用户提供的题面原始字节。
- `data/raw/a/official/`：附件 README、十个 Python 源码、配置及两份算法说明，保持原始字节。
- `data/raw/a/official-cases.zip`：100 个原始 case 的传输压缩包，解压后原始字节不变。此 ZIP 是本次为团队生成的包，不是题目原下载 ZIP。
- `docs/a/source-manifest.json`：本轮文件 SHA-256、PDF/压缩包身份、可复算 code hash。原下载日期未知；未核对原下载 ZIP 的 SHA。

从任务工作区根目录运行：

```sh
python scripts/a_materials.py --extract
uv sync --locked
```

前一条检查全部来源字节并解压 case，遇到被修改的现有原件会拒绝覆盖。解压后的 `data/raw/a/official/data/case_*.json` 不再重复提交。原始目录只读；所有运行输出用任务自己的独立目录，并明确覆盖官方 CLI 默认结果路径，禁止在官方目录里生成或覆盖结果。

## 当前材料缺口

导入契约引用的 `TASKS.json`、`contracts/*.schema.json`、`audit/official_source_manifest.json`、`verify_smoke.py`、`smoke_results.json` 和六份 golden 没有随原 Markdown 提供。本轮 source-manifest 是新生成的来源清单，不能假称就是原审计清单；六次烟测仅有导入材料的报告，尚未在本轮复现。

队长另行指定的实现/复核会话负责 E0 适配与独立验收；当前监控会话只负责协调路由。成员可立即读源码、开发规则/生成器、直接调用原版做 profiling。公开接口沿用官方文件/CLI；`team_eval` 只作为待交付的队内适配选项，不构成新输入格式前提或等待条件。不各自占用同一公共包入口或发明互不兼容的公开字段。

## 交付与验收

- 各自从本次任务包提交建 `codex/` 分支/独立 worktree，不覆盖已有工作；代码/文档经 PR 汇合。本次任务包 PR 未合并也可按精确提交检出开始。
- FORM：`formal/`、`src/adversarial/`、`tests/adversarial/`；E1：`src/eval_exact/`；E2：`src/eval_proxy/`。目录是修改边界，具体语言和内部结构由成员决定。
- 队长维护 `contracts/`、`src/team_eval/`、独立通用生成器和比较器、封存验收数据。跨界修改先以接口变更建议交接；共享依赖文件通过小 PR 协调。
- 对抗样本由 FORM 设计，预期由 E0/手算证据核验。开发反例可用于修复；封存集不交优化器日常调参。超时/崩溃单列，不冒称非法。
- 每份交付包含 commit、输入/配置/代码哈希、命令、环境/种子、结果、失败、范围与下一步，以及对应可用于论文的方法/局限段落。数据图附绘图输入，不用示意数字冒充结果。
- 建议成员实际接手后约 1 小时交首批规则/热点，约 3 小时交首轮可复跑产物；这是协调目标，不是用户已设定的硬截止。首次实质回复写接手时刻和预计交付时刻（Asia/Taipei）。已有明确时间约束则直接报告。

## 通信与任务状态

一人一个任务 Issue，通知和需要对方行动的交接在 Issue；任务卡保存约定；PR 保存产物；Atlas 保存已接受的任务状态。不要复制流水账或空回复。

成员按本人授权开始工作后更新 `doing`；影响交接的阻塞写 `blocked`；交付时一次请求提交 `review` 和 PR 引用；队长独立验收后置 `done`。指派不等于成员已读取、执行或验收。

Atlas 接入见 [ATLAS.md](ATLAS.md)。接入暂时受阻可先按任务卡执行已经获得本人授权的工作，在 Issue 记录一次明确阻塞；不能把工具接入变成等待整个第一轮的前置条件。
