# 菊花cup - A题方法

> Conversation URL: https://chatgpt.com/g/g-p-6ab2d820c86081918067a0c6d5eb1ab6/c/6ab2d851-0348-83e8-ab24-02db57b01aea
> Export time: 2026-09-23 13:32:17

---

**Q：**

你能看到这个 project 的另一篇 chat 吗？里面讲了数学建模比赛的选题策略，以及面对 A 题这种题的方法论。

**A：**

能。我能看到并检索到这个 Project 里那篇关于**数学建模比赛选题策略**的聊天，应该就是《选择建模题目策略》那一篇，而且其中关于 **A 题方法论**的核心上下文也能接上。

我现在掌握的主要内容包括：你的选题目标不是“挑最容易做的题”，而是寻找**对普通队伍门槛高、但这个门槛能被你们的 AI + 分布式 Agent + 工程能力大幅压缩的题**；同时第一目标是**进入数模之星答辩**，不能为了后续答辩好讲而牺牲初赛入围概率。比较候选题时，会看本队相对能力、头部竞争强度、可评审/可验证性、冲高质量成果的上限、最终交付与集成风险等维度。

A 题那边我也能接上。核心不是“一上来挑一个高级优化算法”，而是把题目先变成一个**严格定义、可验证的工程优化系统**：

> 先形式化题意和所有硬约束 → 建立独立可信的 evaluator → 用小规模问题乃至穷举校验 evaluator → 做合法 baseline → 再上启发式/优化算法 → 所有性能提升重新经过 evaluator 验证。

针对那道“通用神经网络处理器核内调度”的 A 题，我们当时实际上已经把它看成一个带有**拓扑依赖、缓冲区生命周期、内存/地址复用、执行周期等约束的调度问题**，并讨论过 critical path、buffer lifetime / release、unlock score 一类问题特定的调度优先级设计，而不是直接套遗传算法、模拟退火之类的通用黑箱算法。

还有一个很重要的共识是：**“方案 → 可信方案 → 有竞争力方案”**。先保证模型、代码、约束、数据、结果和论文叙述完全对齐，再追求性能上限，尤其防止 AI 在高速并行探索时出现“做出了漂亮数字，但其实违反约束/评价器有 bug/比较基线不合法”的情况。

所以你接下来可以直接基于那篇 chat 继续说，不需要重新把整个背景解释一遍。

可以继续从这几个方向展开

-
-

---

**Q：**

总共有四台常驻电脑，三台游戏本，一台 MacBook Pro。我之前跟你讲过关于资源的问题， AI token 的资源。给我构思这道题的方案，目标是数模之星。

**A：**

我建议把这道题的主线定为：

> **面向官方核内调度器的、考虑张量复用与存储生命周期的多核联合切分—分配—排序。**

你们的资源优势，不应该主要体现为“让四台电脑不停跑参数”，而应该体现为：**同时探索几种真正不同的问题结构，用统一实验迅速区分哪些想法成立，再把有效机制收敛到一个几分钟内能够独立运行的求解器。**

按你前面提供的资源，我会以 **Codex 20×账号、CodeBuddy 的大量积分、三台游戏本和一台 MacBook Pro，以及你已经打通的多 Agent 协作协议**为基础设计。不假定账号额度无限，也不把四台机器误当成四份独立的模型额度。

先说明当前判断的依据：**题面已经足够确定研究架构，但我还没有看到官方 `code/`、`data/`、`docs/` 材料包。**所以下面会明确区分题面规定、能够推导的结构，以及需要实验验证的研究假设，不会把尚未运行的构想说成已经有效的算法。

---

## 一、先校正研究对象：你们不是在自由设计一个 NPU 调度器

这一点决定后面几乎所有工作是否值得做。

你们实际提交的是两个对象：每个非 COPY 操作属于哪个子图，以及每个核心执行哪些子图、按什么顺序执行。**Task 内部的拓扑排序、缓存换入换出、多 Pipe 排布，由官方程序完成。**最终 Makespan 又由共享带宽的多核模拟器统一计算，不能拿自己设计的理想调度时间替代。

因此，前面“先建立可信 evaluator”的方法论，在这份题上要落实为：

**封存并读透官方 evaluator，增加独立合法性检查、微型测试与诊断工具，而不是另写一套评分规则。**

### 三个问题，不能只改一个“通信系数”

题面第 5 页图 3 展示的区别非常关键：场景 A 的同核子图之间也需要 DDR 中转；场景 B 才允许同核子图保留核内数据。问题 2、3 进一步明确，同一核心的所有子图合并为一个 Task。

| 问题 | 实际通信与同步规则 | 我会优先研究的决策 |
| --- | --- | --- |
| 问题 1：场景 A | 每个子图一个 Task；跨子图都经 DDR；同核前一 Task 完成后等待 100 cycles，跨核前驱 Task 完成后等待 1000 cycles | **子图粒度**：何处值得切开，何处必须融合 |
| 问题 2：场景 B | 每核一个合并 Task；同核可复用；跨核 COPY_IN 等待源 COPY_OUT 完成及 500 cycles 同步延迟 | **数据归属与生命周期**：哪些操作放在同核、如何安排消费顺序 |
| 问题 3：场景 B 加 L2 | 沿用问题 2，增加共享只读 FIFO Cache；命中不刷新 FIFO 顺序 | **共享输入的到达时序**：如何让复用真正发生，而不是仅在图上“存在共享” |

这些同步规则的准确含义见附录 D，尤其问题 2、3 等待的是对应数据的 COPY_OUT，而不是等待源核心整个 Task 全部完成。把它们错误地建模成“整个核心任务完成后另一个核心才能启动”，会丢掉题目允许的并行空间。

另外，题目明确希望避免暴力迭代搜索，单个用例以 5～10 分钟内得到结果为佳。**四机资源可以支持研发阶段的大量对照实验，但最终提交的算法不能靠长时间穷搜成立。**

---

## 二、统一建模：外层优化方案，内层接受官方“编译与执行”

我会把切图、分核、排序分别记为 P、a、π，把场景记为 σ。主问题写为：

$$
(P^\star,a^\star,\pi^\star)
\in
\underset{(P,a,\pi)\in\mathcal F(G,N,\sigma)}
{\arg\min}
\;
T_{\mathrm{official}}(G,P,a,\pi;N,\sigma).
\tag{1}
$$

这里，可行域包含节点覆盖、子图依赖无环、调度顺序合法，以及经过官方展开后能够满足存储与执行约束。

**额外搬运量和 Cache 命中率保留为辅助指标，不自行发明一个综合分数替代 Makespan。**题目将 Makespan 列为最重要指标，另外两项为次要指标。

式（1）不是让我们直接把官方程序当黑箱乱搜，而是强调：**所有近似模型的作用都是指导决策，最终效果必须回到官方执行语义。**

### 第一层抽象：用“通信域”统一三问

定义一个操作所在的数据复用域：

$$
d_\sigma(o)=
\begin{cases}
P(o), & \sigma=\text{场景 A},\\[2mm]
a(P(o)), & \sigma=\text{场景 B 或场景 B 加 L2}.
\end{cases}
\tag{2}
$$

它表达了一个简单但重要的区别：

在问题 1 中，跨子图就跨越了核内复用边界；在问题 2、3 中，同核的不同子图仍可能属于同一个复用域。

于是，三问可以共享一套张量分析、结构识别和优化框架，而不是分别写三套互不相干的算法。

### 第二层抽象：通信对象应当是张量，而不只是操作之间的边

假设一份输入被十个操作共同使用。

把这些操作分到几个 Task、每个 Task 如何读取，比“总共切断了多少条操作依赖边”更接近实际通信代价。题面本身也强调，共享输入被分散到多个子图或核心，会产生重复读取。

因此，我建议内部同时维护两种图：

**操作依赖 DAG**负责拓扑、关键路径和合法性；**以张量连接生产者与消费者的有向超图**负责复用与通信分析。

这不是为了给普通图算法换一个高级名字。有向无环超图划分已有对应研究，研究对象就包含数据流、多生产者或消费者关系，以及划分后商图仍无环的约束，可以直接借鉴其建模与粗化方法。[![](https://www.google.com/s2/favicons?domain=https://arxiv.org&sz=128)arXiv+1](https://arxiv.org/abs/2002.02962?utm_source=chatgpt.com)

但文献中的通信目标不能直接当作本题评分函数。**同一张量的边界写回、目标 Task 读取如何去重，必须再用官方源码核实。**

---

## 三、最值得押注的三个研究假设

这三条是我认为最有希望形成“数模之星级研究主线”的地方。不是说它们已经被证明有效，而是它们分别对准了题目的真实机制，并且都能设计明确的反证实验。

### 假设一：真正决定切分质量的是“张量复用收益与并行损失”，不是普通割边权重

对于一组强关联操作，合并可能减少边界搬运、重复读取和 Task 开销，但也可能减少并行度、延长数据驻留、增加换入换出。

题目甚至明确提醒：子图过大时，固定启发式核内调度器可能产生大量换入换出，合并后的执行时间可能反而更差。

因此，主算法不应该采用“通信越强越合并”的单一规则，而应该考虑：

**这次合并减少了什么搬运，释放或占用了什么并行资源，又让哪些张量活得更久。**

我会做一个最小实验：选取一类共享输入明显的子结构，逐步增加融合规模，测量 Makespan、边界搬运、缓存换入换出、Cube/Vector 利用情况。

值得寻找的不是一条永远下降的曲线，而是：

> **融合规模增大后，先因减少通信而变好，再因并行度或缓存压力而变差。这个转折能否由可观测特征预测？**

若能预测转折，算法就获得了“自适应选择子图粒度”的依据，而不只是调出一个固定子图大小。

### 假设二：问题 2 的关键不只是“分到同核”，而是“让数据在合适的时间被消费”

场景 B 中，数据可以留在核内，但驻留不是免费的：它持续占用 L1 或 UB，可能挤走其他数据。官方缓存策略又基于固定序列和下一次使用位置决定换出对象。

因此，两个方案即使有相同的核心分配，也可能因为子图组织与顺序不同，产生不同的核内顺序和换入换出。

**这里有一个很值得深挖的控制手段：通过可提交的子图划分和顺序，影响固定调度器的行为。**题面明确说，问题 2、3 的合并 Task 会在不破坏依赖的前提下，按照 `core_schedules` 调整子图间顺序。

具体可以探索“生产—集中消费—尽早释放”的顺序设计，而不是只让两个通信量大的操作同核。

这与红蓝卵石游戏所研究的“有限快速存储与数据搬运之间的权衡”存在直接联系。但本题的换入换出规则是固定的，不能把允许自由调度、自由重算的理论模型直接搬过来。理论应帮助我们识别生命周期和通信下界，而不是改变题目允许的动作。[Erik Demaine](https://erikdemaine.org/papers/PebblesRedBlue_SPAA2018/)

对应的关键实验是：

**固定核心分配，只改变子图组织和合法顺序。**

这样才能分清性能提升究竟来自分核，还是来自对官方核内调度器的有效引导。

### 假设三：问题 3 中，“同时读取同一输入”可能反而破坏预期的 L2 收益

这一条尤其值得尽早验证。

题面规定：COPY_IN **发射时**查询 Cache；未命中则读 DDR，**完成后**才插入 Cache。命中不改变 FIFO 顺序。

由此可以提出一个待检验的推论：

**多个核心在第一次读取完成之前同时请求同一张量，可能都发生 miss，而不是自动共享一次 DDR 读取。**是否存在同张量在途请求合并，需要查源码，不能自行假定。

如果这个现象成立，就可以研究“先完成一次填充，再安排其他相关读取”的合法顺序设计。但这里有两个限制：

第一，输出接口没有“任意插入等待”“新增预取操作”之类的控制项，必须通过允许的子图安排影响发射顺序。第二，不同 Pipe 可以并行，不能认为在前面放一段计算，就一定推迟后面的读取；必须用 Trace 确认实际效果。

我会把它设计成一个对照实验：

**同一组共享输入、同一核心分配，比较最大并行启动与复用感知排序。**同时改变共享工作集和复用间隔，观察收益是否因 FIFO 淘汰消失。

这一方向的价值在于，它研究的是**调度与 Cache 的相互作用**，而不只是“加了 Cache 所以更快”。

---

## 四、主算法：多层次构造，瓶颈驱动修正，少量官方评估

我不建议最终交付“遗传算法、粒子群、模拟退火各跑一遍取最好”的大拼盘。

我会采用下面这条主流程：

> **结构分析 → 多种有物理依据的初始方案 → 无环约束下联合切分与分核 → 存储与顺序优化 → 少量官方评估 → 针对瓶颈修正。**

### 1. 先生成“图的结构画像”

第一轮拿到正式数据后，要统计的不只是节点数，而是：

操作关键路径、可并行分支、Cube/Vector 工作量分布、共享输入消费者集合、张量大小分布，以及若干合法顺序下的活跃张量规模。

同时检查有没有重复计算块、长链、分叉—汇合结构、窄接口连接的大块区域。

**现在还不能断言正式数据属于哪一种结构。**真正的算法资源分配应由这些统计决定，而不是凭题目名称想象数据分布。

### 2. 保留少量机制不同的初始方案

我会至少实现三类候选：偏关键路径与并行度、偏张量复用与低通信、偏缓存安全与短生命周期。

关键路径优先、最早完成时间选择，可以参考 HEFT 一类列表调度思想，作为基础构造方法；但本题有多 Pipe、固定核内展开和动态共享带宽，必须重新设计成本估计。[IEEE Xplore](https://ieeexplore.ieee.org/document/993206/?utm_source=chatgpt.com)

每一类候选都要是真正不同的结构选择，而不是同一算法换十个随机种子。

### 3. 粗化不能破坏无环性

这里有一个非常容易出错的地方：

**原图是 DAG，不意味着任意分组后的子图依赖图还是 DAG。**

例如，一个三节点链，把第一个和第三个放到同一子图，中间节点放到另一个子图，就会得到两个子图相互依赖。题目明确禁止这种划分。

因此，初版可以采用较保守的合法构造：从全局拓扑序的连续片段开始；后续再加入更灵活的无环合并和边界移动。

**尤其不能在问题 2 中简单把“核心编号”当作“子图编号”。**同一核心可以承接多个分散的子图；这些子图在评估阶段合为 Task，不等于提交阶段可以随意合成一个可能产生环的子图。

### 4. 分核要考虑资源向量，不只看总 cycles

一个核心上的 Cube 和 Vector 可以并行，四条 Pipe 则各自串行。用所有操作 cycles 的总和代表核心负载，会忽略这种结构。

所以，给子图安排核心时，至少同时估计矩阵计算负载、向量计算负载、搬入搬出需求、依赖到达时间和核内存储压力。

我们真正想获得的是：

**不同资源的工作相互重叠，强复用数据尽量留在合适的位置，关键操作不要被无关工作拖延。**

不要求机械地把所有核心“填满”。输出允许某些核心的子图列表为空；是否多用一个核心，要看其并行收益能否覆盖新增搬运和同步成本。

### 5. 局部修正必须对应可解释的瓶颈

我会优先实现几种明确的修改：合并高代价边界、拆开导致严重换入换出的区域、移动一组共享输入的消费者、交换合法的子图顺序，以及重排影响关键数据到达的工作。

不要无差别随机修改整张图。

特别要避免一种错误直觉：**DDR 搬运越并行，性能就一定越差，因此应该全部错峰。**官方模拟器是共享总带宽，并发变化会重新分配带宽。减少并发本身不会凭空减少总搬运工作量；只有改善关键计算的启动、计算通信重叠或 Cache 命中，才可能缩短 Makespan。

### 6. 官方评估次数由时间预算决定

候选先经过静态成本、局部生命周期分析等便宜检查，只把少量有希望的方案交给官方 evaluator。

先测一次完整评估需要多长时间，再决定每个实例可以承担多少次校准。**如果一次官方评估已经很慢，最终求解器就必须更多依赖开发阶段学到的构造规则，而不是把大量评估塞进线上求解过程。**

这里要严格区分：

**研发时可以四机并行发现好规则；提交时，所有内部评估和搜索都必须计入实际求解成本。**

---

## 五、理论深度：重点做下界、结构特例和可证伪机制

你前面强调的“寻找数学结构，而不只是现学一堆算法”，在这道题上是有落点的。

但我不会把时间主要花在写一个覆盖所有细节、最终求不动的巨大整数规划上。

### 一个应尽早建立的基础下界

设纯计算关键路径长度为 Lcomp，全部矩阵操作与向量操作的周期总量分别为 WM、WV，必须经过 DDR 的数据量下界为 Dmin。在相应必需流量假设核实后，可以建立：

$$
T^\star
\ge
LB
=
\max\left\{
L_{\mathrm{comp}},
\frac{W_M}{N},
\frac{W_V}{N},
\frac{D_{\min}}{B_{\mathrm{DDR}}}
\right\}.
\tag{3}
$$

理由分别是：依赖链无法并行消除，全部矩阵或向量工作分别受到核心数量限制，必需 DDR 流量受到总带宽限制。本题 DDR 总带宽固定为 60 bytes/cycle。

这里要非常谨慎：**不能把当前方案实际发生的全部搬运量，直接当作所有方案都必须付出的通信下界。**当前方案的重复读取和换入换出，恰恰可能是可优化的。

对于一个已验证的可行解，下界可以给出：

$$
0
\le
\frac{T_{\mathrm{alg}}-T^\star}{T^\star}
\le
\frac{T_{\mathrm{alg}}}{LB}-1.
\tag{4}
$$

右侧是相对最优性差距的上界，不是“测出了真实最优差距”。下界很松时，它也可能没有多少判别力。

### 精确方法主要放在两个位置

**极小图的完整验证。**对足够小的图枚举合法切图、分核和顺序，调用官方评估器；只有确实覆盖完整可行空间，才称其为该小图的最优解。

**识别出来的结构特例。**例如窄接口连接的重复块，可能适合动态规划或局部精确重排。能否成立取决于结构与状态规模，不预先承诺。

这里必须避免一个常见问题：用 CP-SAT 求解一个忽略共享带宽、固定 COPY 耗时的简化模型，然后把结果称为本题的全局最优。**那只能是简化模型的最优。**

我希望理论线最终至少回答一个实际问题：

> 什么结构下应优先融合，什么结构下应优先并行，什么结构下性能已经接近资源下界？

这比单纯证明一个宽泛的复杂性结论，更能支撑你们的算法。

---

## 六、四台机器与 AI 资源：按研究功能分工，不按三道小问割裂

### 机器的初始安排

| 机器 | 初始职责 | 后期如何转用 |
| --- | --- | --- |
| MacBook Pro | 你使用的主控端：模型、接口、任务图、代码审查、结果汇总 | 集成与论文论证；空闲时执行轻量实验 |
| 游戏本 1 | 官方程序封存、语义测试、基线、独立复现 | 冻结环境，重跑最终全部结果 |
| 游戏本 2 | 主算法：超图分析、无环粗化、联合分核排序 | 主要性能实验与消融 |
| 游戏本 3 | 挑战路线、结构特例、L2 时序实验 | 压力测试、失败案例、参数稳健性 |

这是初始分工，不是永久绑定。某条路线结束后，机器直接进入共享实验队列。

**不建议一台负责问题 1、一台负责问题 2、一台负责问题 3，各自做完整工程。**三问共享的数据结构、评估流程和合法性约束太多，这种拆法容易产生三套不一致的模型。

从题面提供的 Python 图处理、核内展开和离散事件模拟流程看，我会先测 CPU、内存和单次评估耗时，再判断是否需要额外算力，而不是先租 GPU。具体瓶颈仍需拿到代码后 profiling。

### Agent 的任务应当围绕“问题”，而不是围绕“论文标题”

你可以同时开放四条研究线，但只保留一个共享工程：

| 研究线 | 要回答的问题 | 有价值的交付物 |
| --- | --- | --- |
| 主线 | 张量感知的联合切分分核，能否稳定优于强基线？ | 可运行算法、同预算对照、失败实例 |
| 挑战线 | 更直接的就绪节点/子图构造，是否比多层次划分更好？ | 机制不同的竞争方案 |
| 理论线 | 是否存在可利用的重复结构、窄边界或紧下界？ | 结构判定、证明或局部精确算法 |
| 调度器与 L2 线 | 子图组织如何改变固定核内顺序与 Cache 行为？ | 最小实验、源码语义、可复用改进规则 |

**每条研究线不能只交一篇“可能有效”的长文。**必须交最小可运行实验、结果、反例和下一步判断。

### Token 应花在高影响的不确定性上

强模型主要负责：读透评价语义、研究抽象是否正确、提出机制不同的假设、分析失败原因，以及审查算法与论文主张。

便宜模型主要负责：明确接口后的实现、批量测试、数据统计、日志整理、复现实验和初稿加工。

我会**预留约四分之一的强模型可用预算给后半程**。后半程最需要高质量判断的，往往不是“再发明一个算法”，而是定位一次异常提升、处理跨模块不一致、决定哪些主张必须删掉。

最终提交的求解器应由固定代码运行，**不依赖比赛结束后仍然能够访问某个 LLM 服务**。

### 你的 Graph Engineering 在这里要管理“证据依赖”

你现在的任务图可以进一步变成：

**题面规则—源码语义—假设—算法版本—实验—结论—论文图表。**

一条论文结论必须能追溯到具体实验，一次实验必须能追溯到具体代码、输入和官方配置。

对你来说，最重要的不是盯着每个 Agent 的聊天，而是只接收一种压缩后的交付：

> 改了什么；为什么可能有效；在哪些实例有效；在哪些实例失败；是否值得继续投入。

这样才能把大量 token 转化为证据，而不是转化为大量待你阅读的文字。

---

## 七、实验设计必须能支撑“为什么有效”，不只是“结果更好”

### 先建立强而公平的基线

至少要有关键路径列表调度、通信感知的融合调度，以及一个考虑存储或资源负载的加强版基线。

题目提供的随机生成脚本明确只是接口演示，不是性能基线。**战胜它不能证明算法有竞争力。**

所有算法在同一配置、同一运行预算下比较。不能自己的方法选四台机器跑一晚上的最好值，基线只运行一次。

### 三组关键消融

我会围绕前面的假设设计：去掉张量共享信息，只用普通边代价；去掉生命周期和换入换出感知；去掉问题 3 的复用时序设计。

但每次消融都要重新生成对应方案，保持比较预算一致，而不是只在论文里把某个系数设为零，其他流程仍隐含使用被移除的信息。

### 问题 3 做一个交叉对照

分别得到“无 L2 优化方案”和“考虑 L2 的优化方案”，再交叉评估：

| 调度方案 | 无 L2 | 有 L2 |
| --- | --- | --- |
| 无 L2 优化得到的方案 | 基准结果 | 固定方案下的硬件收益 |
| 考虑 L2 优化得到的方案 | 检查是否只是一般调度改善 | 硬件与新调度共同作用的结果 |

这样能够区分“硬件变强了”和“算法确实利用了新硬件”。

这不意味着所有格子都会更好；**有解释力的失败结果也应保留。**

### 统计口径必须从第一天统一

题目要求的平均加速比，是先计算每个用例的加速比，再进行算术平均：

$$
\overline S_\sigma(N)
=
\frac{1}{M}
\sum_{i=1}^{M}
\frac{T^{\mathrm{fixed\ single}}_i}
{T_{i,\sigma,N}}.
\tag{5}
$$

不能用“全部单核时间之和除以全部多核时间之和”替代。单核基准也必须采用题目规定的固定启发式结果；题目明确允许因基准启发式非最优而出现超过核数的加速比。

因此，超过核数的加速比不能自动当作错误，也不能被包装成突破并行计算理论限制的发现。

### 固定硬件，改变工作负载，研究适用区间

官方算法评估必须使用规定配置，不得通过改配置制造更好结果。

补充机制实验可以在固定硬件下，改变合成图的共享工作集、计算与搬运比例、分支宽度、张量大小和复用间隔。这样能观察算法在何处由计算受限转为带宽受限，或由复用收益转为缓存压力。

最终我希望看到的，不只是平均加速比，还有**逐用例表现、求解时间、退化案例，以及少量能解释机制的执行时间线**。官方已经输出可供 Perfetto 查看、分析的 Trace，可以直接利用，不必先做一个新的可视化平台。

---

## 八、实施节奏：早期并行探索，中期收敛，最后保住完整交付

在不知道确切剩余时长的情况下，我会按以下比例安排，而不是假定一定有完整的某个比赛周期：

| 剩余时间占比 | 核心任务 | 进入下一阶段的条件 |
| --- | --- | --- |
| 前 15% | 官方语义、数据画像、统一接口、强基线 | 三问都有可运行、可评估的完整路径 |
| 15%～40% | 主线、挑战线、理论线并行探测 | 每条路线至少有一个支持或反驳假设的实验 |
| 40%～65% | 选择主线，整合已证实的机制 | 不再增加没有明确收益的新框架 |
| 65%～85% | 全量实验、消融、失败分析、论文 | 主要结论都有冻结实验支撑 |
| 最后 15% | 独立重跑、附件检查、文字与数字对齐 | 不再做大规模重构 |

这里的“收敛”不是过早放弃深度探索，而是要求每条深度路线给出继续投入的证据。

例如，理论线发现某类结构能精确求解，就接入对应模块；如果正式数据没有这种结构，就把结论留作解释，不让整个项目等待它。L2 路线若只对少数结构有效，也可以变成一个条件触发的策略，不必强行覆盖全部用例。

---

## 九、最后的论文，应当压缩为三个能守住的主张

我会争取让最终成果围绕以下三个主张展开，但是否写进摘要，取决于实验是否真的支持。

**第一，张量级复用与无环约束的联合建模，比只考虑操作依赖边或负载均衡更贴近本题实际代价。**

**第二，在固定核内调度器下，子图粒度与数据生命周期必须联合优化；场景 B 的同核复用并非无条件收益。**

**第三，L2 的收益依赖实际访问时序，调度算法需要同时考虑首次填充、FIFO 淘汰和并行机会。**

对应的证据分别是强基线对照、固定分核的顺序消融、L2 交叉实验，以及小规模验证和资源下界。

**这才是我会为“数模之星”目标选择的路线：不是四台机器跑出一堆更漂亮的数字，而是用四台机器和大量 AI 资源，找到少数真正成立的结构，再把结构变成性能、解释和可复现的算法。**目前还没有正式数据实验，不能据此判断最终排名；但这套方案把投入集中在可验证的算法竞争力上，也符合你“先保证入围竞争力，不为展示性牺牲实际成果”的目标。

下一步最关键的是把题目的完整附件材料包发来，尤其是 `code/`、`data/` 和 `docs/`：**第一轮应该产出官方语义核对表、正式用例结构画像和强基线成绩，用这三样决定上述哪条假设值得成为主攻方向。**

![](https://www.google.com/s2/favicons?domain=https://arxiv.org&sz=128)
> Image：A · 128x128

---

**Q：**

以上是附件中的文档和 data json 文件示例，给你发了一个。

![c1abdc39-be9e-4d61-9649-8ad9c7ed85d4.png](https://chatgpt.com/backend-api/estuary/content?id=file_0000000048e8822f85f2a2d2e3cab21d&ts=497261&p=fs&cid=1&sig=34bbb6c73494323008a47039850747946647f5614445c31960e2f6dba7344558&v=0)
> Image：Q · 1054x740 c1abdc39-be9e-4d61-9649-8ad9c7ed85d4.png

![ca079767-fb7c-4f1d-b4a2-5349fa439d8a.png](https://chatgpt.com/backend-api/estuary/content?id=file_00000000e3f8822f8b358a639d3ad7a2&ts=497261&p=fs&cid=1&sig=ee93f6f3877cf0655b96e0c062db88be2ec0eb1d0be2e0208b497eb6d0715c95&v=0)
> Image：Q · 646x1088 ca079767-fb7c-4f1d-b4a2-5349fa439d8a.png

![9dfc4629-6aa5-4aac-8045-71b0c23ff98b.png](https://chatgpt.com/backend-api/estuary/content?id=file_000000001590822faecb67047acc9175&ts=497261&p=fs&cid=1&sig=d821d47cc1fafde24a34b6dfc5ae7446dc7ab4bf801b901bcc6d23b85a053029&v=0)
> Image：Q · 586x1028 9dfc4629-6aa5-4aac-8045-71b0c23ff98b.png

**A：**

**这个样例已经足够让上一轮方案往前推进，而且需要调整研究重心。**

我已经完整解析了这个 JSON，做了图结构、计算负载、共享输入和串行张量生命周期的统计。最重要的发现是：

> **这个样例不是一张缺乏结构的大图，而是包含大量重复计算分支、共享输入，以及通过极小张量连接的归约—广播结构。应当先利用这些结构降低优化难度，再研究如何通过子图组织影响官方调度器。**

上一轮提出的张量感知建模仍然成立，但**不能预先把“大规模缓存换入换出”或者“L2 容量不足”当作这个样例的主要矛盾**。下面是实际分析结果，以及它怎样改变解题方案。

目前仍没有收到截图中那些 `.py` 文件的源码，所以这里的数值是**静态统计或明确说明的推导，不是官方评估成绩**。

---

## 一、真实数据画像：667 个待分配操作，但结构比节点数量更有价值

以下是对你上传的**这一个样例**的完整程序统计，不能据此推断全部用例都具有相同分布。

| 项目 | 统计结果 |
| --- | --- |
| 操作节点总数 | 796 |
| 需要我们分配子图的非 COPY 操作 | **667** |
| 张量节点 | 889 |
| 原始 Op–Tensor 二部图边数 | 1,899 |
| MATMUL 操作 | 131 |
| Vector 管道计算操作 | 536 |
| COPY_IN / COPY_OUT | 93 / 36 |
| Cube 管道计算工作量 | **44,610 cycles** |
| Vector 管道计算工作量 | **55,496 cycles** |
| 原始 DDR 输入体积，按不同输入张量去重 | **384,078 bytes** |
| 原始 DDR 最终输出体积 | 72 bytes |
| 最大单个张量 | 16,384 bytes |

完整统计和可复现脚本已放在文末的审计包中。

这里首先要防住一个实现错误：**样例里所有 COPY 操作的 `cycles` 都是 0，但搬运绝不是零耗时。**例如文件开头几个 COPY_IN 的确如此；题面明确要求 DDR 搬运根据张量大小和共享带宽计算，不能直接累加 JSON 的 `cycles` 得到总时间。

### 两种“连通分量”，揭示了两层结构

我统计得到：

**完整 Op–Tensor 图有 16 个弱连通分量。**也就是忽略边的方向后，原图可以分成 16 个彼此不连通的部分。

**只保留计算操作之间的生产—消费依赖后，有 36 个弱连通分量。**这些部分之间没有计算先后依赖，但其中一些仍共享同一份原始输入。

这两者的区别很重要：

> **“不需要等待对方计算完成”和“不存在数据复用关系”不是一回事。**

因此，我们确实需要上一轮说的两种表示：用计算依赖图处理合法性和并行性，用张量及其消费者集合处理共享读取。

但还有一个更重要的发现：**不能把这些连通分量全部当作不可拆分的任务。**

---

## 二、已经找到一个必须重点拆解的瓶颈：巨大的纯 Vector 分量

在那 36 个计算依赖分量中，有一个分量包含：

**133 个计算操作，全部属于 Vector 管道，工作量为 28,759 cycles。**

如果直接把它整体分到一个核心，即使其他核心完全空闲、所有搬运都免费，这个核心仍然要串行完成这些 Vector 工作。因为每核同一条 Pipe 同时最多执行一个操作。

可以拿它与资源下界比较。

我算出的纯计算关键路径长度为 3,146 cycles。忽略同步、存储门控、额外搬运等限制，仅考虑五核计算能力、依赖链和必需输入输出搬运，可以得到无 L2 场景的乐观下界：

$$
LB_5
=
\left\lceil
\max\left\{
3146,\;
\frac{44610}{5},\;
\frac{55496}{5},\;
\frac{384150}{60}
\right\}
\right\rceil
=
11100\ \text{cycles}.
\tag{6}
$$

这里的必需搬运量取原始输入与最终输出之和；60 bytes/cycle 来自固定配置。这个下界**不是预测可达到的时间**，而是排除了若干限制后仍必须付出的最低资源成本。

而不拆这个纯 Vector 分量的方案满足：

$$
T_{\text{该分量整体放在单核的方案}}
\ge
28759\ \text{cycles}.
\tag{7}
$$

这已经足以决定第一轮算法设计：

**“先按连通分量分配到不同核心”可以做基线，但不应该成为最终方法。必须识别大分量内部的并行结构。**

这里并不需要先运行一个复杂优化器，结构分析就能指出应当把研究资源集中在哪里。

---

## 三、最值得主攻的具体结构：两轮并行分支，中间经过标量归约与广播

我进一步检查了这个纯 Vector 分量。它的主体可以概括为：

```
13 份输入，每份 16 KiB
        │
        ▼
第一轮：13 条可以并行的计算分支
每条主要是 3 个 RELU，随后 REDUCE 成 2 字节张量
        │
        ▼
按照原图已有的 ADD 依赖进行归约
再与一条辅助分支的结果合并
        │
        ▼
产生一个 2 字节张量，供第二轮的 13 条分支共同使用
        │
        ▼
第二轮：各分支再次使用自己的原始 16 KiB 输入
经过 ADD、RELU、RELU、REDUCE
        │
        ▼
再次归约，接少量末端操作
```

例如，第一条分支的原始输入张量 `117` 供操作 `154` 使用，经若干操作到 `160` 完成第一轮归约；同一个输入张量又被第二轮操作 `291` 使用，而 `291` 还要等待广播张量 `290`。这是文件里的真实依赖，不是为解释算法构造的例子。

两轮的每条主要分支都有 4 个计算操作，每个 268 cycles，因此每条分支的计算工作量为 **1,072 cycles**。中间许多标量操作只有 **13 cycles**。

这个结构把三问之间的区别变得非常具体。

### 问题 1：主要矛盾是并行收益与 Task 同步开销

应该优先在**大数据已经归约成小结果的位置**考虑划分，而不是在一条连续的 16 KiB 数据处理链中间随意切开。

但是，**2 字节边界并不等于便宜边界**。问题 1 的跨核前驱 Task 等待是 1,000 cycles，与一条主要分支的全部计算工作量已经处于同一量级。同步约束取相应前驱完成时间和等待时间的最大值，不能把全图每条边的等待简单累加。

因此，我会首先比较两种结构化方案：

**按阶段分批。**把若干第一轮分支合为一个 Task，分配给不同核心；归约区域另行组织；第二轮也按批次安排。目的是保留并行度，同时减少碎片化 Task 和不必要的等待层次。

**归约节点与相关分支联合分组。**把原图中已有的部分归约节点放入合适的 Task，减少跨 Task 的依赖层次。这里仅改变分组和分配，**不重新结合运算、不改写归约树、不改变原图计算**。

这比“按固定节点数切图”更贴近真实代价。

### 问题 2：主要机会是跨阶段保持输入的数据归属

对同一条分支，第一轮和第二轮都需要同一份 16 KiB 原始输入。

场景 B 允许同一核心的多个子图共用一个 Task，保留核内数据；跨核才插入边界搬运和同步等待。

所以一个值得优先验证的方案是：

> **第一轮与第二轮的对应分支尽量放在同一核心，但仍然表示为不同阶段的子图；在中间归约和广播期间保留原始输入。**

五核下，每核承接两三份这样的输入，单看这些输入的驻留体积并不大。是否能够与该核其他工作共存，要再结合完整生命周期及官方排布判断。

这里还藏着一个很容易犯的错误：

**“前后两段放在同核”不等于“前后两段合成同一个子图”。**

如果把第一轮分支和它对应的第二轮分支合成一个子图，却把中间归约放在外面，就会出现：该子图先向外部归约提供数据，随后又依赖外部归约返回结果。这样会形成子图依赖环，而题目明确禁止这种划分。

所以这一题很适合你强调的结构化思考：**数据归属、子图划分、执行阶段，必须是三个不同的概念。**

### 还要同时考虑 Cube 与 Vector 的搭配

这个样例既有上述大块纯 Vector 工作，也有明显偏 Cube 的其他部分。

我会把“将不同资源需求的工作安排到同核，以争取 Pipe 重叠”纳入候选构造，而不是只按总 cycles 均衡。题面也明确指出，同核 Cube 与 Vector 可以独立并行。

但“放在同核”只提供机会，是否真的重叠，还取决于下面这个调度器细节。

---

## 四、一个比泛泛讨论缓存更关键的机制：存储申请顺序会限制乱序执行

新文档明确说明：

**所有产生片上输出的操作，要按照扩展序列中的 allocation order 申请空间。不是只要数据准备好了、对应 Pipe 空闲，就可以自由发射。**

它意味着一种需要重点检查的现象：

某个较早的操作还在等跨核数据，排在它后面的独立操作即使数据齐全、Pipe 空闲，也可能因为尚未轮到申请空间而不能执行。

这会直接影响前面“把 Cube 工作和 Vector 工作搭配到同核”的效果。**资源需求互补，不保证官方调度器能够把它们重叠起来。**

因此，我会把上一轮“通过子图组织影响核内调度”进一步收窄为一个明确研究问题：

> **怎样安排合法的子图顺序，避免一个尚未满足跨核依赖的操作，挡住本核本来可以执行的独立工作？**

第一轮源码审计尤其应该确认：`core_schedules` 怎样影响合并 Task 的排序，新增 COPY 被放在哪里，以及申请顺序在多核阶段如何落实。题面说明问题 2、3 会依据 `core_schedules` 调整子图间顺序，但具体控制能力仍需要源码验证。

**这条机制线值得强模型投入，因为它有可能解释“静态负载已经很均衡，但实际执行仍然很慢”的原因。**

---

## 五、两项研究优先级，现在应该根据样例下调

### 1. 暂时不能把 SPILL 当作主要瓶颈

我按照文档中的 reverse DFS 规则生成原始整图的拓扑序，然后做了一个**不插入 SPILL 的串行生命周期扫描**：先申请当前输出，再释放末次使用的输入。

结果是：

| 存储 | 扫描峰值 | 固定容量 |
| --- | --- | --- |
| L1 | 229,376 bytes | 524,288 bytes |
| UB | 32,776 bytes | 131,072 bytes |

两类存储在这个扫描中都没有溢出。固定容量来自你提供的配置。

必须强调它的边界：这是**按文档转写的整图顺序及串行扫描**，尚未与官方源码核对，也不是多 Pipe 并行时的峰值。改变切图和分核后，顺序、驻留状态都可能改变。

但它足以修改研究顺序：

**先验证结构化切分、负载搭配和同步门控；只有官方结果显示确实存在明显换入换出，再把大量资源投入 SPILL 优化。**

不能因为全图所有 UB 张量大小加起来超过容量，就推断它们会同时驻留。

### 2. L2 的容量不足，也不是目前最有证据的假设

这个样例全部原始 DDR 输入去重后只有 **384,078 bytes，约 375.1 KiB**；固定 L2 容量是 **1,048,576 bytes**，这些输入合计只占约 36.6%。

因此，假设 Cache 只缓存这些原始输入且从空状态开始，它们可以全部容纳。**不能一开始就把“原始输入互相挤出 Cache”作为主要研究故事。**

问题 3 更值得先检查的是：切分产生了多少重复输入读取，这些重复读取发生时第一次填充是否已完成，以及边界张量或 SPILL 数据是否引入了额外 Cache 占用。官方规则是 COPY_IN 发射时查询，miss 后完成搬运才插入，命中不刷新 FIFO 顺序。

也就是说，L2 线仍然要保留，但它的首个问题应当是：

**“复用是否实际发生，以及为什么没有发生”，而不是先研究一个尚未观察到的容量危机。**

---

## 六、文档中存在几处冲突，必须在团队入口处拦住

这次材料还说明，不能直接把整份 Markdown 当成一份完全一致的规格交给 Agent 自由实施。

### 最明显的是问题编号与当前题面不一致

《核内调度算法》中的对应表，把问题 1、2、3 分别写成“存储峰值、换入换出、流水排布”。但当前 PDF 的三问是场景 A、场景 B、场景 B 加 L2。**这部分文字不能用来重新定义本题交付物。**

### 数据格式说明也不能照着示例硬编码

Markdown 写过 tensor id 从 10001 开始、边带 `data_size`；实际样例的 tensor id 从 1 开始，与操作 id 交错分布，边只有 `source` 和 `target`。当前 PDF 也明确说搬运量取张量的 `size`，不在边上重复存储。

因此，解析器要根据 `ops`、`tensors` 数组建立成员关系，不能用 id 阈值判断节点类型。

此外，文档概览图出现过 Step3“无内存状态”的表述，详细章节却明确规定它处理申请、释放和容量等待。我们应该把这种差异登记为待核对事项，而不是挑一句方便实现的解释。

**这正是四机多 Agent 协作需要统一“规则核对表”的原因：一处误读一旦被多个实现共同采用，结果可能很一致，但全都不对。**

---

## 七、根据这些发现，我会这样启动你们的四机研究

现在不必重做上一轮的整个架构，只需要调整第一批任务。

| 机器 | 第一批任务 | 必须交付的证据 |
| --- | --- | --- |
| MacBook Pro，你的主控端 | 统一题面、文档和源码语义；汇总结构画像；审核研究假设 | 规则核对表、统一接口、可追溯实验记录 |
| 游戏本 1 | 封存官方代码，跑单核与强基线，测实际评估耗时 | 官方结果、Trace、运行成本 |
| 游戏本 2 | 主攻重复分支、归约—广播结构、跨阶段数据归属 | 结构化方案与同预算基线对比 |
| 游戏本 3 | 独立挑战路线：通用张量感知划分；重点检查顺序门控及 L2 首次填充 | 能支持或推翻主线的实验，而不是另一份相似实现 |

对于 AI token，最直接的改进是：**不要让多个强模型反复阅读这份一万六千多行的原始 JSON。**

普通 Python 程序先输出连通分量、共享张量、关键路径、资源负载和局部结构；强模型看这些结果及必要的原始片段，负责提出假设、推导和解释失败。明确后的实现与批量实验，再交给便宜执行模型。

这样，你们的 token 优势才会转化成**更多机制不同、能够被实验区分的研究路线**，而不是更多重复阅读和重复编码。

### 已经做好的可复用工具

我把这次分析整理成了一个仅使用 Python 标准库的静态审计工具，并完成了样例统计交叉检查、微型图测试及环检测测试：

它可以直接扫描完整 `data/` 目录，生成每个用例的结构摘要。它**不是求解器，也不替代官方评估器**。

---

**我现在会把主线调整为：结构化分解与同步阶段感知的多核联合调度，以张量复用建模和官方顺序门控分析作为支撑。**

这不是为了换一个论文标题，而是因为这个样例已经给出了一个具体突破口：**拆开必须拆的大型单管道工作，同时尽量不拆断有价值的输入复用，并避免细粒度同步和申请顺序把并行收益吃掉。**

下一份最需要的是 **`code/` 目录的源码压缩包，最好连同 `README.md`**。拿到它，才能把现在这些有数据依据的判断，推进到“官方基线成绩—瓶颈时间线—第一组有效改进”的验证阶段。

---

**Q：**

算了，我直接把附件全部发给你。你帮我好好思考一下策略。不用着急回答，给你充分的思考时间。我会把我们这篇聊天的 chat 做一个 web clip，交给 Codex，让他去分配任务。

**A：**

看完完整附件、核对实现并做了一轮实验后，我的判断已经比前两轮明确得多：

> **不要把全部资源押在某一种切图算法上。主线应当是：理解官方程序真正允许控制的调度空间，利用图的结构生成少量机制不同的候选，用可靠下界判断哪里值得继续优化，再通过原样评估器完成选择和验证。**
>
>
>
> **数学上的结构识别与下界分析、算法上的切分—分配—排序、工程上的并行实验与可信交付，三条线从一开始同时推进。**

这份回答可以作为你交给 Codex 的主要研究总纲。**前两轮基于题面或单个样例的提案，遇到与本轮源码核对、实验结果冲突的地方，应当更新，不能全部累积成“已确认的事实”。**

我也整理了可直接交接的材料：、，以及包含任务依赖、统计结果、微型实验、代表性运行结果和复现脚本的 。

---

## 一、先说最重要的发现：该把资源集中在哪里

本轮已经完成了全部 100 个用例的静态扫描；为每个用例构造了一个简单的四核场景 B 基线，全部通过官方评估；再把这 100 个方案原样放到问题 3 中评估，形成固定方案的无 L2／有 L2 对照。另外，在 17 个用例上做了几组不同构造方法的探测，并运行了微型机制实验和小图完整枚举。**评估代码、原始数据和固定配置均未修改。**

单核固定基准完成了 76 个，剩余 24 个没有被当成失败或零，也没有据此计算全体平均加速比。完整的 1～5 核曲线仍是后续执行阶段的交付。

### 1. 全体数据不能用之前那个样例代表

你上一轮单独发来的 JSON，实际对应正式数据中的 **`case_010`**。全部数据的跨度明显更大：

| 全部 100 个用例的性质 | 实际统计 |
| --- | --- |
| 原始操作节点数 | 766～38,666 |
| 需要分配的非 COPY 操作数 | 552～35,705 |
| 全体非 COPY 操作总数 | 634,506 |
| 计算依赖图的弱连通分量数 | 1～2,666 |
| 计算依赖图只有一个弱连通分量 | 16 个用例 |
| 最大单输入消费者数量 | 2,666 |
| 原始输入去重体积超过固定 L2 容量 | 50 个用例 |
| 四核保守基线发生 SPILL 新增搬运 | 31 个用例 |

这些统计及逐用例结果都保存在中。

因此，前一轮对 `case_010` 得到的两个判断——“当前顺序下存储压力不大”“原始输入能够全部放进 L2”——**只能保留为该样例的描述，不能成为整题的资源分配依据。**

### 2. 更大的图，不一定更值得投入最强模型

这是本轮对策略影响最大的一项发现。

对那个简单的四核基线，我建立了计算关键路径、Pipe 负载和头尾窗口负载等下界。结果是：

**100 个用例中，已经有 24 个用例能够证明相对最优性差距不超过 5%，32 个不超过 10%。**

例如，操作数最多的 `case_014`：

| 项目 | cycles |
| --- | --- |
| 简单四核基线 | 4,551,951 |
| 已建立的下界 | 4,306,503 |
| 相对真正最优解的差距上界 | 约 5.70% |

这不是证明下界一定可达，而是说：**即使还有一个未知的更强算法，它在这个实例上能超越当前结果的空间，也已经受到明显限制。**

所以，你们不应该按照“图最大、总 cycles 最多、日志最长”来分配研究资源。

更合理的是：

> **先判断当前方案距离可证明的性能极限有多远，再判断差距来自什么机制，以及同一种改进能覆盖多少个用例。**

这正是你强调的数学判断能力应当发挥作用的地方：**不是先找一个名字更高级的算法，而是先排除低收益的研究区域。**

---

## 二、源码核对后，有几项规则必须重新写进团队的共同模型

题目要求输出的是子图归属和每核子图顺序，内部展开与最终 Makespan 由提供的评估程序决定。因此，我们优化的不是一台可以任意重排指令的理想 NPU，而是**这个固定展开与执行流程下的方案空间**。

### 1. 最终执行遵循固定 Pipe FIFO，不会自动跳过受阻操作

源码中的流程是：

先生成扩展序列，再把它投影到各条 Pipe，形成固定的操作顺序；多核阶段只能发射当前 Pipe 的队首操作。

这意味着：

**一个 Pipe 的队首还在等待跨核数据时，后面即使存在数据已经就绪的独立操作，也不一定能越过去执行。**

因此，普通的“负载均衡＋通信最小化”模型可能给出一个看起来很好的分配，实际却让各核频繁等待。

更准确的研究对象，是问题 2、3 展开后的执行图：

$$
H(P,a,\pi)
=
\bigl(
V_{\mathrm{expanded}},
E_{\mathrm{data}}
\cup E_{\mathrm{memory}}
\cup E_{\mathrm{FIFO}}
\cup E_{\mathrm{cross}}
\bigr),
\qquad
H(P,a,\pi)\ \text{必须无环}.
\tag{8}
$$

其中，切分、分核和顺序共同决定展开后的操作、内存复用边、Pipe 顺序边和跨核依赖。

**这个展开执行图，比原始计算 DAG 更接近最终优化对象。**

### 2. 我前一轮对全局存储门控的描述需要收窄

局部 Step3 确实按 allocation order 处理申请空间，但最终多核阶段不是重新维护同样的一套全局申请队列。

源码会把局部存储复用转换为额外依赖边，再让多核模拟器执行这些依赖。

所以正确的表述是：

> **局部申请顺序影响生成哪些内存复用约束；最终多核阶段受这些约束和固定 Pipe FIFO 控制。**

这不仅影响理论模型，也影响性能诊断。不能把所有空闲时间都解释成“当前没有剩余缓存”，有时真正限制执行的是已经生成的复用依赖。

另一个细节是：问题 2、3 输出的 `memory_peak_by_core` 取自局部 Step3。**要讨论实际多核时间线上的驻留峰值，需要独立重演，不能直接把这个字段解释成重新扫描全局执行得到的峰值。**

### 3. “子图图无环”还不足以保证方案最终可执行

我构造了一个四个计算操作的微图：原图无环，节点覆盖正确，分区校验也通过，但把同核顺序、Pipe FIFO 与跨核 COPY 合起来后形成等待环，最终被官方程序拒绝。

因此，团队必须区分：

**格式／分区合法**，以及**经过官方展开后可执行**。

不能把前一种状态写成“验证通过”。

这个机制也提供了一条算法方向：官方异常中带有具体的环边。可以利用它定位需要调整的局部顺序或分配，而不是候选失败后只随机重试。

### 4. 场景 B 的跨核多播，源端会按目标核心重复 COPY_OUT

这是一个不能用一般硬件常识替代源码的地方。

本版场景 B 的实现，对每个实际的“源核—目标核”连接分别插入 COPY_OUT／COPY_IN。**不是源端写一次，所有远端共同读取。**

我用一个 600-byte 中间张量送到两个远端核心做了测试：

| 相同数据关系 | 新增搬运量 |
| --- | --- |
| 场景 A：源 Task 一次写出，两个目标分别读取 | 1,800 bytes |
| 场景 B：两个源核—目标核连接各有一对 COPY | 2,400 bytes |

这不意味着场景 B 整体更慢；微图中它仍因同步机制不同而更快。它意味着：

**通信代价必须匹配本版实际实现。不能直接把一个通用超图割代价当成准确成本。**

### 5. 问题 3 的搬运字段，不等于实际 DDR 服务流量

源码在 Cache 模拟之前就计算了 `data_movement_bytes`，随后原样输出。因此，固定方案在问题 2、3 下，这个字段可以完全相同，尽管部分读取已经走 Cache 而不是 DDR。

处理方式应当是：

**保留官方指标原值；另外统计实际 DDR 路径、Cache 路径及其字节数。**

不要为了让论文图表更符合直觉，悄悄修改指标定义。

以上源码位置、微图、方案和结果都已列入。**这份共同规则核对表，应当成为所有 Agent 的入口材料。**

---

## 三、已经有实验证据表明：单一路线会明显失效

我没有直接开发完整求解器，而是做了几种用于判断方向的构造。

“保守基线”保留计算依赖弱连通分量，按总计算量贪心分配，每核合成一个子图。

“小张量切分”尝试在极小张量连接处拆开结构，修复商图中的环后，再做块级分配。

“细粒度列表分配”以计算操作为单元，用简化的计算与通信代价进行调度。

下面都是**四核、场景 B、原样官方评估器的结果**，越小越好：

| 用例 | 保守基线 | 小张量切分 | 细粒度列表分配 |
| --- | --- | --- | --- |
| `case_001` | 58,984 | 58,984 | 78,028 |
| `case_002` | 261,945 | 261,945 | 111,212 |
| `case_010` | 29,918 | 24,566 | 64,978 |
| `case_044` | 124,268 | 156,394 | 70,260 |
| `case_048` | 222,220 | 222,220 | 110,334 |
| `case_051` | 607,628 | 188,224 | 188,732 |
| `case_094` | 24,640 | 25,350 | 57,192 |

完整的 17 个用例对照保存在中。这些是简单的探测构造，**不是成熟强算法，也不是随机抽样的最终性能比较。**

但它们已经回答了几个重要问题。

**第一，保留整个计算分量有时很好，有时严重压缩并行度。**
 `case_051` 的改善非常明显，但这不意味着所有分量都应拆开。

**第二，小张量连接不是万能切分规则。**
 它帮助了 `case_010`，却没有解决 `case_002`、`case_048`，并且让部分用例变差。

**第三，细粒度调度有真正的价值，但不能无条件使用。**
 它改善了 `case_002`、`case_044`、`case_048`，却明显损害了 `case_010`、`case_094`。

因此，我建议最终采用：

> **少量候选构造＋基于结构与瓶颈的触发规则＋官方评估确认。**

这不是把十几个算法拼成一个大杂烩。它应当回答清楚：

**哪个候选解决哪种机制，何时值得启用，付出多少求解成本，以及为什么最终选中它。**

---

## 四、主求解器应该如何组织

### 1. 先建立正确的中间表示，避免三个决策互相混淆

内部至少应分别保存：

| 对象 | 用途 |
| --- | --- |
| 原始 Op–Tensor 图 | 张量来源、消费者、大小、位置、真实数据依赖 |
| 计算操作 DAG | 关键路径、就绪关系、分区合法性 |
| 子图划分 | 场景 A 的 Task 粒度，以及场景 B 的顺序分桶 |
| 操作／子图的核心归属 | 数据复用域与跨核通信 |
| 全局参考序与每核子图顺序 | 构造可控的局部顺序 |
| 展开后的执行图与时间线 | 最终可执行性、FIFO 等待、内存复用和瓶颈诊断 |

尤其不能把**子图编号、核心编号、阶段编号**当成同一个东西。

例如，同一份输入在两个阶段使用，可以把前后两阶段放到同核以争取复用，但仍保留为两个子图。不能因为希望同核，就把中间依赖外部归约的前后两段强行合成一个会产生商图环的子图。

### 2. 保留一个低风险基线，随后进行可中断改进

求解器启动后，先得到一个可信方案，再尝试更好的候选。任何新候选出现校验失败、等待环、异常或超时，都不能破坏已保存的合法结果。

这条规则看似工程化，实际上很重要：

**探索越激进，越需要有一个不会被探索破坏的当前最佳方案。**

但要注意：保守基线也不能永远只用“四核装箱”这一版。它必须支持题目要求的不同核心数量，并且经过全部用例验证。

### 3. 主候选一：结构与同步阶段感知的粗化

这条路线优先寻找：

重复分支、共享输入、长链、分叉—汇合、归约—广播、较小的数据接口，以及 Cube／Vector 工作量互补的区域。

这里不需要先猜“这是一种什么神经网络”。**真正进入算法的，应当是可检测的拓扑和张量属性。**

对问题 1，重点不是把每条分支都拆成一个 Task，而是联合决定：

**多少分支合成一批、归约部分放在哪里、需要多少层跨核同步，以及哪些独立工作可以一起交给一个 Task。**

题面规定问题 1 的同核 Task 切换等待和跨核前驱等待分别为 100、1,000 cycles；问题 2、3 的跨核 COPY 同步延迟为 500 cycles。这些固定等待足以改变细粒度切分是否值得。

一个有价值的研究问题是：

> **能否根据分支工作量、接口大小与同步深度，推导或估计合适的批次粒度，而不是全体用例共用一个固定子图大小？**

### 4. 主候选二：考虑固定 FIFO 的分配与排序

这条路线作为独立挑战者保留，不要求它从结构路线出发。

它可以从较细的单元出发，综合考虑关键路径、各 Pipe 负载、跨核 COPY、共享输入以及内存压力，再进行列表构造和局部调整。

但成本模型必须逐步增加本题特有因素：

**不仅估计某项工作什么时候做完，还要估计它会不会挡住当前 Pipe 后面的工作。**

建议先做两个分离实验：

固定核心归属，只改变分桶和顺序；固定宏块划分，只改变核心归属。

否则，一个结果变好了，你们仍不知道真正有效的是切分、分核还是顺序。

### 5. 局部改进必须由瓶颈触发

不要无差别地随机移动节点。

当 Trace 显示主要问题是输入重复读取，就尝试聚合相关消费者；主要问题是一个核心的 Vector 长时间占满，就尝试拆分可并行分支；主要问题是跨核等待挡住 Pipe，就调整相关子图顺序或数据归属；主要问题是 SPILL，就缩短特定张量的驻留或改变粒度。

**一个局部修改，应当对应一个能够被前后时间线验证的因果解释。**

等待环也可以进入这个过程：把环边映射回引入它的分配或顺序决策，再做有针对性的修复。不能删除原始依赖、修改 validator 或把失败解释成“分数还没算出来”。

### 6. 评估缓存有价值，但缓存键不能过于粗糙

相同候选、相同材料和配置的评估结果应当复用。局部展开也可能缓存。

不过，**“操作集合相同”并不足以认定两个 Task 等价**：边界 COPY、子图顺序、实际 ID 与并列优先级都可能影响展开结果。

因此，精确缓存必须包含足够完整的上下文；粗略结构缓存只能用于成本估计，不能冒充正式评估结果。

问题 3 的全局 Cache 和共享带宽，又意味着局部 Task 成本不能直接相加得到最终 Makespan。

---

## 五、理论线应该从第一天开始，而且要有实际交付

你前面反复强调，不希望只是“现学一堆算法，再局部调参”。这道题确实可以用数学结构减少大量无效探索。

我建议理论线同时承担三个任务。

### 1. 下界：决定哪里值得继续投入

除了总工作量和关键路径，还可以利用操作的“最早开始时间”和“最长剩余路径”。

忽略通信和资源竞争，定义每个计算操作的 head 与 tail。对某一类 Pipe，取 head 不早于某个阈值、tail 不短于某个阈值的一组非空操作：

$$
\begin{aligned}
\mathcal U_p(\alpha,\beta)
&=
\left\{
v:
\operatorname{pipe}(v)=p,\;
h_v\ge\alpha,\;
q_v\ge\beta
\right\},\\[2mm]
T^\star
&\ge
\alpha+\beta+
\frac{1}{N}
\sum_{v\in\mathcal U_p(\alpha,\beta)}c_v,
\qquad
\mathcal U_p(\alpha,\beta)\ne\varnothing .
\end{aligned}
\tag{9}
$$

原因很直接：这些操作不能在前面的时间窗口开始前执行，也必须给后面的依赖计算留下时间；中间窗口内只有有限数量的对应 Pipe。

这是一种容量约束推导，不要求模拟所有调度细节。与其他合法下界取最大值，仍然是合法下界。

对于当前算法的可行结果：

$$
0
\le
\frac{T_{\mathrm{alg}}-T^\star}{T^\star}
\le
\frac{T_{\mathrm{alg}}}{LB}-1.
\tag{10}
$$

这就能给出前面提到的 `case_014` 的最优性差距上界。

需要把两件事明确区分：

**差距上界很小，是停止大量投入的有力依据；差距上界很大，却不证明一定存在巨大可实现收益，也可能只是下界太松。**

### 2. 结构压缩：把大量重复节点变成少量决策

对于结构和工作量相同的重复分支，可能不必一开始就分别搜索每个操作的位置，而是先决定每核承接多少条、如何分批，再重建具体方案。

这非常适合你们的强模型去研究，因为它可能直接降低决策维度。

但必须谨慎：**图结构看起来对称，不代表官方评估器对所有节点置换都严格不变。**固定 ID 的并列排序可能影响具体顺序。

所以在证明完成前，应当把这种压缩称为“构造方法”，而不是未经证明地宣称严格等价。

类似地，可以寻找接口较小的局部区域，在固定边界条件后研究动态规划或局部精确优化。不要一上来建立一个覆盖全部 Cache、带宽和执行顺序的巨大精确模型，然后让整队等待它。

### 3. 小图精确解：检验规则和算法，不只是展示漂亮案例

我已经做了一个四计算节点、两核微图的完整枚举：每问枚举 308 个分区／分配／顺序编码，经官方程序判断可行性，三个问题的最优值均为 64 cycles。

这个“最优”只适用于该微图，但它有明确用途：检查候选生成、合法性判断和性能解释是否正确。

后续应该扩展成覆盖不同机制的小图集，而不是只保留一个容易验证的例子。

**理论线的合格产物可以是一个新构造、一个下界证书、一类精确可解结构，或一个推翻错误假设的反例。不能把它降级成最后给已有代码补公式。**

---

## 六、L2 值得研究，但不应该提前成为整篇论文的中心

本轮全体固定方案对照得到：

**100 个四核保守方案中，29 个加入 L2 后 Makespan 下降，71 个不变；中位加速比为 1，最大约为 1.396。**

这是这组保守方案的观察，**不是 L2 优化的收益上限**。

它说明：不能仅凭“有较高共享输入比例”或者“Cache 命中率变高”，就判断最终时间一定显著下降。

另一方面，时序机制已经有一个很清楚的最小实验：

| 同一张图、相同核心分配，只调整两个独立子图的顺序 | 无 L2 | 有 L2 |
| --- | --- | --- |
| 两核同时首次读取共享输入 | 333 | 333 |
| 其中一核先处理原图已有的独立工作 | 333 | 261 |

没有增加预取操作，没有插入任意等待，也没有修改配置。第一次读取完成后，后面的读取才有机会命中。

这符合题目规定的“COPY_IN 发射时查询、完成后插入”的机制；源码中也没有合并在途首次 miss 的处理。 具体微图及官方运行结果见。

因此，L2 专项先回答四个问题就够了：

**哪些访问是真的重复读取？为什么没有命中？命中省掉的传输是否阻塞关键计算？为获得命中而调整顺序，会损失多少并行性？**

实验上保留“无 L2 优化方案”和“L2 专用方案”，分别在无／有 L2 下交叉评估。这样才能分清硬件收益、一般调度收益和真正针对 L2 的算法收益。

---

## 七、四台机器和 AI 资源，应当怎样分配

### 1. 人的职责不是传统的“建模／编程／论文”三分法

继续采用你偏好的组织方式：

**你负责模型、验收条件、研究优先级和最终判断；其他研究节点负责独立推进路线，交回能够比较的证据。**

论文写作从早期就记录模型、假设和证据，但不需要把某个人永久固定成“只写论文的人”。

四台机器也不是四个彼此独立的项目：

| 机器的初始角色 | 主要工作 |
| --- | --- |
| MacBook Pro：主控与研究汇总 | 共同模型、理论分析、接口审查、结果与论文主张 |
| 游戏本一：结构主线 | 阶段识别、粗化、同步粒度与相关实验 |
| 游戏本二：独立挑战线 | 分配、顺序、等待环与不同构造 |
| 游戏本三：验证与回归 | 原样评估器、固定基准、全量实验、干净环境复现 |

这只是初始分工。机器空闲后进入共享队列，不必等某个研究角色来使用。

**四台电脑用于研发并行；题目中的 2～5 个 NPU 核心是仿真对象，两者是不同层次。**

### 2. 先把独立实验运行好，不急着增加基础设施

目前实际发现的工作集中在 Python 图处理、核内展开与事件模拟。某些大型单核参考评估还有逐事件扫描带来的明显成本。

因此，第一步是测清楚 CPU、内存、输出序列化和评估热点，把独立实验分布到现有机器上。**本轮没有证据表明需要先上 GPU 训练或额外搭建复杂计算平台。**

你已有的 Agent 协议应当直接使用。不要在比赛期间再建一个通用调度产品，让基础设施反过来消耗比赛时间。

### 3. 最强模型要用在高影响的不确定性上

强模型持续承担源码合同、模型选择、理论推导、结构识别、失败分析和集成审查；低成本模型主要承担接口清楚后的实现、批量测试与记录。

但不要机械地规定“绝大多数工作都给便宜模型，强模型只用一点点”。**评价语义、实验设计和集成问题本身就需要强判断，不能按代码行数分配模型。**

与此同时，也不要让多个强模型反复吞入几百 MB 原始 JSON。

应该先由确定性程序提取所需视图，再按任务提供：

“这个结构的局部图”“这个张量的消费者”“这条等待环”“这一类用例的统计”“本次变更的对照结果”。

你已经在做的 Graph Engineering，在这里最有用的部分是**控制读取范围与证据依赖**，而不是让所有 Agent 都读同一份全量材料。

### 4. 研究优先级要考虑题面实际统计口径

题面要求先计算每个用例的加速比，再做算术平均，不是把所有时间相加后求比值。

因此，同一核心数下，某个用例改善对平均加速比的贡献是：

$$
\Delta\overline S_{i,N}
=
\frac{T_i^{\mathrm{single}}}{100}
\left(
\frac{1}{T_{i,N}^{\mathrm{new}}}
-
\frac{1}{T_{i,N}^{\mathrm{old}}}
\right).
\tag{11}
$$

这不是评委完整评分规则，但它是明确的实验收益指标。

**不能因为一个大用例减少了很多绝对 cycles，就自动认为比一类中小用例的系统性改善更重要。**把指标贡献、下界所示空间、同类覆盖数量和验证成本一起用于决定下一轮投入。

---

## 八、交给 Codex 的任务图，建议这样建立

证据包内的 `task_manifest.json` 已经给出具体依赖。核心任务可以压缩为下面这张表：

| 任务 | 关键产物 | 验收条件 |
| --- | --- | --- |
| 材料与规则冻结 | 源码合同、哈希、微型回归测试 | 明确初步合法与最终可执行的区别；复现多播、等待环、L2 首次读取 |
| 基准与用例索引 | 全体结构特征、100 个固定单核基准、覆盖表 | 复用现有结果；补齐缺失项；未运行不记作零 |
| 共同求解与评估接口 | IR、预算控制、当前最佳方案、独立输出目录 | 任一路线可接入；失败不会破坏合法结果 |
| 结构主线 | 阶段／重复结构构造器 | 不只在成功案例验证，也覆盖已知反例 |
| 分配与顺序挑战线 | 独立候选、顺序消融、等待环修复 | 使用相同指标与预算；不共享未经审查的错误成本假设 |
| 理论线 | 下界证明、证书、结构压缩、小图 oracle | 明确适用条件；不把局部或简化最优当全局最优 |
| L2 条件专项 | 时序候选、交叉对照 | 判断依据是 Makespan，而不是只看命中率 |
| 全量实验与论文 | 表格、曲线、逐例结果、主张索引 | 原样评估器、完整覆盖、独立复现 |

这里有三个组织细节尤其重要。

**第一，理论线从开始就并行。**
 不需要等待主算法全部完成，也不应该在最后才被通知“给现有结果补一些理论”。

**第二，每个产物只有一个明确写入者。**
 不同 Agent 使用独立工作树或分支；公共接口和公共模块指定维护者。不要让几条路线同时修改同一个核心文件。

**第三，每次任务交回的不是只有一篇方案说明。**
 最小交付应该包含：假设、实现或推导、适用结构、支持证据、反例、成本，以及继续／停止的判断。

任务持续两轮不能产生可复现的性能改善、适用性解释或明确理论成果，就应该缩小范围、合并或停止，而不是继续堆 token。

---

## 九、实验纪律决定你们能否把“好想法”变成完整成果

### 唯一运行身份，防止不同结果互相覆盖

官方脚本有默认输出文件名。多核数、多场景、多算法并行时，直接沿用默认路径很容易覆盖结果。题面提供了显式指定结果、Trace 和日志输出路径的方式。

每项实验必须记录：

**用例、问题、核心数、算法版本、参数、随机种子、材料／配置／代码／方案哈希、运行时间、状态，以及指标来源。**

不能让一个失败任务留下旧的成功结果，然后被汇总脚本误认为本轮成功。

### 所有内部搜索与选择成本都计入求解时间

题目建议单例在 5～10 分钟内给出结果，并明确希望避免暴力迭代搜索。

研发阶段可以大量并行实验，用来发现更好的规则；最终算法则必须在自己的预算内运行。

**四台机器离线跑出来的最佳方案，不应当被包装成一个只运行几秒的算法结果。**

也不能把“从十几个候选中调用官方评估器选最优”的成本藏到算法外面。

### 先冻结比较规则，再看验证结果

正式阶段需要按结构族和规模组织开发与验证，不要仅按连续文件编号切分。

最终仍然要报告全部 100 个用例。当前预研已经看过全体结构摘要，后续不能再声称这些数据完全未见；但仍可以冻结算法与参数，让独立验证节点检查尚未用于调试的实例与机制。

平均成绩之外，保留逐用例结果、尾部退化和失败情况。**一个在多数用例上更好、但会在少数用例上失效的方法，必须有明确的处理机制。**

### 问题 3 的一核点也应当实测

问题 1、2 的固定单核基准及加速比定义不能换成你们优化过的单核结果。

但问题 3 的一核 Cache 对照，也不能因为“只有一个核心”就机械填成没有收益：单核 SPILL 回读仍可能涉及 Cache。要按选定的对照口径实测，并与前两问的固定基准区分。

完整基本实验已有 **100 个用例、三问、2～5 核，共 1,200 组多核组合**，再加单核和消融。这个规模足以说明，实验工程不能拖到最后才开始。

---

## 十、什么样的成果值得作为冲击数模之星的主张

现在还没有依据判断最终排名；但可以明确，你们应当争取形成什么样的成果，而不是只追求“算法列表很长”。

我建议论文集中在三个能够守住的主张上。

### 主张一：把实际可执行性纳入联合建模

不仅考虑原始 DAG 和分区图，还考虑固定 Pipe FIFO、跨核依赖和内存复用边。

贡献可以体现为：更准确的模型、可执行的构造方法、针对等待环的修复，以及可复现的反例验证。

### 主张二：结构自适应的少量候选，比单一构造更稳定

重点不在“用了多个算法”，而在于：

**结构识别能否解释选择，候选之间是否解决不同瓶颈，选择成本是否受控，最终是否稳定改善完整测试集。**

前面那张对照表已经说明这个方向值得研究，但还没有完成这项主张所需的正式验证。

### 主张三：性能收益能够被下界和机制实验解释

对于接近下界的实例，说明已经受到什么资源限制；对于改善明显的实例，说明减少了哪类等待、复制或 SPILL；对于 L2，区分硬件收益和顺序优化收益。

**一篇能说明“为什么改善、何时无效、还能改善多少”的论文，比只给一个平均加速比和几张漂亮时间线更有研究内容。**

你们的 AI 调度体系，是获得这些成果的生产方式。它可以进入复现说明和工作流介绍，但不应替代题目本身的算法贡献。

---

## 最后的执行建议

我会把比赛剩余时间按大致四段组织：前 15% 完成共同合同、基础接口与基准，同时启动结构和理论探测；接下来 25% 用小规模实验区分路线；中间 35% 收敛、整合和验证；最后 25% 冻结、全量实验、论文与独立复现。

这里真正不能牺牲的是最后一段。**优化成果没有被完整验证、没有进入可复现程序、没有与论文数字对应起来，就还没有成为可提交的成果。**

交给 Codex 后，第一步不是让它同时写十套算法，而是：

> **先读取并复用本轮审计和实验结果，冻结共同接口；随后并行启动结构主线、分配／顺序挑战线和理论线。各路线用官方结果、反例和成本交付，主控据此决定整合与停止。L2 保留为有条件的专项，不提前占据全题。**

这条路线的核心，是把你的四机、模型额度和调度能力转化为**更多高价值假设的验证能力**，再把探索结果收敛成一个可靠、快速、解释充分的求解器，而不是让并行探索本身成为最终成果。

---
