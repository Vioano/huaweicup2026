# Pro B · Coherent 本轮完整回答

原会话：https://chatgpt.com/g/g-p-6ab2d820c86081918067a0c6d5eb1ab6-huaweicup/c/6ab39af5-bcf4-83e8-a5ec-a093f60f2039

message_id：`dcf7fafc-9f17-404c-aca4-39003b2c3035`

来源：网页完整渲染；独立原生清单同ID，原生正文触及长度上限。附件与读取限制见本目录manifest。

---

## 1. 实际读取的 branch HEAD 与文件

本轮固定了以下读取快照。**分支的当前 HEAD、报告中的 as-run commit、官方源码身份分别记录，没有混为同一个版本。**

| 分支 | 本轮固定读取 HEAD |
| --- | --- |
| `main` | `f27ef37bb76dcf556f35d3f2328e405d92241d9c` |
| `codex/q1-bounded-search-20260924` | `4dff90ef699fd51845cf482951e8477066f5f566` |
| `codex/q2-structure-nikolastarx` | `74c46372faf5910b9b3cce6ad9a61a7e040b17aa` |
| `codex/q2-budget-search-yuanzhifang` | `0b58c123cccf02fc993b741d79dcd8511e4dd38f` |
| `codex/q3-core-nikolastarx` | `a4e7ee13310d693ec4fb5cc236669ceb3b172d1f` |
| `codex/q1-exact-batch-20260924` | `5bfe53a29c1ba05167239f51ea937e602f7f85b4` |
| `codex/evaluator-native-probe-20260924` | `03f02e79de4b4bd6f55241385664b154f4332454` |

这些 SHA 来自授权连接返回的分支引用。

### 实际阅读范围

| 材料组 | 本轮实际读取 |
| --- | --- |
| Q3 | `pilot-20260924/metrics.csv`、`REPORT.md`、`run.json`；`online-20260924/REPORT.md`；`docs/a/q3/METHOD.md`、`AFFINE_LAG_EQUIVALENCE.md`；`src/q3/construct.py` |
| Q2 结构 | `joint-20260924/summary.json`、`metrics.csv` |
| Q2 预算 | `docs/a/q2/STAGE_B.md`；stage-b 的 `REPORT.md`、`metrics.csv`、`candidates.csv` 前段 |
| Q1 | `Q1_SEARCH`、`Q1_GUIDED_MOVES`、`Q1_PROFILE_REFINE`、`Q1_TRACE_EXPLANATION`、`Q1_TRACE_PROSPECTIVE`；关联结果摘要、case008 旧单 Task 记录 |
| 精确评估 | `P1_BATCH` 的提交内容及接口、复用边界；native replay 的 `HANDOFF.md` |
| 官方源码 | `singlecore_evaluate.py` 全文；Q1、Q2、Q3 的主要构造与模拟代码；Step1/2/3 核心算法；两种联合环校验；固定 `config.txt` |
| 规范与勘误 | `contract-v1.md` 相关章节、`EVALUATOR_AMENDMENT_20260923.md` 全文；FORM `65d6c0e…` 的 SPEC；`e1575a1…` 的独立审查说明 |

**本轮独立完成的检查**是：四份 case008 计划的字节身份核对；两份归档 E0 结果的压缩前缀解码；三份计划之间的真实编辑距离计算；120 个纯整数仿射排序模型、9600 次比较的区间验证。**没有新增 E0/E1 调用，没有启动全 100 例实验。**

有一项重要缺口：原始 case JSON 存在于 `official-cases.zip`，授权连接能读到该文件的 Git 身份和大小，但本轮未能取得可解压的完整字节。因此，**本轮完整读取的原始 case JSON 数为零，也没有重新执行 `singlecore_evaluate.py`**。以下会明确区分源码推导、已回读的 E0 字段、仓库报告以及待验证命题，而不把旧报告升级为本轮复现。

交付物：

完整研究备忘及七张任务卡 · 机器可读 GOALS.json · 计划、检查脚本、证据前缀与完整任务包

---

## 2. 精确术语表

以下沿用 CRG v1，不另行改义。本轮不提出 Definition Amendment。

首先，“官方映射”研究的是整条确定性行为，而不是只研究最后一个数：

$$P
\xrightarrow{\mathrm{Task/COPY}}L_1
\xrightarrow{\mathrm{Step1}}L_2
\xrightarrow{\mathrm{Step2}}L_3
\xrightarrow{\mathrm{Step3}}L_4
\xrightarrow{\mathrm{global\ events}}L_5
\xrightarrow{\mathrm{result}}L_6 .
\tag{PB-01}$$

| 术语 | 本轮的无歧义含义 |
| --- | --- |
| **固定环境** | 按 D00，固定具体图、问题语义、核数、配置和官方代码身份。另将 Python、运行目录、文件名等作为实验控制条件记录。 |
| **合法方案空间** | 明确使用方案层、Task-order 层还是执行层。JSON 可读、商图无环、执行成功和成绩好是不同判断。 |
| **官方映射** | 冻结环境中，plan 经过式（PB-01）产生的完整行为；Makespan、spill、Cache 等只是不同投影。 |
| **映射族** | 明确允许变化的图、问题、核数、配置、实现版本轴所对应的映射集合。本轮主研究图、问题和核数，少量配置变化只作诊断。 |
| **观测映射／行为签名** | 明确选择哪些行为、如何表示及比较。完整对象、无损哈希、摘要特征不能混称同一种签名。 |
| **guard／regime／切换边界** | guard 是离散条件；regime 是指定决策签名的等值类。固定环境的一次局部编辑翻转 guard，才是 D07 意义的边界跨越；改变核数是跨环境变化。 |
| **primitive operation** | 带类型、确定参数和合法适用域的部分方案变换。一次 split、insert、move 等需要给出完整规格，不因代码短就自动成为 primitive。 |
| **编辑大小** | 保留分区、归属、顺序等多维变化数量。改变一个构造参数，不等于只改变一个次序关系。 |
| **离散雅可比／局部响应** | 在固定环境中，指定 primitive 前后的指定行为差异。数值取差或比，集合取对称差，序列与事件采用各自的对齐方式，不当作连续导数。 |
| **耦合度** | 按 D12 分别计算层覆盖、远端对象比例和传播距离。不能只说“强耦合”，也不能把数据图上不可达当成资源影响为零。 |
| **敏感方向** | 同时满足小编辑阈值、显著响应阈值和指定域稳定度。单个大 macro 的巨大收益不是稳定局部敏感性的证明。 |
| **行为等价／行为商** | 当前观测相同只是 D14；D15 还要求未来允许操作及其所有 continuation 不可区分。商的样本必须先区分真正不同的 plan。 |
| **最小充分状态** | 相对于未来方案编辑及未来观测充分，而不只是让某次仿真继续运行的 checkpoint。抽象等价类存在不等于找到了廉价表示。 |
| **局部评估器集** | 每个评估器有 detector、预测目标和不确定性；域外、冲突或 guard 风险时 ABSTAIN，回到更精确评估。 |
| **生成完备／可达完备** | root-generative、pairwise reachability、restricted completeness 分开。本轮有限候选族没有完整官方空间的完备性声明。 |
| **macro operation** | 图 guard、regime guard、primitive policy、允许操作集、预期可观察机制组成的部分变换，必须有失败回滚。 |
| **多核尺度／continuation** | 核数是映射族的离散轴；利用旧计划构造下一核数方案，不等于冷启动重跑，也不保证性能改善。 |
| **峰值见证** | 具体环境、具体计划、明确参考及高收益观察。是否“高”必须先指定门槛。 |
| **峰值机制** | **图结构 × 方案／操作 × 问题 × 核数 × 配置 × regime** 联合作用的条件性命题，具有可迁移的干预预测。 |
| **Peak-to-Mechanism Distillation** | 见证→反事实→逐层差异→guard／原因→图结构条件→跨实例／核数／问题测试→guarded macro。 |
| **算法蒸馏** | 将验证过的 primitive、guard、macro、路由和索引压成竞争版程序，并报告质量、时间、内存及可解释性损失。 |

本轮尤其保留两个区别：**仿真状态充分，不等于对未来 plan 编辑充分；构造参数去重，不等于不同 plan 的行为商压缩。**

---

## 3. 当前峰值事实表

### 3.1 先把证据等级与数值身份分开

表中：

- **E0 回读**：本轮直接读取了归档官方结果中的指定字段，**不表示本轮重新运行，也不表示完整 Trace 已核验**。

- **AUTHOR-REPORT**：报告、metrics、run 身份等已交叉阅读，但原始完整结果／计划／Trace 尚未在本轮全部独立回读。

- **†**：基准 487605 来自旧 E0 单 Task 记录；该计划字节已核对，但本轮尚未重新执行 singlecore CLI。

“winner”仅表示表中明确候选池的胜出者，不表示全局最优。

| 图 | q | k | 单核基准／普通参考 | 当前 winner | CRG-19 speedup | 邻近或反事实证据 | 等级 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 008 | Q2 | 4 | 基准 **487605†**；普通方案 123060 | **63768**，resource-word | **7.64655†** | affine 为 64686，但不是单 primitive 邻居 | winner 指定字段 E0 回读；基准 AUTHOR-REPORT |
| 008 | Q3 | 4 | 同上 | **63768**，同一 resource-word plan | **7.64655†** | 0 hit、432 miss；新单插入邻居待评价 | 指定字段 E0 回读 |
| 008 | Q1 | 4 | 基准 487605† | **113686**，另一份粗化计划 | **4.28905†** | 后续两个 split 候选 114685、122751，均未改善 | AUTHOR-REPORT；不是 resource-word 的 Q1 成绩 |
| 044 | Q2 | 4 | D 方案 125648；单核基准未核验 | **69113**，M2 | — | 同池保留明显退化候选；joint 中其他顺序为 74099、71242 等 | AUTHOR-REPORT |
| 002 | Q2 | 4 | D 132209；M1 72415 | **72056**，joint-critical | — | 同一研究中的 earliest-start 为 92004 | AUTHOR-REPORT |
| 044 | Q3 | 4 | component 89039 | **70261**，affine | — | 命中率下降、spill 下降，Makespan 改善 | AUTHOR-REPORT |
| 080 | Q2 | 4 | 两计划比较 | **111466**，component | — | affine 为 **112445**，更差 | AUTHOR-REPORT |
| 080 | Q3 | 4 | 同样两份计划 | **83124**，affine | — | component 为 **90736**；排序相对 Q2 翻转 | AUTHOR-REPORT |
| 051 | Q1 | 4 | 父计划 338698 | **336057**，一次 split | — | Task8 第四个计算操作后切分；另有切点对照 | AUTHOR-REPORT |
| 044 | Q1 | 4 | 父计划 116227 | **114443**，merge(6,8) | — | merge(8,10) 为 **126094**；零 spill split 也更差 | AUTHOR-REPORT |

Q3 三图配对、Q2 joint 和 stage-b 的数值分别来自对应的固定记录。    Q1 的改善及失败候选在 profile 和前瞻报告中均有保留。

### 3.2 case008 的计划身份，已经独立核对

以下三份计划的 **SHA-256 和 Git blob SHA-1 都重算匹配**：

| 计划 | SHA-256 |
| --- | --- |
| component | `c9e86dae0d8290112a9a8ec8a2fe1052f6edd18f0a1765706c7f0351cf70fdf9` |
| affine_eighth | `40ec77159696d284091f6926aab72b711ec682769434a32e14b54229b4ec7357` |
| resource_word | `1935d89ad16a59a52ad62b2e00b4b43f30e783ade40fe481fc6063d905da64ce` |

它们确实保持相同的有序 `node_to_subgraph` 和操作核心归属，只改变 `core_schedules`。但是变化并不“小”：

| 比较 | 分区变化 | 核归属变化 | 改变的顺序位置 | 翻转的先后关系对 |
| --- | --- | --- | --- | --- |
| component → resource-word | 0 | 0 | **596** | **1116** |
| affine → resource-word | 0 | 0 | **720** | **3716** |

因此，**64686 与 63768 只差 918 cycles，不能据此把它们称为局部编辑邻居。**后者相对于前者有更多次序关系翻转。这项检查直接约束了 D10、D11、D13 的使用。

同时保存了一个真正按“一次核内插入”构造的候选：只在 core0 将 sg96 从第 14 个索引位置移到 sg7 之前，即索引 10。它只改变 **5 个位置、4 对先后关系**，其他核和 mapping 不变；**尚未检查其原图合法性，也没有 Makespan**，所以只是反事实种子，不是已经完成的 witness。

字节核对与编辑差异记录 · 待验证反事实及 provenance

### 3.3 63768 的原始结果头部，还揭示了一个重要限制

从 Q2 压缩 E0 产物的前缀，直接读到了：

| 字段 | 值 |
| --- | --- |
| global Makespan | **63768** |
| 每核 Step3 `local_makespan` | **64215** |
| 每核 `memory_dependency_count` | **570** |
| 每核 MTE2 / MTE3 / M / V 操作数 | **108 / 27 / 54 / 162** |
| spill 字节 | **0** |
| 跨核 COPY | **0** |
| 总计划 COPY 字节 | **2654424** |

Q3 对应原件头部则直接给出 63768、0 hit、432 miss。解码文件保留了“不完整前缀”标记，没有冒充完整 JSON 或通过 CRC 的归档。

这里有两个直接结论：

**零 spill 不等于没有内存复用依赖。**这个 winner 每核仍有 570 条相关依赖。

**Step3 的局部 Makespan 不能不加条件地当成最终全局时间的下界或固定 Task 工期。**本例局部值反而大于最终值；其差异必须依照官方两阶段语义解释，不能用理想调度器替代。官方源码也明确全局阶段重新安排时刻，只保留准备阶段的依赖与 Pipe 顺序。

### 3.4 “四核超过 7 倍”应怎样解释

以尚待本轮 singlecore 复跑的基准记录 487605 为条件，算术分解是：

$$\frac{487605}{63768}
=
\frac{487605}{123060}
\frac{123060}{63768}
\approx
3.96234\times1.92981
=
7.64655 .
\tag{PB-02}$$

这说明高比值包含“固定基准→普通四核方案”和“普通方案→结构顺序方案”两段改善。**它不是 Cache 带来的 7.65 倍，也不能把第一因子未经控制就全部归为纯核数收益。**

更有意思的是：

$$123060-63768
=
59292
=
27\times2196 .
\tag{PB-03}$$

运行记录中，case008 是 108 个计算时长模板相同的作业，四核每核 27 个，单作业中间 V 阶段总工作为 2196。这个精确吻合支持一个很强的候选解释：**资源词把原来暴露出来的 V 等待大部分隐藏到了 M 工作后面。**但“吻合”仍然不是全部因果贡献的证明；COPY 相位和 MEMORY_REUSE 的变化尚需完整对照。

---

## 4. 对峰值机制路线的总体评价

**值得继续，但应收敛到三个机制候选，而不是建立“所有问题都有少数敏感方向”的先验。**

### 4.1 优先级最高：Q2/Q3 的资源重入交错

已有证据支持它成为一个 **macro 来源候选**：固定分区与归属，改变优先级，出现很大的执行改善；Q2/Q3 成绩相同且没有 Cache 命中，排除了“这次收益来自 Cache 命中”的解释。

但当前代码的 graph guard 比“同构重复分支”弱得多。`word_descriptor` 检查的是实际串行计算链、Pipe 模式、首尾 M 工作相同、计算时长 tuple 相同，以及 V 总工作不超过两次 M 工作之和。**它没有证明完整 Op–Tensor 图同构，没有检查所有输入共享、tensor size/pos 和生命周期都相同。**

因此，待验证的机制应写成：

> 当图含重复的 M→V*→M 计算链，完整输入／内存结构落在已验证风险域内，且处于 Q2/Q3 的单核合并 Task 语义时，通过合法优先级编译形成有限前视的资源词，减少 M 队首等待 V 的时间，从而获得稳定收益。

不能写成“识别到 M→V→M 就必快”。

### 4.2 Q1 最值得保留：晚输入门控前缀分离

case051 的解释比许多更大的百分比更有价值，因为它已经把差异定位到了明确的门控与关键路径位置。

仓库记录中，旧 Task8 在 13007 开始；拆分后的前半可以在 6482 开始、9674 结束，后半仍等到 13007。旧关键节点持续 2722 cycles，新后半只有 81；其余 119 个关键路径节点持续时间不变，差额恰好是 2641。**这是一条 trace 上的精确时间账目，不是“所有 Task 工期固定”的预测模型。**

同时，前瞻实验已经给出负结果：003、008、044 三个新池中的六个待排序 split，其入口提前量全部为零，没有重排和新增收益。

这改变了下一项任务的目标：

**不是继续调整“入口提前量”的权重，而是检验候选生成器能否直接找到真正脱离晚输入门控的切点。**

### 4.3 Q3 独立候选：共享输入的 FIFO 准入相位

case080 的同计划 Q2/Q3 排名翻转，是一个明确的场景交互见证线索：affine 增加了 spill，Q2 变差，Q3 却改善。它足以要求我们保留这类候选，不能按 Q2 排名或零 spill 硬筛掉。

真正需要提炼的是：

> 共享 support 结构，在某个由完成次序、准入、淘汰和两带宽池活跃集合定义的 regime 下，怎样通过合法顺序操作改善关键 COPY 的服务路径？

“命中率更高”只能是观测，不是充分机制。case044 已经提供相反方向的对照：命中率下降，但 Makespan 改善。

### 4.4 三类峰值的操作性分类

建议下一次试验预先采用以下规则，而不是事后给 winner 起名字：

| 分类 | 建议的操作标准 |
| --- | --- |
| **Structural peak** | 在预定义 graph guard 下，至少 3 个非简单复制的匹配实例、至少 3 种核数；至少 80% 的正域单元达到事先规定的收益门槛，并报告覆盖率、成本及最差回归 |
| **Parameter-local peak** | 在预先列出的较窄核数／配置／regime 单元内达到上述重复性，但跨出该域后稳定失效；边界能被 detector 预测 |
| **Accidental peak** | 小扰动下收益反复消失，预注册条件无预测力，而且有限修正预算内仍无法归纳；只暂称“该测试域内偶然” |
| **尚未分类** | 缺少反事实或迁移证据；不能因尚未找到解释就归为 accidental |

资源词的 prospective 高收益门槛可先设为**相对同核普通方案改善至少 10%**；Q1 单次局部操作可设为**至少 0.5% 且至少 1000 cycles**。这些是研究阈值建议，不是宣称当前材料已经通过。

---

## 5. 九条关键可证伪命题

| ID | 命题及当前等级 | 最有判别力的反证 |
| --- | --- | --- |
| **B1：资源重入机制** | **HYP**：图与 mapping guard 成立时，资源词主要通过减少 M 队首等 V 的空隙产生可迁移收益 | 相同 M/V 总工作但非重入结构；相同计算链但输入共享或 tensor 大小不同；新匹配图持续失败 |
| **B2：Task 屏障改变机制** | Q1 与 Q2/Q3 的 Task 组织差异为 **FORMAL**；资源词在 Q1 的收益幅度为 **HYP** | 同一字节 plan 横跨 q 后，观察 Task、COPY、100/1000 等待和 FIFO；不能只比较三个最后数值 |
| **B3：早前缀分离** | **HYP**：存在不依赖晚到输入的前缀时，一次 split 可将其移出关键等待 | 前缀仍被本核前项卡住；重编译后的生产者时刻变化；新增 COPY 抵消提前空间 |
| **B4：backing 状态解释悬崖** | 条件 spill 字节公式是 **FORMAL**；其能否帮助预测 split/merge 为 **HYP** | 加入 backing、pos、版本后仍系统误排；检测成本超过少量 E0 所省成本 |
| **B5：FIFO 相位可预测** | **HYP**：080 的排序翻转能由实际准入／淘汰与两池状态定位，并在小域内预测 | 只有事后 hit rate 能“解释”；相同预定义 guard 的新邻居反复给出相反结果 |
| **B6：核数 continuation** | 末尾追加空核的指标保持是条件性 **FORMAL**；warm macro 优于 cold 是 **HYP** | 保持旧 ID／顺序后仍出现旧操作时刻差；或者 warm 的准备、修复、评价成本抵消收益 |
| **B7：仿射区间去重** | 固定构造条件下的整数排序区间是 **FORMAL**；本轮有 9600 次纯排序检查 | 区间内完整 plan 字节不一致；或相对 hash-dedup 基线没有净构造成本收益 |
| **B8：当前签名不等于行为商** | D14 与 D15 的区别是定义层结论；有用压缩表示仍是 **HYP** | 两个当前同签名计划被后续合法 primitive 区分；不同计划的样本商压缩率接近 1 |
| **B9：少数敏感类** | **HYP**：少数预定义 operation classes 能解释大部分显著响应，并跨域稳定 | 等编辑尺度、等抽样后，需要接近整个 operation basis 才能解释 80% 响应；或解释力仅存在于挑变量的旧池 |

两个补充很重要。

**第一，B7 不是 D16 的证据。**同一段 lag 参数产生完全相同的 plan，在方案空间里本来就是同一个点。它可以节省重复构造，但不能将参数样本数当作不同 plan 数，制造很高的行为商压缩率。

**第二，小 primitive 邻域没有显著响应，不一定推翻一个需要协同编辑的 macro。**它会否定相应的小编辑敏感性主张，但可能留下“较大、结构化组合操作有效”的结论。不能把 coherent 框架中的某一条箭头强行保住。

---

## 6. 图结构候选特征体系

**纯图特征必须只由 G 决定。**依赖当前 plan、容量或 trace 的量，应单独标为环境／方案条件化特征。

| 特征家族 | 可计算定义 | 检验方式与风险 |
| --- | --- | --- |
| **DAG depth / width** | 语义 COPY 收缩后的最长依赖路径；小图用传递闭包与匹配求偏序宽度 | 大图可先报固定 Kahn 顺序的最大前沿作为下界／代理，不能叫精确 width；最大层大小也不自动等于 width |
| **弱连通分量** | eligible DAG 无向化后的分量数、大小分布、最大分量工作占比 | 区分可整体分核的作业与大耦合图；分量之间仍可争用 DDR、共享输入 |
| **M/V 工作分布** | 分 Pipe 累加有效计算周期；分量内分别统计，不只用全图比率 | 构造“总比率相同但内部顺序不同”的负对照，检验比率是否真的有预测力 |
| **资源重入** | 对真正串行分量提取压缩 Pipe 词、首尾 M 与中间 V 阶段工作 | 将 M→V→M 与 M→M→V 等同工作量图比较；不要只匹配字符串而忽略真实依赖 |
| **重复分支** | 对有界大小的 Op–Tensor 子图比较带属性结构、端口、size/pos；哈希只用于预分桶 | 当前构造仅检查计算时长模板，尚未证明完整同构；大组件无法在预算内核实时标 unknown |
| **shared tensor support** | 每个输入 tensor 的消费 job 集合；相同集合合成统计意义的输入束 | 只能压缩统计，不能合并真实 tensor、Cache key、请求次数或整数取整 |
| **fan-in / fan-out** | Op 度数与 tensor 消费者数分别统计，并保留字节加权分布 | 度数相同但源端逐目标 COPY_OUT 不同、关键消费者不同，可能有完全不同结果 |
| **size / pos 分布** | L1、UB、DDR 分别统计大小分位数、总量及单操作输入输出并集 | 一旦除以容量就依赖 C，不再是纯 G 特征 |
| **critical outputs** | 计算依赖图中到各出口的最长尾工作、出口覆盖结构 | 这是 data-only criticality，不是官方资源关键路径；后者必须从执行观测取得 |
| **Task-boundary potential** | 对事先固定、与分数无关的规范拓扑切线，计算跨界 tensor 并集曲线 | 可作为纯图候选特征；实际边界量必须结合 P，Q1 按 Task、Q2/Q3 按 core 区分 |
| **memory pressure** | 固定规范拓扑序上的首次／末次 touch 闭区间前沿，按 pos 分开 | 不是实际驻留峰值，更不是容量证书；真实 Step1/2、backing 和 incarnation 另行观测 |
| **product / grid** | 将触达两个输入束的 job 当边，做二着色、完整二部连接、重边和 support 闭合检查 | 不把识别问题变成昂贵的最大 biclique 搜索；未匹配就退出该构造域 |
| **reuse topology 与 ID** | support 重叠、规范次序下的重用间隔、同深度／同类型并列中的原 ID 次序 | 图同构不保证固定 ID 启发式相同；重编号是新的 G，不是免费等价归一化 |

对于 case008，目前报告给出的“108 分量、864 eligible ops、统一计算时长模板”与构造代码一致，但**本轮没有读取完整 G 后独立验证这些结构计数**。对于其他特征，我没有把“看起来合理”写成“已经有效”。

下一步应先做**零 E0 的只读结构扫描**，再冻结正例、反匹配例和变量选择。即使扫描全部 100 个 G，也不等于授权跑 100 个 solver；这两种成本与实验身份必须分开。

---

## 7. 多核／Q1–Q3 映射族研究设计

### 7.1 先按“最早可能失效的层”分解参数轴

这是目前比笼统宣称“映射低维”更可执行的结构。

| 变化 | 通过合法性检查后，最早可能改变的计算层 | 可复用什么／不能复用什么 |
| --- | --- | --- |
| **Q1 固定分区，仅改变分核、核序或 Task wait** | 局部计算结构可保持；核心元数据、联合环和 L5 改变 | 可以研究复用官方局部编译；不能只回放两个核，更不能复用旧 Makespan |
| **同 plan 从 Q2 到 Q3，其他共有参数固定** | Task/COPY、Step1–3 使用相同构造路径；差异进入 Cache 全局事件 | 可以配对比较共同准备产物；不能把 Q2 分数直接当 Q3 排名 |
| **只改变 Q3 L2 容量／带宽** | L5 的准入、命中、两池事件 | 不应重新发明局部调度；也不能按 hit rate 线性缩放时间 |
| **改变 L1／UB 容量** | L3 spill 和 L4 内存依赖即可变化 | 必须重新检查 backing、物理化身和 MEMORY_REUSE |
| **改变 DDR 带宽** | COPY 参考工作量、局部排布／内存依赖及全局事件均可能变化 | 不能把原 Makespan 按带宽倒数缩放 |
| **改变分区或 B 场景优先级** | L1 边界／COPY 所属，或 L2 优先序 | 后续 spill、FIFO、MEMORY_REUSE、Cache 都可能失效 |

上述差别直接来自两个 Task builder、Step2/3 和全局模拟。

尤其不能把 Q1 的固定分区编译缓存直接用于 Q2 的 resource-word 顺序搜索：**Q2 优先级本身会改变 Step2/3。**

### 7.2 核数 continuation：先证明嵌入，再测改善

对冻结源码，在保留全部旧 ID、有序 mapping、旧核次序和配置的条件下，只在末尾追加一个空核，不新增请求，也不改变原有 active request 集合。对成功执行的计划，可得到指标投影上的条件性结论：

$$M_{E_{k+1}}\!\left(\iota_k(P)\right)=M_{E_k}(P).
\tag{PB-04}$$

这里的等式**不是完整结果等价**：核数、空核条目等元数据不同。它也不是“使用新核会更快”。官方共享带宽按在飞请求而非核预算等分，是这个嵌入结论的重要原因。

如果该嵌入在所声明成功域内成立，扩大可用核预算的可行域最优值就不会因为“多了一个可空置核心”而恶化；**有限预算求解器实际找到的值仍可能非单调**。两者不能再混淆。

建议每个核数同时保留三种对象：

**冷构造；旧方案追加空核；从旧结构出发的一次 guarded continuation。**

报告从一核开始的整条轨迹成本，以及“已给定父计划”条件下的边际成本。不能把前一核数的搜索当作免费预计算。

对于报告中的 108 个同质作业，若限制整 job 不拆分，均衡分核的 M 工作尺度为：

$$L_{\mathrm{whole\text{-}job}}(k)
=
2a\left\lceil\frac{108}{k}\right\rceil,
\qquad a=1158.
\tag{PB-05}$$

| k | 受限整-job 族的 M 工作尺度 |
| --- | --- |
| 1 | 250128 |
| 2 | 125064 |
| 3 | 83376 |
| 4 | 62532 |
| 5 | 50952 |

**这些不是本轮实测 Makespan，也不是不加条件的原问题最优值。**它们只是判断启动／收尾、带宽和内存代价何时开始主导的参照。

### 7.3 Q1、Q2、Q3 必须保存“同 plan 跨环境”与“各环境自己优化”两张表

同 plan 跨 q 比较，用于识别 Task barrier、复制同步和 Cache 的机制差别；各 q 自己构造的最好方案，用于实际性能比较。混在一起会把“换了语义”和“换了算法”两种干预混淆。

Q1 尤其不能把 singleton 看成零成本优先级标签：每个子图是独立 Task，同核也重新通过 DDR 交接，还有 Task 等待。Q2/Q3 才是在同一个核 Task 内利用子图顺序组织资源词。

依 CRG D29，Q3 仍记录固定单核基准比；另外保存**同 plan、同核数的 Q2/Q3 比值**作为无 Cache／有 Cache 对照。两者名称、分子分母和用途都不互换。

### 7.4 合适的数学语言

我建议使用：

> **带 guard 的参数化编译—离散事件系统，以及相对于指定决策签名的有限行为相图。**

其中可分两个签名：

**编译签名**记录 Task/COPY、Step1 序、spill victim、backing 来源、incarnation 链、Pipe FIFO、MEMORY_REUSE；**事件签名**记录关键 release 的决定者、退休／发射并列顺序、资源池 active set、Cache hit/miss 与准入／淘汰。

这比直接称为“光滑分层空间”或“分段线性映射”稳妥。官方包含整数取整、binary64 计算、固定并列顺序和数据结构状态，不能不加条件地套上连续几何。

**speedup curve 只能提出 regime transition 候选。**同一 Makespan 可以对应不同签名；非关键分支跨 guard，也可能不改变 Makespan。

### 7.5 与其余 coherent 对象的接口

峰值见证首先向 Pro A 提供完整父子计划和指定观测，用于形成真正的局部响应表；再测层耦合、远端影响和稳定敏感性。对新生成 ID，应同时保留原 ID 和生成 provenance，不能靠重新编号把差异抹掉。

行为商不是必经关卡。若观测包含完整 L0 plan，不同计划已经被输入层区分，不能期待非平凡压缩；若换一个不含全部输入标签的观测，则必须重新检查未来操作可执行性。**不能为了得到高压缩率而偷偷删掉会影响未来行为的信息。**

局部评估器也不是必需品。Q1 exact batch/native 交接已经显示复用收益强烈依赖候选数量：原生路线报告在大批固定划分候选中很快，但只有三个候选时可能更慢。不能将这类批吞吐峰值直接用作单例 macro 的成本假设。

---

## 8. 最小行为相图实验

### 8.1 首次实验建议：总上限 64 次新 E0，而不是铺满笛卡尔积

这是一份**尚未运行的提案**。先回读已有完整产物，能按身份复用的就不重跑；所有补跑、只读观测验证、失败和确认都进入统一账本。

| 模块 | 比较对象 | 初始额度 |
| --- | --- | --- |
| 重入尺度 | 008：普通／word，Q2/Q3，核数 1–5 | 20 |
| Task 屏障 | 008：相同两份 plan，Q1／4 核 | 2 |
| 真正局部邻居 | 008：两个通过合法性检查的一次插入邻居，各跑 Q2/Q3 | 4 |
| 入口门控 | 051：父／cut4／cut5；两种等待诊断配置下的父／子 | 7 |
| backing 悬崖 | 044：父／好 merge／坏 merge／零 spill 坏 split；两个容量临界对照 | 8 |
| FIFO 相位 | 080：两 plan×Q2/Q3；两个 L2 临界对照 | 8 |
| 固定单核 | 008、044、051、080 各一次 | 4 |
| 观测无副作用、确认、失败预留 | 用尽即停止，不填补未知值 | 11 |
| **合计上限** |  | **64** |

配置变化只能使用独立诊断副本，**不得修改官方固定配置原件，也不得将诊断结果写作正式成绩**。固定配置中的容量、带宽、等待和 L2 参数已回读。

### 8.2 每个 witness 的完整比较对象

| 层 | 应保存和比较的内容 |
| --- | --- |
| L0 | 原始 plan 字节；mapping 插入序；分区、归属、次序 edit signature；primitive 参数 |
| L1 | Task 成员、边界 COPY、COPY 所属子图、生成 ID、跨核端点 |
| L2 | raw Step1 与优先级调整后的完整序列；只说“计算词相同”不够 |
| L3 | overflow、victim、backing 来源、是否真正 COPY_OUT、from/to incarnation、版本和 pos |
| L4 | 四 Pipe FIFO、MEMORY_REUSE、局部内存统计、准备阶段时刻 |
| L5 | Task／COPY release、队首阻塞、DDR／CACHE_READ active set、完成／发射序、Cache 准入与淘汰 |
| L6 | 完整结果、Makespan、搬运分项、Cache 指标、状态及成本 |

spill 的官方计划搬运统计使用条件式，而不是固定两次搬运：

$$D_{\rm spill}(P)=
\sum_{s\in\mathrm{Spills}(P)}
s.\mathrm{size}\,
\left(1+\mathbf1[s.\mathrm{spill\_out\_copies\_data}]\right).
\tag{PB-06}$$

已有 backing 时只增加 COPY_IN；版本递增，L1／UB 位置保留。Q3 中这个统计仍不能直接叫作实际 DDR 字节，因为部分读取可能走 Cache。

### 8.3 反证应当怎样真正改变决策

如果 **B1 只在 case008 成立**，且 tensor／ID／核数小扰动使收益无法预测，则将资源词从 structural 候选降为局部特例。

如果 **B3 的新候选仍全是零提前量**，就停止给入口提前量做更多排序包装；这表明当前 generator 没覆盖该机制，而不是需要更复杂模型。

如果 **B5 只能用事后 hit rate 解释**，就不给它在线 detector 地位，只保留离线机制描述。

如果 graph guard 在新图上没有优于简单结构基线的预测力，或者 macro 适用率很低且检测很贵，则降低部署优先级。**机制解释有价值，不代表每个机制都值得在线使用。**

---

## 9. 建议 Codex `/goal` 候选任务

下面七项都是候选任务，未启动。完整卡片已放入 GOALS.md 和 GOALS.json。

共同禁止范围记为 **F0**：不修改官方源码、原始图、固定配置、FORM／contract、历史证据和生产 solver；不改变 Cache／FIFO／带宽语义，不插入任意等待、预取或重算；不自动扩大预算。诊断图／配置只创建独立副本。所有计时包含构造、观测、路由、评价、回退与落盘。

### PB00-WITNESS：补齐可信见证与真正反事实

**命题：**现有峰值能够绑定到准确环境、计划和完整官方行为，而不是同分不同 plan、跨版本拼接或错误单核基准。

**输入／branch／commit：**Q3 `a4e7ee1…` 的 pilot；Q1 `4dff90e…` 的 profile/trace；Q2S `74c4637…` 的 joint；Q2B `0b58c12…` 的 stage-b；完整 SHA 见第 1 节。使用已核验的三个 case008 plan 哈希及官方 case ZIP。

**允许修改范围：**`research/peak_B/PB00-WITNESS/`、对应新 `results/a/peak_B/` 目录。**禁止修改范围：**F0。

**实验协议：**恢复原图并校验 manifest；重算四图 singlecore；完整回读 008、051、044、080 的指定证据。每个中心补一条真实单 primitive 边，先验合法域再评价。只读观测分别做有／无观测的完整函数对照与未插桩 CLI 对照。最多 **32 次新 E0、20 分钟研究墙钟**。

**输出：**W 身份表、L0–L6 检查点、普通／坏／局部反事实、原配置单核结果、全部失败。

**验收指标：**哈希一致；基准确实来自 singlecore；每个完成的 W 至少有一个经验证的单 primitive 邻居；观测不改变官方结果。

**反证／停止条件：**身份、原分数或观测差分不一致就隔离该 W；预算耗尽保持 unknown。**依赖：**无。**可并行性：**按图独立读取，统一账本；计时对照串行。**预计资源：**每子任务一个 CPU worker，建议 2 GiB 采样停止阈值；工程量估计半天。**成功后解锁：**其余所有机制任务共享可靠输入。

---

### PB01-REENTRY：资源重入峰值能否跨图迁移

**命题：**重复 M→V*→M 结构在 Q2/Q3 的适当 guard 下，通过降低 M 队首等待形成可迁移收益，而非只适合一个 JSON。

**输入／branch／commit：**Q3 `a4e7ee1…` 的 `construct.py`、`METHOD.md` 和 PB00 的 case008 完整见证。

**允许修改范围：**独立 `PB01-REENTRY` 研究／结果目录。**禁止修改范围：**F0，不修改原构造器的历史身份。

**实验协议：**固定普通／word 的分区和归属；对齐 M/V、COPY 和 MEM 行为。加入同总工作量非重入结构、共享输入变化、tensor 大小变化等反匹配对照。只读结构扫描后，选择两个未用于当前规则调参的匹配／反匹配样本；没有匹配样本就报告覆盖不足。本任务主要固定四核，核数轨迹交 PB05；最多 **32 次新 E0**。

**输出：**实际 graph guard、mapping guard、primitive composition、阻塞／重叠账目、迁移表与回滚路径。

**验收指标：**预定义正域至少 80% 达到相对同核普通方案 10% 改善，并有新增图覆盖；对 008 的 V 等待隐藏解释通过完整 trace 检查。

**反证／停止条件：**只有 008 有效；同 guard 新图反复退化超过 5%；必须删除 COPY／MEM 才能“解释”；检测成本不抵收益。**依赖：**PB00、Pro C 的 primitive 规格。**可并行性：**可与 PB02–PB04 并行。**预计资源：**单 CPU worker、每次 E0 最多 30 秒，工程量约 0.5–1 天。**成功后解锁：**受守卫资源词 macro，而不是通用最优性声明。

---

### PB02-EARLY-PREFIX：让 Q1 生成真正能提前的前缀

**命题：**051 的收益来自分离晚输入门控之外的工作；提升来自候选生成，而不是对零提前量候选继续调排序。

**输入／branch／commit：**Q1 `4dff90e…` 的 `profile_candidates.py`、`Q1_TRACE_EXPLANATION`、`Q1_TRACE_PROSPECTIVE`；051 父计划及 cut4/cut5，003/008/044 负例。

**允许修改范围：**独立 `PB02-EARLY-PREFIX` 目录。**禁止修改范围：**F0，特别禁止把旧父计划的时刻冒充新 incumbent 时刻。

**实验协议：**从真实 Task Step1 和晚输入依赖生成前缀；不读取候选分数选阈值。与旧两 split 槽位生成器比较，同四候选、同总时间；每次重新构造并检查联合环。最多 **40 次新 E0**。

**输出：**非零提前量候选覆盖率、父子门控对齐、非局部 DDR 响应、按调用数及墙钟的 incumbent 曲线。

**验收指标：**至少两个未用于规则选择的父计划产生非零提前量候选，并取得同预算净质量或达到同质量的时间收益。

**反证／停止条件：**仍全部为零；生产者重定时抵消提前空间；只有 051 有效。**依赖：**PB00。**可并行性：**与资源词、Cache 任务独立。**预计资源：**单 CPU worker，建议 2 GiB 采样阈值，工程量约一天。**成功后解锁：**Q1 的 release-aware split macro 候选。

---

### PB03-BACKING-CLIFF：识别 Q1 合并／拆分的内存悬崖

**命题：**首次 backing 物化、已有 backing reload、L1／UB 与 incarnation 链，可以解释部分 split／merge 的突变；是否值得做在线局部评估器仍需成本检验。

**输入／branch／commit：**Q1 `4dff90e…`；MAIN `f27ef37…` 的 Step2/3；审查 `e1575a1…`；044 好／坏 merge 和零 spill 坏 split。

**允许修改范围：**独立 `PB03-BACKING-CLIFF` 目录。**禁止修改范围：**F0，尤其不改变官方 victim 选择。

**实验协议：**逐记录区分 `original_copy_in`、`new_spill_out`、`reused_spill_out`，保留 pos、version、next-use；比较纯字节、闭区间压力、backing-aware 三种有限排序。另做少量临界容量诊断副本。最多 **32 次新 E0**。

**输出：**条件 spill 记账、guard crossing 表、误排反例、检测成本和 ABSTAIN 条件。

**验收指标：**式（PB-06）逐项匹配 E0；至少两条可复核的状态边界；新排序有前瞻收益，或准确得出“不值得在线使用”。

**反证／停止条件：**状态增加后仍系统误排；依赖事后候选结果；检测比精确比较更贵。**依赖：**PB00。**可并行性：**可与 PB02 并行，只读共享 Task 剖面格式。**预计资源：**单 CPU worker，约 0.5–1 天。**成功后解锁：**backing-aware macro guard 或局部评估器的可靠拒答域。

---

### PB04-CACHE-INVERSION：定位 Q3 排名翻转的相位条件

**命题：**080 的 Q2/Q3 排名反转来自可定位的 FIFO 准入／淘汰和资源池相位，而不是可用 hit rate 一句话概括。

**输入／branch／commit：**Q3 `a4e7ee1…` 的 080 两计划及配对记录；008 零 hit 对照；044 命中率下降却更快的对照。

**允许修改范围：**独立 `PB04-CACHE-INVERSION` 目录。**禁止修改范围：**F0，不把 LRU、预取或立即插入替代 FIFO 完成时插入。

**实验协议：**同字节计划跑 Q2/Q3；记录完成准入而非仅访问次序，保留在飞 hit 被淘汰后重新插入。少量 L2 临界容量上下邻点作诊断；若更早 guard 已变化，标成复合变化。最多 **32 次新 E0**。

**输出：**准入／淘汰与两池事件 diff、跨 q 反转见证、执行前 detector 候选和反例。

**验收指标：**复现固定反转，且至少预测一个新相位／边界单元；域外能够拒答，不用 Q2 排名硬剪枝。

**反证／停止条件：**只有事后 hit rate 可解释；同 guard 邻居无一致响应；适用率和检测成本不合算。**依赖：**PB00。**可并行性：**独立于 Q1 两任务。**预计资源：**单 CPU worker，约一天。**成功后解锁：**Q3 专用共享输入／相位 macro 候选。

---

### PB05-CORE-CONTINUATION：机制能否沿 1–5 核延续

**命题：**保留旧方案结构的 continuation，能在部分已验证机制域内比同预算冷构造更快地得到同等或更好方案。

**输入／branch／commit：**Q1 `4dff90e…`、Q3 `a4e7ee1…`；EXACT `5bfe53a…`、NATIVE `03f02e7…` 仅在相应验收域可选。中心为 008/Q2、051/Q1、080/Q3。

**允许修改范围：**独立 `PB05-CORE-CONTINUATION` 目录。**禁止修改范围：**F0，不把前一核数的搜索成本藏掉。

**实验协议：**每一步比较追加空核、一次 warm macro、一次 cold 构造；保留旧标签和 mapping 顺序。记录整条轨迹及边际成本；最多 **48 次新 E0**，不使用未经本域验收的原生倍率作为成本承诺。

**输出：**各核数计划、guard 值、行为签名、CRG-19 曲线、质量—时间轨迹。

**验收指标：**空核嵌入在指定指标投影上保持一致；warm 不靠额外预算赢；至少一个跨核族出现可重复的质量或时间收益。

**反证／停止条件：**换核数立即失效且须大改全计划；额外修复和评估抵消收益。**依赖：**PB00，以及对应已固定的 PB01/PB02/PB04 macro。**可并行性：**三条轨迹可并行，单轨迹内部串行。**预计资源：**每轨迹单 CPU worker，宏已齐备时工程量约半天。**成功后解锁：**多核尺度上的 macro 路由与 warm-start 策略。

---

### PB06-LAG-CELLS：只做值得做的仿射参数压缩

**命题：**完整排序的精确整数区间可以减少重复构造；它既不证明哪个区间最好，也不自动构成不同 plan 的行为商。

**输入／branch／commit：**Q3 `a4e7ee1…` 的 `AFFINE_LAG_EQUIVALENCE.md`、`construct.py`，以及本轮纯整数测试。

固定前缀和局部 job 位置后，相邻操作保持顺序的条件为：

$$(j_u-j_v)\ell
\le
p_v-p_u-\mathbf1[u>v].
\tag{PB-07}$$

全部相邻约束与正整数域相交，得到一个精确区间；不同核独立 lag 时是区间的笛卡尔积，不能默认为一个标量扫描。当前线上代码仍是固定 `affine_eighth`，并没有因为这个证明自动获得提速。

**允许修改范围：**独立研究参数接口与 `PB06-LAG-CELLS` 目录。**禁止修改范围：**F0，不改写旧固定 1/8 算法身份。

**实验协议：**检查区间内部和两侧边界的完整有序 plan 字节。强基线采用“参数扫描＋plan hash 去重”，两方法都不重复评价相同 plan。最多 12 个新 distinct plan 做 Q2/Q3 配对，即 **24 次 E0**。

**输出：**精确区间、构造去重率、跨边界编辑大小、同时翻转多个比较的反例和总成本。

**验收指标：**区间字节一致性零差异；至少减少一半重复构造或产生净时间收益；相同 distinct-plan 集合的最好值不损失。

**反证／停止条件：**参数几乎每变一次就换 plan；区间计算更贵；多核组合爆炸。**依赖：**PB00 的原图与身份。**可并行性：**与 PB01–PB04 独立，结果可供 PB05 使用。**预计资源：**CPU 单 worker，约半天。**成功后解锁：**参数层去重和 guard 边界见证，而不是虚假的评估吞吐或行为商成绩。

---

**最终取舍：先补齐 PB00，再优先并行验证 PB01、PB02、PB04。**PB03 是当前 Q1 生命周期风险模型的重要支撑；PB05 应消费已经固定的 macro，而不是边迁移边重新调参；PB06 只有在确实要扩展仿射参数族时才值得接入。

目前最值得追求的成果不是“找到一个 7 倍参数”，而是：**说明哪类图、哪种官方语义、哪些核数与状态条件下，一个明确的合法操作会改变哪些行为，并能在没看结果之前预测它值得尝试。**
