# 数据契约

- `raw/`：原始题目附件，原件只读，不在此目录写入预处理产物。
- `processed/`：由脚本产生的清洗数据，生成后作为下游只读输入。
- `manifest.csv`：来源清单；每份数据填写项目相对路径、来源 URL/附件说明、获取日期、SHA-256、单位和备注。

导入真实数据前确认来源与共享范围；大数据由团队约定 LFS 或统一存储，并在清单中写清获取方式。当前尚无真实比赛数据。demo 的合成输入写入 `results/demo/input.csv`，不会写入 raw/。
