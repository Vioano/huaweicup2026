图 5-4 用 case003/k2 的 R05 官方配对负例与 S16 完整 500 格对照，解释"搬运减少仍可能变慢"。面板 1：seed（旧初解，计划 sha256 e6c87dd7…）与 recovered（R05 恢复候选，d6da7474…）两方案在 2 核上的配对时间线（每核一行，色深=500-cycle 桶内活跃操作数），红色虚线为各自官方 makespan 端点（248,166 / 254,508 cycles，与 result.json 一致）。面板 2：搬运量分解（字段与 result.json data_movement_bytes 一致）——基础 COPY（original_graph_copy_bytes）两方案同为 1,284,264 B；新增 COPY（added_copy_bytes，切分新增）seed 5,067,158 B → recovered 3,923,290 B；scheduled COPY 合计 6,351,422 → 5,207,554 B（降 18.00964%），但官方 Makespan 反升 6,342 cycles（+2.55555%），两方案 spill 均为 0。面板 3：S16 固定提交 70f2e8bd 的完整 500 格配对散点（前版 2794ceba vs 主方法 c665）：横轴 ΔMakespan、纵轴 Δ额外 DDR（均为绝对量），保留全部改善/持平/退化——同降 55 格、Makespan 降但 DDR 升 199 格、降且不变 14 格、两项均不变 232 格；120 个旧额外 DDR=0 的格不计算无定义的相对变化，一律以绝对量展示。口径声明：基础/新增/scheduled COPY/spill 各按 result.json 原字段定义，不混用；单例 R05（S15，e6ae369 批次）与全量批次（S16，70f2e8bd 批次）不是同一版本对，面板分别标注；COPY 活跃时长占比不称为带宽利用率；官方值与下界之差不称为实测 DDR 等待。单位：Makespan=cycles，搬运量=B。
