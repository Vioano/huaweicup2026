图 5-4 用 case003/k2 的 R05 官方配对负例与 S16 完整 500 格对照，解释"搬运减少仍可能变慢"。面板 1（S15 R05 @e6ae369）：seed（旧初解，计划 sha256 e6c87dd7…）与 recovered（R05 恢复候选，d6da7474…，图例行标签缩写 rec）两方案在 2 核上的配对时间线——每方案每核一行，操作按 trace 原始管道类别（PIPE_MTE2 搬入 / PIPE_MTE3 搬出 / PIPE_V 计算 / PIPE_M）以真实 start/end 画条，task（SUBGRAPH）区间仅作浅灰背景、不计入任何操作计数；红色虚线为各自官方 makespan 端点（248,166 / 254,508 cycles，与 result.json 一致），且只贯穿所属方案的两行。1–6 cycles 的微操作在 25 万 cycle 全量轴上低于 1 px，完整起止见 events.csv（67,385 行，零时长事件 0 个）；本面板不称带宽利用率。面板 2（S15 R05 @e6ae369）：搬运量分解（字段与 result.json data_movement_bytes 一致）——基础 COPY（original_graph_copy_bytes）两方案同为 1,284,264 B；新增 COPY（added_copy_bytes，即 results.csv 的 extra_ddr）seed 5,067,158 B → recovered 3,923,290 B（−22.57%）；scheduled COPY 合计（=基础+新增，另列于 results.csv 的 scheduled_copy_bytes）6,351,422 → 5,207,554 B（−18.01%）；"新增 COPY 下降"与"总 COPY 下降"分别表述，不混用。官方 Makespan 反升 6,342 cycles（+2.56%），两方案 spill 均为 0。面板 3（S16 @70f2e8bd，前版 2794ceba → 主方法 c665，与面板 1/2 的 S15 单例不是同一版本对）：完整 500 格配对散点，横轴 ΔMakespan、纵轴 Δ额外 DDR（均为绝对量），保留全部改善/持平/退化——同降 55 格、Makespan 降但 DDR 升 199 格、降且不变 14 格、两项均不变 232 格；120 个旧额外 DDR=0 的格不计算无定义的相对变化，一律以绝对量展示（相对变化完整精度值见 tradeoff.csv 的 relative_ddr 列，非零分母 380 格全部通过 rel_tol=1e-7 复核）。口径声明：基础/新增/scheduled COPY/spill 各按 result.json 原字段定义，不混用；extra_ddr 一律指新增 COPY；单例 R05（S15，e6ae369 批次）与全量批次（S16，70f2e8bd 批次）不是同一版本对，面板分别标注；COPY 活跃时长占比不称为带宽利用率；官方值与下界之差不称为实测 DDR 等待。单位：Makespan=cycles，搬运量=B。提取与绘图：extract_inputs.py 显式接收 --r05-zip / --s16-report 并核对两项固定来源 SHA-256 后生成五张交付 CSV，plot.py 只读交付 CSV 复现图件；原始 Trace 不复制入图包。
