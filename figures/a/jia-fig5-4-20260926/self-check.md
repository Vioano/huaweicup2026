# 图 5-4 自查记录（甲，v1，2026-09-26）

## 实际执行结果（本会话为该目录唯一写者，声明 5846554869）

- 命令：`.venv/Scripts/python.exe figures/a/jia-fig5-4-20260926/plot.py`（工作目录=仓库根），退出码 0；同一次运行产出 figure.svg 与 figure.png（300 dpi，6.5×7.2 in，165mm 版心）。
- 数据完整性断言通过：两方案事件表最晚结束 == 各自官方 makespan（248,166 / 254,508 cycles）；S16 恰 500 格、四类计数 55/199/14/232 与证据 S16 文字一致；两方案 spill=0 断言通过。
- first_load.zip（S15 result.zip）SHA-256 实测 c90065d9d90aae3be4144080483e3ac394a6ef150f92fc7d4450d81114ba2c66，与 S15 记载一致。

## 逐项对应派发单

1. **固定 case003/k2 的 R05 两方案**：seed=旧初解（plan e6c87dd7…）、recovered=R05 恢复候选（plan d6da7474…），均来自 e6ae369 批次 result.zip（batch.json rows 登记身份）；配对事件、周期与搬运字节全部解析自 zip 内 result.json/trace.json。
2. **同轴时间线**：面板 1 四行（2 方案 × 2 核），500-cycle 桶占用密度热条；端点虚线=各自官方 makespan；"色深=桶内活跃操作数"不是吞吐率声明。
3. **搬运量分解**：面板 2 三组柱=基础/新增/scheduled COPY 合计（原字段名标注），数值标签逐柱给出；分解关系 total=base+extra 在图中可加和验证；spill=0 如实标注。
4. **S16 全量散点**：面板 3 绘 500 格（ΔMakespan, Δ额外 DDR），四类着色含格数；旧额外 DDR=0 的 120 格不计算相对变化，全部以绝对量展示。
5. **口径**：COPY 活跃占比未出现，"带宽利用率"未出现；E0 与下界之差未出现；单例与全量面板分别标注批次与版本。

## 缺口

- 无（S15/S16 固定原件均可读，哈希核对一致；不新增实验）。
