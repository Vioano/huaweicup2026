# case_026 实例图 X/Y 标签勘误候选

状态：**固定图像已生成，待论文组织任务核对与入稿**。这是原图的局部标签勘误，不是新的实验或新的算法结果。

| 文件 | 用途 |
| --- | --- |
| `fig-p1-cuts-xy-corrected.png` | 可供合稿检查的新版 1536×1024 PNG；原图不覆盖 |
| `imagegen-letter-proposal.png` | 生图模型仅修改两个字母的候选；**其余像素发生漂移，不可直接入稿** |
| `build_label_fix.py` | 将候选字母的局部像素转入固定原图，并校验来源与输出 |

原图：固定 Git 提交 `6ee1684675e92b65ddea96a5bb2b6dacefefafef`，`paper/manuscript-v1/figures/fig-p1-cuts-zh.png`，SHA-256 `ce7f192887ec4860ff4767b7322907c36605465f34c81975d67e5ed52a8f9c3f`。论文检查点 CP06 的 `main.tex` 第 540–542 行引用该图；该检查点图件清单仍记录**旧图**的哈希，不能在旧检查点内静默换图。

勘误依据是当前论文组织任务对固定算法 `834d8c957538ee069c66aadac9509552a4cc69d7` 与真实 `case_026`、5 核实例的核对：X 为从原核心转至 Core 1 的 `sg6`（节点 714、716、722）；Y 为仍在 Core 0 的 `sg0`（节点 710、712、720）；J 为 Core 0 的 `sg5`（节点 724、726）。因此蓝框标题改为 `Y · sg0 · Core 0`，绿框标题改为 `X · sg6 · Core 1`，橙框不变。

按 1536×1024 RGB 像素独立比较，新旧图只有两个字母位置的 1245 个像素不同：左标题 x=841–874、右标题 x=1171–1209，y=58–86；两个限定区域之外**零像素变化**。节点、边、箭头、Core、DDR/COPY 数据、图例和底部指标都保留旧图原像素。新版 SHA-256：`32c7183d3dc385f5c3ed305fceb21f7ddddc25d7488769cc226442e156fef522`。已在原分辨率目视检查文字位置，没有观察到字母残影。

建议图注：在右半幅，节点 714、716、722 对应的计算任务从原核心调整至 Core 1，记为 X（sg6）；节点 710、712、720 对应的计算任务仍在 Core 0，记为 Y（sg0）；节点 724、726 所对应的汇合任务 J（sg5）也留在 Core 0。图中数值继续沿用原实例，不因标签更正而重新计算。

未核项：本次只核两字母及图像不变范围，没有重新运行求解器/E0，也没有独立复核原图的 44,114→42,014 cycles、COPY 字节、图中 `t719/t721/t723` 含义和最终正文中的论证。论文组织任务须把本图与对应段落、时间线、图注和新版 PDF 逐项对齐；Fang 可选版或进一步重画，但未经核对不得标成入稿通过。
