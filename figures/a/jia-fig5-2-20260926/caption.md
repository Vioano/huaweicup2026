图 5-2 展示问题二闭区间容量条件（式 (5-12)/(5-13)，P2 初稿 5.5.1）在一个手算可核小型结构示例上的机制：上图为按物理身份去重的 9 个张量生命周期区间（蓝色 L1 池、橙色 UB 池），区间端点 f(t)、ℓ(t) 为该张量第一次、最后一次被触碰的 Step2 操作序号，两端闭合——同一步的输入与输出在执行前的容量检查中需要同时存在（紫色竖带标出 L1 步 2 的 T3 输入×T4 输出、UB 步 4 的 U1 输入×U3 输出两处共同驻留），首次即末次触碰的张量（T6、U3 尾段）不遗漏。下方两图为按式 (5-12) 逐位置累加的 L1、UB 驻留字节阶梯及固定配置容量线（L1=524288 B=512 KiB、UB=131072 B=128 KiB，表 5-1），黑点为三个手算核对位置：L1 峰值前 425984 B（步 1）、峰值处 524288 B（步 2，恰等于容量线，式 (5-13) 的 ≤ 仍成立）、释放后 65536 B（步 6）；UB 峰值前 49152 B（步 3）、峰值处 73728 B（步 4）、释放后 24576 B（步 6）。横轴为 Step2 执行序号 i，不是时间（cycles 与执行序不混用）。本图为静态闭区间估计，不代表实际并行 trace 峰值，也不是 Step3 成功或 Makespan 最小的判据；未使用官方 spill 记录，图中不绘制虚构 spill 事件。单位：字节 B。
