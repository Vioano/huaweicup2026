# 图 5-2 自查记录（甲，v2，2026-09-26）

## 本版变更（回应返工单 F52-R01/R02，意见 5845634878）

- F52-R01：occupancy.csv 由 12 行扩为 **16 行**，补 (0,L1,0)、(0,UB,0)、(7,L1,0)、(7,UB,0)，覆盖完整范围 i=0..max(ℓ)+1=7；plot.py 改为从完整 steps 重绘阶梯与横轴，右端回落到 0 全部释放可见。第 6 步标注改为"部分释放后"，第 7 步标注"全部释放 0"。
- F52-R02：删除 plot.py 底注、caption 中"首次即末次触碰的张量（T6、U3 尾段）不遗漏"的不实断言（实际 9 行 lifetimes 无 start==last）；改为"末触步仍计入占用，下一步释放；T6=[5,6]、U3=[4,6] 即两端闭合例证"。面板 1 内的"（首触即末触）"条件标注分支一并移除。自查、图注、底注同步。
- 建议项（非门槛）：底部长机制说明已精简并移入 caption；底注只留闭区间例证与 spill 限定。
- lifetimes.csv、capacity.csv、handcheck.csv 未改动（handcheck 6 行保留，未重新造例子）。

## 实际执行结果（本机制图环境）

- 命令：`.venv/Scripts/python.exe figures/a/jia-fig5-2-20260926/plot.py`（工作目录=仓库根），退出码 0。
- 脚本内置断言全部通过（v2 加强）：
  1. occupancy.csv 恰有 16 个唯一 (step,space)（i=0..7 × L1/UB），且由 lifetimes.csv 闭区间重算的逐位置占用与之全部相等；
  2. handcheck.csv 三个位置（×两池共 6 行）与占用曲线数值一致；
  3. 命题 2 静态条件 max_i H_p(i) ≤ C_p 两池均成立（L1 峰值 524288 = 容量取等；UB 峰值 73728 < 131072）。
- 输出：figure.svg（423.83×502.34 pt）/ figure.png（300 dpi，1770×2093 px）。SVG 宽 423.83 pt≈149.51 mm，按 165 mm 正文版心插入时放大 1.104 倍，最小字号 6.2 pt≈插入后 6.85 pt。

## 完整占用序列核对（F52-R01 完成判据，i=0..7 独立累加 sum(bytes for f≤i≤ℓ)）

- L1：[0, 425984, 524288, 425984, 229376, 196608, 65536, 0] ✓（与返工单给定序列一致）
- UB：[0, 32768, 49152, 49152, 73728, 40960, 24576, 0] ✓
- 主图展示首尾 0，容量线不变（L1 512 KiB、UB 128 KiB）。

## 手算核对（三个位置 × 两池，保留 v1 口径）

L1（步 2 峰值处恰等于容量线）：
- 峰值前（步 1）：T1 196608 + T2 131072 + T3 98304 = **425984 B**
- 峰值处（步 2）：T1 196608 + T2 131072 + T3 98304 + T4 98304 = **524288 B**（= C_L1，式 5-13 取等仍满足 ≤）
- 部分释放后（步 6）：仅 T6 = **65536 B**（T5 于步 5 末释放；T6 末触步 6 仍计入，步 7 释放为 0）

UB：
- 峰值前（步 3）：U1 32768 + U2 16384 = **49152 B**
- 峰值处（步 4）：U1 32768 + U2 16384 + U3 24576 = **73728 B**
- 部分释放后（步 6）：仅 U3 = **24576 B**（U3 末触步 6 仍计入，步 7 释放为 0）

## 验收标准逐项

1. 物理张量按身份去重（9 个物理张量，无同物重复计数）；区间端点为闭区间语义（式 5-12 的 f(t)≤i≤ℓ(t)），三处手算与曲线一致（脚本断言 + 上列手算）。通过。
2. 横轴明确为 Step2 操作序号（执行序），图轴标签与图注均声明"非时间"；L1 与 UB 分面板、分容量线各自核对。通过。
3. 图题与图注显式声明"静态估计，非实际并行峰值"；无官方 spill 记录，未绘制 spill 事件。通过。

## 数据口径

- 手算示例为派发单授权路径（"可手算示例，或绑定固定计划的真实完整物理序列"），图题/图注/来源记录三处标注"结构示例（手算可核）"；容量值取 P2 初稿表 5-1 固定配置（非自造）。
- 未完成项：无（如需绑定固定计划的真实物理序列版本，待有固定计划派生表后另行增补）。
