# 图 4-2 自查记录（甲，v4，2026-09-26）

## v4 修订核对（逐条对应工作台 03:50 意见 914acd15）
1. audit.command → 改为单个字符串字段（含换行的四条实际命令），tool_versions 单列；预览命令写全输入输出路径与有效 Python 字符串，无省略号。机检 strip 兼容。
2. 插入宽度物理可读性 → 规范版重排为上下组合三面板（同图号 fig-4-2、同节点身份），画布 720×1080；按 template-2026（A4，左右边距 22.5mm，正文宽 165mm）插入：最小字号 10px≈6.5pt、主标签 11-12px≈7.2-7.8pt（接近 8pt 目标）；1500px 物理宽度预览 preview-insert-width.png 目检无放大可读、无穿字/遮挡/裁切。旧口径 146.6mm 换算约 5.8pt 亦已在 audit 注明。
3. self-check.md → 更新至 v4（本文件），反映当前表头（id/subgraph/core/order）与本版版式检查；v2→v4 列名变更说明保留于 audit.sources.note，数据内容未重造。

覆盖核对：12 节点三面板恰好覆盖一次；13 条依赖与 (a) 一致；Task 次序满足依赖（Task 4 在 Task 1/2/3 之后）；DAG 无环。
未完成项：无。
