# 图 4-2 自查记录（甲，v5，2026-09-26）

## 本版（v5）依据
- 实际论文模板：paper/template-2026/gmcm2026.cls（origin/main@e82c20098eb53cfb00d5f2287173f6894b3fb291），正文宽 165mm、正文高 248.5mm。
- 实际导出边界：SVG viewBox 770x1100（draw.io CLI 导出，含边缘留白）。按 165mm 宽插入，高 235.7mm，正文页内可容纳（正文高 248.5mm，未计图注；若加图注需整页放置或微缩，最终排版由整稿决定）。
- 字号换算：最小 10px 约 6.07pt；主标签 11-12px 约 6.7-7.3pt。约 8pt 仅为排版建议非硬门槛；本版以 165mm 物理宽度预览目检可读为准。
- 生成命令：见 audit.json.command（单字符串，含导出/校验/预览完整输入输出与工具版本）。

## 版式遮挡修复（对应 04:20 意见第 3 条）
- (b) 阶段 0 框加宽且 s/a 节点右移，标签与节点不再重叠；
- (c) 三条核心泳道各加高 14px 作为独立标签带，Task 块整体下移，Core 0/1/2 编号不再被 Task 框遮挡；
- 边缘三个虚线边标签（跨核依赖/d1→r/d3→r）在导出边界处被裁切，已移除（信息保留于图例与对应表）；
- 重导出后 SVG/PNG 目检：无穿字、无遮挡、无裁切。

## 历史稿区分
- v1=be5863dd 前身；v2=e9f3a520；v3=98ae588e；v4=c44e4d40（重排上下组合，但 self-check 哈希过期、command 仍为对象、遮挡未除，本版已修正）。
- 本版 v5：版式遮挡修复 + 打包一致性（command 单字符串、self-check v5、哈希按最终字节重算、audit.files 不含 audit.json 自身）。

## 检查结论（v5）
- 12 节点/13 依赖、DAG 无环、同核顺序相容（工作台已独立复核确认的部分未重做）。
- nodes.csv 列 id/stage/subgraph/core/order/note；core 从 1 开始，图面 Core 0/1/2 对应 CSV 1/2/3。
- 结构示例（自整理，非实测数据）；条块宽度无时间含义。
