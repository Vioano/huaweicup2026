# 图 4-2 自查记录（甲，v2，2026-09-26）

## v2 修订核对（逐条对应工作台退回意见）
1. 边数口径 → 核定源文件 (a)/(b) 均 13 条边（ae1-ae13 / be1-be13），设计即为 13；DELIVERY/图注统一改为 13 条，未补边凑数。
2. 版式 → 前沿框扩大至完整包住 d1/d3；be2/be4 折线改走外缘，不再压阶段1说明；(c) 跨核依赖线改从 Task0 左侧沿外缘下行，不穿 Task0 文字；归约尾标签移至框外右侧。
3. 机器可复核映射 → 补 nodes.csv（12 行，逐节点 subgraph/core/order_in_core）与 edges.csv（13 行）及 audit.json（真实哈希）。core 列按模板从 1 开始，图面 Core 0/1/2 对应 CSV 1/2/3（转换见 audit.core_convention）。
4. 插入宽度重验 → 生成 preview-insert-width.png（1600px 宽插入观感），逐项目检：箭头不穿文字、图例与底部说明可读、无缺字裁切；限定语（结构示例/条宽无时间含义）保留。

覆盖核对：12 节点三面板恰好覆盖一次；13 条依赖与 (a) 一致；Task 次序满足依赖（Task 4 在 Task 1/2/3 之后）。
未完成项：无。
