# v10 图件矢量复刻：固定源与文字审查

本清单只针对冻结 v9 的实际入稿文件。输入来自 `f5673a7e3cb3a5f0f7f269e1f8f03120ee19702c` 的 `paper/manuscript-v1/checkpoints/v9/figure-manifest.json`。此阶段检查源与几何，不修改 v9、正文或构建器；下列图内文字是**原图抄录和待审项**，不是获准用于 v10 的词表。

| 固定稿图号、页码 | v9 实际文件（像素） | 相同画面且可逐形编辑的源 | 处理 |
| --- | --- | --- | --- |
| 图1.4-1，p.7，`fig:progression` | `fig-progression-v3.png`，1536×1024，SHA-256 `d4a75c65f85de8c7b83beecf9b46d05fa3295061e666bad3849c6eed8fbf1d43` | 未找到。`fig-progression.svg` 与 `.json` 是较早、结构和文字不同的矢量示意，不是 v9 原画面的可编辑源。 | 复刻四个框组及其中的框、箭头、文字。 |
| 图3.2-1，p.15，`fig:framework` | `fig-framework-v2.png`，1448×1086，SHA-256 `b751a7c597f8e20d9b1a867796faab6621c08f066f0bd08542a5eed4dad89e8d` | 未找到相同画面的 Draw.io/SVG；仅有另一张 `fig-framework-zh.png`。 | 复刻三路构造示意与下方共同检查步骤。 |
| 图5.3-1，p.36，`fig:hypercut` | `fig-hypercut-v2.png`，1942×809，SHA-256 `8a28dda4bec1f03b2d582e85d8fb538081d56ebd711682c35b119a75574e61d6` | 未找到。`fig-hypercut.svg` 与 `.json` 是早期内容不同的可编辑方案。 | 复刻三种放置关系及数值。 |
| 图4.3-1，p.24，`fig:p1-cuts` | `fig-p1-cuts-xy-corrected.png`，源文件1536×1024，SHA-256 `32c7183d3dc385f5c3ed305fceb21f7ddddc25d7488769cc226442e156fef522`；v9构建器以 `trim=0 235 0 0,clip` 裁掉底部235像素后入稿 | 未找到。`fig-p1-cuts.svg` 与原 PNG 的版面、X/Y 标记和文字不同。 | 只复刻实际可见的左右结构图，不重新加回源PNG底部的统计、图例或子图文字。 |
| 图4.2-2，p.22，`fig:fang-fork` | `fig4-2-fang-A.png`，1536×1024，SHA-256 `76b1b13c073f404dc7c51e60f7ceecbcd51aaeb5cc9fdc3ea775614061592b3f` | **有相关的 Draw.io 源，但不是同版画面**：固定提交 `49280ec` 的 `figures/a/fig42-beautify-20260926/source/fig4-2-fork-join-partition.drawio` 含 77 个单元、0 个嵌图；该源导出为纵向三大段 1448×2107 PNG，而 v9 用户选定 A 版为横向双结构加下方三核任务行。`fig4-2-A-confirmed.png` 与 v9 PNG 哈希相同。 | 复用原源中的可核语义和连接关系，按用户选定 A 版重新排版；不能把旧 Draw.io 直接冒充像素级复刻。 |

## 图内文字逐图清单与需交 Gemini 的问题

- **图1.4-1**：`问题一/问题二/问题三`；`sg0 · Core 0`、`sg5 · Core 0`、`Task · Core 0`；`720/724`、`COPY_OUT/DDR/COPY_IN`、`t721 · 2 B`；`每个子图对应一个Task`、`数据保留在这个Core中`、`增加共享只读Cache`；下方 `问题三的读取位置`、`COPY_IN`、`命中？`、`是/否`、`Cache 250 B/cycle`、`DDR 60 B/cycle`、`传输完成后放入Cache`。需核对 Task 与子图关系、Cache 查询条件、速率的准确来源和表达；不能用“数据保留在这个Core中”等含混句代替具体存取行为。
- **图3.2-1**：`读入计算图与固定配置`；三列 `问题一/二/三`、`拆分独立分支`、`安排计算位置和顺序`、`调整树状计算的顺序`、`数据`、两处 `读`、`Core 0/1`；底部 `生成完整的切分与调度方案`、`检查操作覆盖与执行顺序`、`依据下界排除不可能更快的方案 适用时`、`完整评价并比较完成时间`、`输出最佳合法方案`。需明确各步适用条件、评价程序及候选范围；有限候选中的最优不能写成全局“最佳”。
- **图5.3-1**：三组 `Core 0/1`、`u/v₁/v₂`、`t · 8 B`、`λₜ = 2 / 2 / 1`、`16 B / 16 B / 0 B`。数值要标明究竟是该张量引起的边界复制字节数；λₜ 定义和 16/0 B 的计算条件应放图注或邻近正文，不凭单图暗示真实完成时间收益。
- **图4.3-1**：实际入稿可见部分有 `sg0 · Core 0`，`Y · sg0 · Core 0`，`X · sg6 · Core 1`，`J · sg5 · Core 0`；操作编号 `710/712/714/716/720/722/724/726`；`t721/t723/t719 · 2 B`。源PNG底部的 `(a) 原始划分`、`(b) 分支援助细化`、`case_026 · 5 Cores | 5 → 7 Tasks | 44,114 → 42,014 cycles | −4.76% Makespan | +163,852 B COPY` 与 `依赖/跨子图/图外端口/ADD 节点编号` **在 v9 已经裁掉**，不应出现在复刻图里。X/Y 与操作归属是固定稿勘误结果，必须逐项保留；底部长统计与限定条件由图注/邻近正文承担。
- **图4.2-2**：结构节点 `s/a/b₁/c₁/d₁/b₂/d₂/b₃/c₃/d₃/r/t`；`阶段 0/1`、`K = 3`；三行 `Core 0/1/2`、`Task 0: s → a`、`Task 1: b₁ → c₁ → d₁`、`Task 2: b₂ → d₂`、`Task 3: b₃ → c₃ → d₃`、`Task 4: r → t`；图例 `同一 Core 内的 Task 顺序` 与 `跨 Core 的数据依赖（经 DDR）`。阶段与核心数、任务与操作节点的区别应在图注先界定。若图内保留短标签，须与全文官方中文及精确英文原词保持一致。

## 对齐和停止条件

复刻以**已审批的新词表**替换原图文字；几何尽量以原 PNG 的像素坐标为基准。图内只保留读图必需的短标签，长标题、评价条件、统计结论和限制由论文 LaTeX 图题/邻近正文承担。先保存每个可编辑形状及文字对象，再导出 SVG、PDF、PNG；用原图叠合检查位置、尺寸和颜色，在论文实际宽度下复查字号与线条。因文字经规范修订、原图带有光晕/随机纹理，不能承诺逐像素完全相等；需列出测得的几何偏差和有意改变的文字。未经最终词表，不将候选导出物当作可入稿版本。
