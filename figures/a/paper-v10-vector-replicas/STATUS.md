# v10 矢量复刻交接状态

2026-09-27：用户要求先讨论论文篇幅和语言策略，监督会话通知本专项暂停正式改图和入稿。此目录保存复工起点。

- `SOURCE_AUDIT.md`：冻结 v9 五张优先位图的固定哈希、页面、可编辑源查找结果和原图文字清单。
- `make_draft.py` 与五份 `*-geometry-draft.drawio`：依据原 PNG 像素坐标手工建立的**可编辑几何草稿**。每个节点、文字框、连线均为 Draw.io 单元，XML 检查无整图嵌入。草稿暂用 v9 原文字，未经 v10 用词审批，不可入稿。
- 几何 QA 只做了四张临时 PNG 的初看；最后一次圆角、画布边界和 `p1-cuts` 标题间距修正后尚未重看。`fang-fork` 仅生成并检查 XML，未渲染检查。所有图均未完成原图叠合测量、论文宽度实查、SVG/PDF/PNG 正式导出，也未对接 v10 新稿。
- 复工时先取得论文监督会话审定的 `labels-ready.json` 固定版本，并逐项替换图内文字；保持图4.3-1在 v9 被裁掉的底部内容**不回到图内**。再导出、叠合比较、检查文字/箭头/线条和具体偏差，最后由写作会话决定入稿。

冻结 v9、正文、构建器和官方评价程序均未修改。图件与源稿“同内容”“同版式”“实际入稿”“最终人工验收”是不同状态，本目录目前只到前两项的草稿阶段。
