# 图 6-1 交付包｜P3 结构构造与缓存评价路线（v1）

- 图号：6-1　版本：v1（2026-09-26，按派发单 5844639959 制作；用户特别授权与 fig-5-1 v3 返工并行）
- 主责：甲（farmeruncle123）　输入数据约束：仅仓库固定提交（P3 初稿@1e35e399、算法源码@311322b、模板@e82c2009），无外部数据；0 新实验

## 本图对应派发要求

1. **主流程对应当前完整算法**（不沿用更早入口）：按固定入口 `src/q3/forest_solve.py` @311322b 逐行核对——witness_solve.evaluate_candidates 产生基准与结构候选（路由表 6.3.1 五行）→ encoded 字节去重 → pipe_bound.analyze 认证下界剪枝（式 6-10）→ 官方 P3 评价（total_e0_limit=3 含基准）→ 严格改进接受（strictly_lower_official_makespan）→ 合法方案及指标。
2. **不暗示求解器可修改官方 Cache 规则**：E0 节点显式标注「求解器不可修改官方 Cache 替换规则、容量与带宽」。
3. **同计划无 L2 对照独立成支**：从最终方案引出虚线分支——同一方案字节在无 L2 场景评 M₂（式 6-4，仅 Cache 场景变化；配对审计绑定计划身份：476 份复用/24 份另评）→ CacheGain 出口（C=M₃/M₂）；显式标注「仅报告指标，候选接受仍只比较 M₃，不与独立优化的 P2 主算法比较混同」。
4. **旁注区分未纳入扩展**：容量切分/共享输入流水驻留 DP（6.4）、部分预加载与部分分桶（6.4.4）、单尾切分与首波阶梯、四系数响应压缩（6.5）——标注「研究拓展，本入口不执行」，以开放虚线指回 route（仅表示"未执行"的参照关系）。
5. **剪枝适用检查与严格接受条件标在对应节点**：prune 节点含 L(P) 构造（必需依赖+同核 FIFO+跨核固定等待）、判据（L(P) ≥ 当前合法方案 Makespan → bound_pruned）与界不支持的处理；acc 节点含严格改进判据与保留条件。

## 新增排版规则执行（响应工作台箭头意见）

- **箭头严禁伸入节点框内**：全部连线端点显式锚定节点边框（exitX/Y、entryX/Y），waypoint 全部走框外走廊；导出 PNG 逐箭头目检——11 条边箭头均落在边框线上，无一条伸入框内。
- 边标签不压框内正文（e6「未被剪枝的候选」位于框间空隙；plan→nol2 长标签移除、由分支标题承载）。
- html=1 标签无裸 "<" 字符。

## 文件清单

| 文件 | 角色 | 说明 |
|---|---|---|
| fig6-1-p3-cache-route.svg/.png/.drawio | 主图 | viewBox 实际 711×738 |
| nodes.csv / edges.csv | 绘图输入 | 12 节点（id/stage/module/note）/ 11 边 |
| caption.md | 图注 | 图意、口径、边界 |
| preview-insert-width.png | 物理宽度预览 | 1500px |
| self-check.md / audit.json | 记录 | v1 |

## 已通过的检查（v1）

- validate.py 0 error / 25 warnings（容器与样式提示，无交叉警告——0 处边交叉）。
- 1500px 插入宽度目检：无穿字、无遮挡、无裁切、无缺字；箭头全部落边框（新增规则执行）。
- viewBox 711×738；165mm 宽插入高 171.3mm，最小字 ≈6.58pt、主标签 11–13px → 7.24–8.55pt；正文页内容纳，无需缩放。

## 未完成项

- 无（modern 变体不在本轮）。
