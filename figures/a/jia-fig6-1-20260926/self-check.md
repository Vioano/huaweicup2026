# 图 6-1 自查记录（甲，v1，2026-09-26）

## 实际执行结果（本机制图环境）

- 导出：draw.io CLI（--disable-gpu --no-sandbox）rc=0，SVG viewBox 711×738、PNG 1422×1476（scale=2）。
- validate.py：0 error / 25 warnings（容器与样式提示）；**0 处边交叉**。
- 尺寸与字号（fontSize×W/711×72/25.4）：最小 10px → 6.58pt@165mm；主标签 11–13px → 7.24–8.55pt。165mm 宽插入高 171.3mm，正文页内容纳，无需缩放。

## 验收标准逐项自查（对应派发单 5844639959）

1. **方法流程对应当前完整算法，不沿用更早入口** → 通过：主流程按固定入口 forest_solve.py @311322b 逐行核对绘制（witness_solve 基准与候选 → encoded 去重 → pipe_bound 下界剪枝 → E0 → 严格接受），无旧入口符号。
2. **不暗示求解器可修改官方 Cache 替换规则、容量或带宽** → 通过：E0 节点显式标注该边界；全图无修改官方行为的节点或箭头。
3. **同计划缓存对照与独立优化的 P2 主算法比较不混为一条分支** → 通过：对照为独立虚线分支（最终方案 → M₂ 同计划评价 → CacheGain 出口），节点文字显式「仅报告指标，候选接受仍只比较 M₃，不与独立优化的 P2 主算法比较混同」；主线候选接受节点只含 M₃ 严格改进。

## 箭头规则执行（响应工作台意见，新增硬性规则）

- 全部 11 条边端点显式锚定节点边框（exitX/Y、entryX/Y 声明），waypoint 走框外走廊（x=528/532 走廊、x=36 左缘）。
- 导出 PNG 逐箭头目检：11 个箭头全部落在边框线上，无一条伸入框内。
- 边标签不压框内正文（e6 在框间空隙；plan→nol2 长标签移除由分支标题承载）。
- 该规则已写入本队长期作图约定，适用于后续所有流程图。

## 交付包完整性

- SVG/PNG/.drawio/nodes.csv（12 行 4 列）/edges.csv（11 行 3 列）/caption.md/DELIVERY.md/self-check.md/audit.json（figure-auto-review-v1，哈希最后统一重算，不含 audit.json 自身）。
- 数值可复核：图中无统计数据节点；≤3 次评价、476/24 份配对、式编号均来自初稿 6.2/6.3/6.6 与固定源码。

## 历史稿

v1 为本图首版（旧图不计数，按派发单重新制作）。

未完成项：无。
